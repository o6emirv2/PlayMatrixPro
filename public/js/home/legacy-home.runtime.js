import { loadFirebaseWebConfig } from "../../firebase-runtime.js"; import { AVATARS, AVATAR_CATEGORIES, DEFAULT_AVATAR, AVATAR_FALLBACK, normalizeAvatarUrl, isCatalogAvatarUrl } from "../../data/avatar-catalog.js"; import { createAvatarPicker } from "../profile/avatar-picker.js"; import { createFramePicker } from "../profile/frame-picker.js"; import { ACCOUNT_PROGRESSION_VERSION, ACCOUNT_LEVEL_CURVE_MODE, normalizeAccountXpExact, getAccountLevelFromXp, getAccountLevelProgressFromXp } from "../../data/progression-policy.js";
 let initializeApp = null; let firebaseGetAuth = null; let firebaseOnAuthStateChanged = null; let firebaseSignOut = null;
let firebaseSignInWithEmailAndPassword = null; let firebaseCreateUserWithEmailAndPassword = null; let firebaseSendEmailVerification = null; let firebaseSendPasswordResetEmail = null; let firebaseVerifyBeforeUpdateEmail = null;
let firebaseGetIdToken = null; let firebaseReload = null;      const $ = (id) => document.getElementById(id);     const clamp = (n, min, max) => Math.max(min, Math.min(max, n));
    const formatNumber = (n) => Number(n || 0).toLocaleString('tr-TR');      const bindIfPresent = (id, eventName, handler, options) => {       const element = $(id);       if (!element) return null;
      element.addEventListener(eventName, handler, options);       return element;     };      function getGameAccentClass(game = {}) {
      const key = String(game.name || '').toLowerCase();       if (key.includes('crash')) return 'game-card--crash';       if (key.includes('satran')) return 'game-card--chess';       if (key.includes('pişti') || key.includes('pisti')) return 'game-card--pisti';       if (key.includes('pattern')) return 'game-card--pattern';
      if (key.includes('space')) return 'game-card--space';       if (key.includes('snake')) return 'game-card--snake';       return 'game-card--default';     } 
    function reportClientError(scope, error, extra = {}) {       try {         if (typeof window.__PM_REPORT_CLIENT_ERROR__ === 'function') {           window.__PM_REPORT_CLIENT_ERROR__(scope, error, extra);         }
      } catch (_) {}     }      function safeExecute(label, task) {       try {
        return task();       } catch (error) {         console.error(`[PlayMatrix] ${label}`, error);         reportClientError(`safeExecute:${label}`, error, { source: 'script.js' });         return null;
      }     }      function safeRenderGameShowcaseSkeleton(count = 3){       try {         if (typeof window.renderGameShowcaseSkeleton === 'function' && window.renderGameShowcaseSkeleton !== safeRenderGameShowcaseSkeleton) return window.renderGameShowcaseSkeleton(count);       } catch (error) {         console.warn('[PlayMatrix] skeleton renderer fallback', error);       }       try {         const grid = $('gamesGrid');         if (!grid) return null;         const existingStatic = grid.dataset.staticPainted === '1' && grid.children.length > 0;         if (existingStatic) return null;         const fragment = document.createDocumentFragment();         const total = Math.max(1, Math.min(6, Number(count) || 3));         for (let i = 0; i < total; i += 1) {           const card = document.createElement('article');           card.className = 'game-card skeleton is-skeleton';           const top = document.createElement('div'); top.className = 'game-top';           const icon = document.createElement('div'); icon.className = 'game-icon skeleton';           const tags = document.createElement('div'); tags.className = 'tag-stack';           tags.appendChild(createTextElement('span', 'mini-tag skeleton', ''));           tags.appendChild(createTextElement('span', 'mini-tag skeleton', ''));           top.append(icon, tags);           const body = document.createElement('div'); body.className = 'game-body';           body.appendChild(createTextElement('h3', 'game-title skeleton', ''));           body.appendChild(createTextElement('div', 'game-desc skeleton', ''));           const footer = document.createElement('div'); footer.className = 'game-footer';           footer.appendChild(createTextElement('span', 'game-status skeleton', ''));           footer.appendChild(createTextElement('span', 'game-cta skeleton', ''));           card.append(top, body, footer);           fragment.appendChild(card);         }         grid.classList.add('is-loading');         grid.replaceChildren(fragment);         const empty = $('gamesEmpty');         if (empty) empty.hidden = true;       } catch (error) {         console.warn('[PlayMatrix] skeleton hard fallback failed', error);       }       return null;     }      window.renderGameShowcaseSkeleton = window.renderGameShowcaseSkeleton || safeRenderGameShowcaseSkeleton;
     const AUTH_UNAVAILABLE_MESSAGE = "Kimlik altyapısı geçici olarak hazır değil. Lütfen biraz sonra tekrar dene.";     const FIREBASE_RUNTIME_TIMEOUT_MS = 3500;
    const FIREBASE_IMPORT_TIMEOUT_MS = 6000;      let firebaseConfig = null;     let app = null;     let auth = { currentUser: null };
    let firebaseReady = false;     let firebaseBootError = null;     let firebaseBootPromise = null;      function timeoutAfter(ms, label) {
      return new Promise((_, reject) => {         window.setTimeout(() => reject(new Error(label)), Math.max(1000, Number(ms) || 1000));       });     } 
    function withTimeout(promise, ms, label) {       return Promise.race([promise, timeoutAfter(ms, label)]);     }      function syncRuntimeAuth() {
      window.__PM_RUNTIME = window.__PM_RUNTIME || {};       window.__PM_RUNTIME.auth = auth;     }      function createAuthUnavailableError(actionLabel = 'Bu işlem') {
      const detail = firebaseBootError?.message ? ` (${firebaseBootError.message})` : '';       return new Error(`${actionLabel} şu anda kullanılamıyor. ${AUTH_UNAVAILABLE_MESSAGE}${detail}`);     }
    function normalizeFirebaseAuthError(error) {
      const code = String(error?.code || '').trim();
      const message = String(error?.message || '').trim();
      const map = {
        'auth/configuration-not-found': 'Firebase Authentication yapılandırması playmatrixpro-b18b7 projesinde hazır değil. Firebase Console > Authentication bölümünde Email/Password sağlayıcısını ve Authorized domains alanında playmatrix.com.tr ile www.playmatrix.com.tr domainlerini etkinleştir.',
        'auth/network-request-failed': 'Firebase bağlantısı kurulamadı. İnternet bağlantını kontrol edip tekrar dene.',
        'auth/email-already-in-use': 'E-posta zaten kullanımda.',
        'auth/wrong-password': 'Hatalı e-posta veya şifre.',
        'auth/invalid-credential': 'Hatalı e-posta veya şifre.',
        'auth/invalid-email': 'Geçerli bir e-posta adresi gir.',
        'auth/weak-password': 'Şifre daha güçlü olmalı.',
        'auth/too-many-requests': 'Çok fazla deneme yapıldı. Bir süre sonra tekrar dene.'
      };
      return map[code] || message || 'Firebase kimlik işlemi tamamlanamadı.';
    }      async function bootFirebase() {
      if (firebaseReady) return true;       if (firebaseBootPromise) return firebaseBootPromise;        firebaseBootPromise = (async () => {         try {
          firebaseConfig = await loadFirebaseWebConfig({ required: false, scope: 'app', timeoutMs: FIREBASE_RUNTIME_TIMEOUT_MS });           if (!firebaseConfig) {             firebaseBootError = new Error('PUBLIC_FIREBASE_CONFIG_MISSING');             return false;           }
           const [firebaseAppModule, firebaseAuthModule] = await withTimeout(Promise.all([             import("https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js"),             import("https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js")           ]), FIREBASE_IMPORT_TIMEOUT_MS, 'FIREBASE_IMPORT_TIMEOUT');
           initializeApp = firebaseAppModule.initializeApp;           firebaseGetAuth = firebaseAuthModule.getAuth;           firebaseOnAuthStateChanged = firebaseAuthModule.onAuthStateChanged;           firebaseSignOut = firebaseAuthModule.signOut;
          firebaseSignInWithEmailAndPassword = firebaseAuthModule.signInWithEmailAndPassword;           firebaseCreateUserWithEmailAndPassword = firebaseAuthModule.createUserWithEmailAndPassword;           firebaseSendEmailVerification = firebaseAuthModule.sendEmailVerification;           firebaseSendPasswordResetEmail = firebaseAuthModule.sendPasswordResetEmail;           firebaseVerifyBeforeUpdateEmail = firebaseAuthModule.verifyBeforeUpdateEmail;
          firebaseGetIdToken = firebaseAuthModule.getIdToken;           firebaseReload = firebaseAuthModule.reload;           app = initializeApp(firebaseConfig);           auth = firebaseGetAuth(app);           firebaseReady = true;
          firebaseBootError = null;           syncRuntimeAuth();           return true;         } catch (error) {           firebaseReady = false;
          firebaseBootError = error instanceof Error ? error : new Error(String(error || 'FIREBASE_BOOT_FAILED'));           syncRuntimeAuth();           return false;         } finally {           firebaseBootPromise = null;
        }       })();        return firebaseBootPromise;     }
     function ensureFirebaseReady(actionLabel = 'Bu işlem') {       if (!firebaseReady) throw createAuthUnavailableError(actionLabel);       return true;     }
     async function waitForFirebaseReady(actionLabel = 'Bu işlem') {       if (!firebaseReady) await bootFirebase();       return ensureFirebaseReady(actionLabel);     }
     function onAuthStateChanged(_authInstance, callback, errorCallback, completedCallback) {       let nativeUnsubscribe = null;       let active = true;       let nativeAttached = false;
       const emitGuestState = () => {         if (!active || nativeAttached) return;         try {           callback(null);
        } catch (error) {           if (typeof errorCallback === 'function') errorCallback(error);           else console.error('[PlayMatrix] auth shim callback', error);         }         if (typeof completedCallback === 'function') completedCallback();
      };       bootFirebase().then((ready) => {         if (!active) return;         if (!ready || !firebaseReady || typeof firebaseOnAuthStateChanged !== 'function') {           emitGuestState();           return;         }
        nativeAttached = true;         nativeUnsubscribe = firebaseOnAuthStateChanged(auth, callback, errorCallback, completedCallback);       }).catch((error) => {         firebaseBootError = error instanceof Error ? error : new Error(String(error || 'FIREBASE_BOOT_FAILED'));         emitGuestState();       });
       return () => {         active = false;         if (typeof nativeUnsubscribe === 'function') nativeUnsubscribe();       };
    }      async function signOut(authInstance) {       await bootFirebase();       if (!firebaseReady) return;
      return firebaseSignOut(authInstance || auth);     }      async function signInWithEmailAndPassword(authInstance, email, password) {       await waitForFirebaseReady('Giriş');
      return firebaseSignInWithEmailAndPassword(authInstance || auth, email, password);     }      async function createUserWithEmailAndPassword(authInstance, email, password) {       await waitForFirebaseReady('Kayıt');
      return firebaseCreateUserWithEmailAndPassword(authInstance || auth, email, password);     }      async function sendEmailVerification(user) {       await waitForFirebaseReady('E-posta doğrulama');
      return firebaseSendEmailVerification(user);     }      async function sendPasswordResetEmail(authInstance, email) {       await waitForFirebaseReady('Şifre sıfırlama');
      return firebaseSendPasswordResetEmail(authInstance || auth, email);     }      async function verifyBeforeUpdateEmail(user, email) {       await waitForFirebaseReady('E-posta güncelleme');
      return firebaseVerifyBeforeUpdateEmail(user, email);     }      async function getIdToken(user, forceRefresh = false) {       await waitForFirebaseReady('Oturum doğrulama');
      if (!user) throw new Error('NO_USER');       return firebaseGetIdToken(user, forceRefresh);     }      async function reload(user) {
      await waitForFirebaseReady('Hesap yenileme');       return firebaseReload(user);     }      bootFirebase().catch((error) => {
      firebaseBootError = error instanceof Error ? error : new Error(String(error || 'FIREBASE_BOOT_FAILED'));     });      window.__PM_RUNTIME = window.__PM_RUNTIME || {};     window.__PM_RUNTIME.auth = auth;
    window.__PM_RUNTIME.signOut = signOut;     window.__PM_RUNTIME.firebaseReady = () => firebaseReady;     window.__PM_RUNTIME.firebaseBootError = () => String(firebaseBootError?.message || '');     window.__PM_RUNTIME.getIdToken = async (forceRefresh = false) => {       if (!auth.currentUser) throw new Error('NO_USER');
      return getIdToken(auth.currentUser, forceRefresh);     };     window.__PM_RUNTIME.ensureServerSession = async (options = {}) => bootstrapServerSession(auth.currentUser, options);      const PLAYMATRIX_API = window.__PM_API__ || null;
    const API_CANDIDATES = typeof PLAYMATRIX_API?.getCandidates === 'function'       ? PLAYMATRIX_API.getCandidates()       : [String(window.__PM_RUNTIME?.apiBase || '').trim(), String(window.__PLAYMATRIX_API_URL__ || '').trim(), String(document.querySelector('meta[name="playmatrix-api-url"]')?.content || '').trim()].filter(Boolean);      let API_URL = (typeof PLAYMATRIX_API?.getApiBaseSync === 'function'
      ? PLAYMATRIX_API.getApiBaseSync()       : API_CANDIDATES[0] || '');      function setResolvedApiBase(base) {       const normalized = String(base || "").trim().replace(/\/+$/, "").replace(/\/api$/i, "");
      if (!normalized) return API_URL;       API_URL = normalized;       window.__PM_RUNTIME = window.__PM_RUNTIME || {};       window.__PM_RUNTIME.apiBase = normalized;       window.__PLAYMATRIX_API_URL__ = normalized;
      return API_URL;     }      async function probeApiBase(base) {       if (typeof PLAYMATRIX_API?.probeBase === 'function') {
        return PLAYMATRIX_API.probeBase(base);       }       const normalized = String(base || "").trim().replace(/\/+$/, "").replace(/\/api$/i, "");       if (!normalized) return false;       try {
        const response = await fetchWithTimeout(`${normalized}/api/healthz`, { method: "GET", headers: { "Accept": "application/json" }, credentials: "omit", cache: "no-store" }, 1800);         return response.ok;       } catch (_) {         return false;       }
    }      function getFallbackApiBases(currentBase = "") {       const current = String(currentBase || "").trim().replace(/\/+$/, "").replace(/\/api$/i, "");       return API_CANDIDATES
        .map((value) => String(value || "").trim().replace(/\/+$/, "").replace(/\/api$/i, ""))         .filter((value, index, arr) => value && arr.indexOf(value) === index && value !== current);     }      async function resolveApiBase() {
      if (typeof PLAYMATRIX_API?.ensureApiBase === 'function') {         return setResolvedApiBase(await PLAYMATRIX_API.ensureApiBase());       }       for (const base of API_CANDIDATES) {         if (await probeApiBase(base)) return setResolvedApiBase(base);
      }       return setResolvedApiBase(API_CANDIDATES[0] || "");     }      const apiBaseReady = resolveApiBase();
     function fetchWithTimeout(resource, options = {}, timeoutMs = 5000) {       if (typeof PLAYMATRIX_API?.fetchWithTimeout === 'function') {         return PLAYMATRIX_API.fetchWithTimeout(resource, options, timeoutMs);       }
      const controller = new AbortController();       const timer = window.setTimeout(() => controller.abort(), Math.max(1000, Number(timeoutMs) || 5000));       return fetch(resource, { ...options, signal: controller.signal }).finally(() => window.clearTimeout(timer));     } 
    async function requestWithApiFallback(endpoint, fetchOptions = {}, retryableStatuses = [404, 405, 502, 503, 504]) {       await apiBaseReady;       const orderedBases = [API_URL, ...getFallbackApiBases(API_URL)];       let lastNetworkError = null; 
      for (let index = 0; index < orderedBases.length; index += 1) {         const base = orderedBases[index];         try {           const response = await fetchWithTimeout(`${base}${endpoint}`, { ...fetchOptions, cache: "no-store" }, fetchOptions.timeoutMs || 5000);           const canRetry = retryableStatuses.includes(response.status) && index < orderedBases.length - 1;
          if (response.ok || !canRetry) {             if (base !== API_URL) setResolvedApiBase(base);             return response;           }         } catch (error) {
          lastNetworkError = error;           if (index >= orderedBases.length - 1) throw error;         }       } 
      if (lastNetworkError) throw lastNetworkError;       throw new Error("Sunucuyla bağlantı kurulamadı.");     }      const REF_STORAGE_KEY = "pm_pending_referral_code";
     const REWARDS = [
      { label:"10.000 MC", val:10000, color:"rgba(255,255,255,0.14)" },       { label:"20.000 MC", val:20000, color:"rgba(255,255,255,0.09)" },       { label:"25.000 MC", val:25000, color:"rgba(255,255,255,0.14)" },
      { label:"45.000 MC", val:45000, color:"rgba(255,255,255,0.09)" },       { label:"65.000 MC", val:65000, color:"rgba(255,255,255,0.14)" },       { label:"90.000 MC", val:90000, color:"rgba(255,255,255,0.09)" },
      { label:"120.000 MC", val:120000, color:"rgba(255,255,255,0.14)" },       { label:"1.000.000 MC", val:1000000, color:"rgba(255,255,255,0.09)" }
    ];        const GAMES = [
      {         name: "Crash",         category: "online",         access: "auth",         url: "/games/crash",
        color: "69,162,255",         icon: "fa-arrow-trend-up",         desc: "Gerçek para içermeyen, refleks ve zamanlama odaklı hızlı tempo multiplier oyunu.",         tags: ["Canlı Oyun", "Rekabet", "Hızlı Tur"],         keywords: "crash multiplier online rocket roket çarpan"
      },       {         name: "Satranç",         category: "online",         access: "auth",
        url: "/games/chess",         color: "104,178,255",         icon: "fa-chess",         desc: "Klasik satranç deneyimini modern arayüz ve giriş tabanlı rekabet akışıyla oyna.",         tags: ["PvP", "Strateji", "Arena"],
        keywords: "chess online pvp satranç"       },       {         name: "Pişti",         category: "online",
        access: "auth",         url: "/games/pisti",         color: "93,95,254",         icon: "fa-layer-group",         desc: "Kart takibi ve tempo yönetimi isteyen online pişti deneyimi.",
        tags: ["Kart", "Online", "Klasik"],         keywords: "card kart multiplayer online pisti pişti"       },       {         name: "Pattern Master",
        category: "classic",         access: "auth",         url: "/games/pattern-master",         color: "97,220,176",         icon: "fa-shapes",
        desc: "Dikkat ve görsel hafıza odaklı ücretsiz pattern oyunu.",         tags: ["Ücretsiz", "Zeka", "Refleks"],         keywords: "arcade pattern master ücretsiz zeka"       },       {
        name: "Space Pro",         category: "classic",         access: "auth",         url: "/games/space-pro",         color: "103,170,255",
        icon: "fa-user-astronaut",         desc: "Tarayıcıda anında açılan hafif ve hızlı klasik arcade uzay oyunu.",         tags: ["Arcade", "Retro", "Ücretsiz"],         keywords: "arcade pro space uzay"       },
      {         name: "Snake Pro",         category: "classic",         access: "auth",         url: "/games/snake-pro",
        color: "85,214,140",         icon: "fa-wave-square",         desc: "Retro hisli, akıcı ve ücretsiz snake deneyimi.",         tags: ["Retro", "Arcade", "Ücretsiz"],         keywords: "arcade pro retro snake yılan"
      },     ];       const createSocialState = () => ({
      activeTab: "global",       selectedKey: "global:lobby",       directMessages: {},       unreadDirect: {},       mobilePanelOpen: false,
      currentActiveDmUid: null,       directHistoryLoadedAt: {},       directHistoryPending: {},       typingTimerId: 0,       pendingInviteNavigation: null,
      pendingMatchmaking: null,       matchmakingToastEl: null,       centerSummary: null,       centerLoading: false,       centerError: "",
      centerLoadedAt: 0,       friendsLoading: false,       friendsError: "",       friendsLoadedAt: 0,       realtimeMode: "full",
      realtimeError: "",       chatPolicy: null,       notifications: [],       notificationsUnread: 0,       notificationsSummary: null,
      notificationsLoading: false,       notificationsError: "",       dmSettings: {},       dmSearchQuery: "",       dmSearchResults: [],
      dmSearchLoading: false,       dmSearchError: "",       dmSearchTargetUid: "",       dmSearchNextCursor: "",       dmSearchHasMore: false,       directHistoryErrors: {},       directHistoryRequestedAt: {},
      friendTargetDraft: "",       inviteDraft: {         gameKey: "chess",         mode: "2-52",         bet: "10000"       },
      composerDrafts: {         global: "",         direct: {}       },       transientFocus: {
        id: "",         selectionStart: null,         selectionEnd: null       },       renderCache: {
        tabs: "",         list: "",         main: ""       }     });
     const state = {       currentSheet: null,       authMode: "login",       userData: null,
      selectedAvatar: AVATARS[0],       selectedFrame: 0,       soundEnabled: localStorage.getItem("pm_ui_sound") !== "off",       wheelRotation: 0,       drag: { active:false, startY:0, deltaY:0 },
      activeFilter: "all",       usernameDebounce: null,       socket: null,       socketScriptPromise: null,       realtimeConnected: false,
      lobbyMessages: [],       friends: { accepted: [], incoming: [], outgoing: [], counts: { accepted:0, incoming:0, outgoing:0, online:0 } },       inviteToasts: new Map(),       social: createSocialState(),       monthlyRewardShownKey: "",
      authReady: false,       authStatus: "pending",       authBootInFlight: false,       authBootAttemptedAt: 0,       profileSyncInFlight: false,       profileSyncRetryCount: 0,       realtimeReconnectAttempts: 0,       realtimeReconnectTimer: 0,
      realtimeShouldBeConnected: false,       logoutInFlight: false,       lastSessionIssueKey: "",       lastLevelUpEventId: ""     }; 
    window.__PM_RUNTIME.authReady = () => !!state.authReady;     state.sessionBootstrapPromise = null;     state.sessionBootstrapAt = 0;     state.sessionBootstrapBackoffUntil = 0;      const heroCarouselState = {
      index: 0,       timer: 0,       autoplayMs: 5000,       slideSpan: 0,       touchActive: false,
      startX: 0,       dragOffset: 0,       startOffset: 0,       bound: false,       slides: []
    };      let currentLeaderboardData = null;     let currentLeaderboardTab = "level"; 
    const LOBBY_DOM_MESSAGE_LIMIT = 120;     const PENDING_REWARD_SESSION_KEY = "pm_pending_reward_seen";     const PM_SESSION_TOKEN_KEY = "pm_session_token";     const HEARTBEAT_INTERVAL_MS = 5 * 60 * 1000;     let activityHeartbeatTimer = 0;     let activityHeartbeatListenersBound = false;
    function readServerSessionToken(){ try { return sessionStorage.getItem(PM_SESSION_TOKEN_KEY) || localStorage.getItem(PM_SESSION_TOKEN_KEY) || ""; } catch(_) { return ""; } }
    function storeServerSessionToken(payload){ const token = String(payload?.sessionToken || payload?.session?.token || "").trim(); if (!token) return; try { sessionStorage.setItem(PM_SESSION_TOKEN_KEY, token); } catch(_) {} try { localStorage.setItem(PM_SESSION_TOKEN_KEY, token); } catch(_) {} }
    function clearServerSessionToken(){ try { sessionStorage.removeItem(PM_SESSION_TOKEN_KEY); } catch(_) {} try { localStorage.removeItem(PM_SESSION_TOKEN_KEY); } catch(_) {} }
     function escapeHtml(value){       return String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "\'":"&#39;" }[char] || char));     } 
    function stripHtml(value){       return String(value ?? '').replace(/<[^>]*>/g, ' ').replace(/\s+/g, ' ').trim();     }      function sanitizeIconClass(value, fallback = 'fa-circle-info') {
      const raw = String(value || '').trim();       const match = raw.split(/\s+/).find((token) => /^fa-[a-z0-9-]+$/i.test(token));       return match || fallback;     } 
    function createFaIcon(iconClass, style = {}) {       const icon = document.createElement('i');       icon.className = 'fa-solid ' + sanitizeIconClass(iconClass);       Object.entries(style || {}).forEach(([key, value]) => { icon.style[key] = value; });       return icon;
    }      function setIconHost(host, iconClass) {       if (!host) return;       host.replaceChildren(createFaIcon(iconClass));
    }      function createTextElement(tagName, className, text) {       const node = document.createElement(tagName);       if (className) node.className = className;
      node.textContent = String(text ?? '');       return node;     }      function appendSafeInlineMarkup(host, html) {
      if (!host) return;       host.replaceChildren();       const tokens = String(html || '').split(/(<br\s*\/?\s*>|<\/?b>)/i).filter((token) => token !== '');       let strong = null;       tokens.forEach((token) => {
        if (/^<br\s*\/?\s*>$/i.test(token)) {           host.appendChild(document.createElement('br'));           return;         }         if (/^<b>$/i.test(token)) {
          strong = document.createElement('strong');           host.appendChild(strong);           return;         }         if (/^<\/b>$/i.test(token)) {
          strong = null;           return;         }         (strong || host).appendChild(document.createTextNode(stripHtml(token)));       });
    }      function setTextIfPresent(id, value) {       const element = $(id);       if (element) element.textContent = String(value ?? '');
      return element;     }      function safeUrl(value){       const fallback = DEFAULT_AVATAR;
      if (!value || typeof value !== "string") return fallback;       const raw = value.trim();       if (!raw) return fallback;        const catalogAvatar = normalizeAvatarUrl(raw, '');
      if (catalogAvatar) return catalogAvatar;        if (/^(\/assets\/|assets\/|\.\/assets\/)/i.test(raw)) {         return raw.startsWith('/') ? raw : `/${raw.replace(/^\.?\//, '')}`;       }
       try {         const url = new URL(raw, window.location.origin);         const isSameOrigin = url.origin === window.location.origin;         const isAllowedExternal = url.hostname.endsWith("gstatic.com") || url.hostname.endsWith("googleusercontent.com") || url.hostname === "firebasestorage.googleapis.com" || url.hostname.endsWith("firebasestorage.app") || url.hostname === "playmatrix.com.tr" || url.hostname === "www.playmatrix.com.tr";
        if (isSameOrigin || isAllowedExternal) return url.href;       } catch(_) {}       return fallback;     } 
    function bindAvatarImageFallback(img){       if (!img || img.dataset.pmFallbackBound === 'true') return;       img.dataset.pmFallbackBound = 'true';       img.addEventListener('error', () => {         if (img.dataset.pmFallbackApplied === 'true') return;
        img.dataset.pmFallbackApplied = 'true';         img.src = AVATAR_FALLBACK;       });     } 
     async function resolveLoginIdentifier(identifier){       const raw = String(identifier || "").trim();       if (!raw) throw new Error("E-posta veya kullanıcı adı zorunlu.");       if (raw.includes("@")) return raw;
      const response = await fetchPublic("/api/auth/resolve-login", "POST", { identifier: raw });       if (!response?.email) throw new Error("Hatalı kullanıcı adı veya şifre.");       return String(response.email).trim();     } 
    async function bootstrapServerSession(user = auth.currentUser, options = {}){       const targetUser = user || auth.currentUser;       if (!targetUser) return null;       const force = !!options.force;       const now = Date.now();       if (!force && state.sessionBootstrapBackoffUntil && now < state.sessionBootstrapBackoffUntil) return null;       if (!force && state.sessionBootstrapPromise) return state.sessionBootstrapPromise;
      const execute = async () => {         try {           const token = await getIdToken(targetUser, !!options.forceRefresh);           const headers = { "Authorization": `Bearer ${token}`, "Content-Type": "application/json" };           await apiBaseReady;
          const response = await requestWithApiFallback("/api/auth/session/create", {             method: "POST",             headers,             credentials: "include",             cache: "no-store",
            body: JSON.stringify({})           }, [404, 405, 502, 503, 504]);           const payload = await response.json().catch(() => ({}));           if (!response.ok || payload?.ok === false) {             const err = new Error(payload?.error || `Sunucu hatası. (${response.status})`);             err.status = response.status;             throw err;
          }           storeServerSessionToken(payload);           state.sessionBootstrapAt = Date.now();           state.sessionBootstrapBackoffUntil = 0;           state.lastSessionIssueKey = "";           return payload;         } catch (error) {           const status = Number(error?.status || 0);           if (status === 503 || String(error?.message || '').includes('(503)')) {             state.sessionBootstrapBackoffUntil = Date.now() + 30000;           }           throw error;         } finally {
          if (state.sessionBootstrapPromise === promiseRef) {             state.sessionBootstrapPromise = null;           }         }       };
      const promiseRef = execute();       state.sessionBootstrapPromise = promiseRef;       return promiseRef;     } 
    async function endServerSession(){       try {         const sessionToken = readServerSessionToken();         const headers = sessionToken ? { 'x-session-token': sessionToken } : {};         await requestWithApiFallback('/api/auth/session/logout', { method: 'POST', headers, cache: 'no-store', credentials: 'include' }, [404, 405, 502, 503, 504]);       } catch (_) {} finally { clearServerSessionToken(); }     }
     function notifySessionIssueOnce(title, message, tone = "warning") {       const key = `${title}::${message}`;       if (state.lastSessionIssueKey === key) return;       state.lastSessionIssueKey = key;
      showToast(title, message, tone);       window.setTimeout(() => {         if (state.lastSessionIssueKey === key) state.lastSessionIssueKey = "";       }, 5000);     }
     function clearRealtimeReconnectTimer() {       if (state.realtimeReconnectTimer) {         window.clearTimeout(state.realtimeReconnectTimer);         state.realtimeReconnectTimer = 0;
      }     }      function scheduleRealtimeReconnect(reason = "reconnect") {       if (!state.realtimeShouldBeConnected || !auth.currentUser || !state.authReady) return;
      if (state.realtimeReconnectTimer) return;       state.realtimeReconnectAttempts = Math.min(8, Number(state.realtimeReconnectAttempts || 0) + 1);       const retryDelay = Math.min(1000 * (2 ** Math.max(0, state.realtimeReconnectAttempts - 1)), 15000);       updateSocialConnectionBadge("Bağlantı yenileniyor");       state.realtimeReconnectTimer = window.setTimeout(() => {
        state.realtimeReconnectTimer = 0;         ensureRealtimeConnection(true, { reason }).catch(() => null);       }, retryDelay);     } 
    async function forceSecureLogout(reasonMessage = "Oturum süren doldu.", options = {}) {       if (state.logoutInFlight) return false;       state.logoutInFlight = true;       try { await endServerSession(); } catch (_) {}       try { if (auth.currentUser) await signOut(auth); } catch (_) {}
      stopActivityHeartbeat();       disconnectRealtime({ preserveIntent: false });       state.authReady = false;       if (!options.silent) notifySessionIssueOnce("Oturum kapatıldı", reasonMessage, "warning");       closeSheet();
      setTimeout(() => { window.location.hash = "#hero"; }, 50);       state.logoutInFlight = false;       return false;     } 
    async function sendActivityHeartbeat(reason = "active"){       if (!auth.currentUser || !state.authReady) return false;       try {         await fetchPrivate("/api/me/activity/heartbeat", "POST", {           activity: String(reason || "heartbeat").trim() || "heartbeat",
          interactive: ["input", "focus", "visible", "pageshow", "login", "boot", "click", "pointerdown", "mousemove", "keydown", "touchstart"].some((token) => String(reason || "").toLowerCase().includes(token))         });         return true;       } catch (error) {         const message = String(error?.message || "");
        if (message.includes("Oturum") || message.includes("zaman aşım") || message.includes("Geçersiz token") || message.includes("Geçersiz oturum")) {           await forceSecureLogout("Güvenlik nedeniyle oturumun kapatıldı. Tekrar giriş yapman gerekiyor.");           return false;         }         return false;
      }     }      function stopActivityHeartbeat(){       if (activityHeartbeatTimer) {
        window.clearInterval(activityHeartbeatTimer);         activityHeartbeatTimer = 0;       }     } 
    function startActivityHeartbeat(){       stopActivityHeartbeat();       if (!auth.currentUser || !state.authReady) return;       if (window.__PM_RUNTIME_SHARED_HEARTBEAT__) return;       sendActivityHeartbeat("boot").catch(() => null);
      activityHeartbeatTimer = window.setInterval(() => {         sendActivityHeartbeat(document.visibilityState === "visible" ? "visible" : "idle").catch(() => null);       }, HEARTBEAT_INTERVAL_MS);        if (!activityHeartbeatListenersBound) {
        activityHeartbeatListenersBound = true;         ["visibilitychange", "focus", "pageshow"].forEach((eventName) => {           window.addEventListener(eventName, () => {             if (!auth.currentUser) return;             sendActivityHeartbeat(eventName).catch(() => null);
          }, { passive: true });         });          ["pointerdown", "keydown", "touchstart"].forEach((eventName) => {           window.addEventListener(eventName, () => {
            if (!auth.currentUser) return;             sendActivityHeartbeat(eventName).catch(() => null);           }, { passive: true });         });       }
    }      function getFrameLevelLabel(level = 0){       const normalized = normalizeFrameLevel(level);       return normalized <= 0 ? 'Çerçevesiz' : `Seviye ${normalized} Çerçevesi`;
    }      function normalizeFrameLevel(level = 0){       return Math.max(0, Math.min(100, Math.round(Number(level) || 0)));     }
     function getProgressionData(userData = {}) {       return userData?.progression && typeof userData.progression === 'object' ? userData.progression : {};     } 
    function isCurrentAccountProgression(userData = {}) {       const progression = getProgressionData(userData);       const version = String(userData?.accountProgressionVersion ?? progression.accountProgressionVersion ?? progression.version ?? '').trim();       const curveMode = String(userData?.accountLevelCurveMode ?? progression.accountLevelCurveMode ?? progression.curveMode ?? '').trim();       return (version === ACCOUNT_PROGRESSION_VERSION || version === 'playmatrix-progression-v1') && (!curveMode || curveMode === ACCOUNT_LEVEL_CURVE_MODE);
    }      function getUserAccountXpExact(userData = {}) {       const progression = getProgressionData(userData);       const candidates = [progression.xp, progression.accountXpExact, userData?.accountXpExact, userData?.accountLevelScoreExact, progression.accountLevelScoreExact, userData?.accountXp, userData?.accountLevelScore, progression.accountXp, userData?.xp];       const raw = candidates.find((value) => value !== undefined && value !== null && String(value).trim() !== "");       return normalizeAccountXpExact(raw || 0);     }      function getUserAccountXp(userData = {}) {       const serverProgression = getProgressionData(userData);       const serverXp = serverProgression.xp ?? serverProgression.accountXp ?? userData?.accountXp ?? userData?.xp;       return getAccountLevelProgressFromXp(serverXp ?? getUserAccountXpExact(userData)).accountXp;     }      function getUserLevel(userData) {       const progression = getProgressionData(userData);       const explicit = Number(progression.level ?? progression.accountLevel ?? userData?.accountLevel ?? userData?.level);       if (Number.isFinite(explicit) && explicit > 0) return normalizeFrameLevel(explicit);       return normalizeFrameLevel(getAccountLevelFromXp(getUserAccountXpExact(userData)));     } 
    function normalizeApiUser(userData = {}) {       const base = userData && typeof userData === 'object' ? { ...userData } : {};       const serverProgression = getProgressionData(base);       const serverIsCurrent = isCurrentAccountProgression(base);       const accountXpExact = getUserAccountXpExact(base);       const clientProgression = getAccountLevelProgressFromXp(accountXpExact);       const accountXp = clientProgression.accountXp;       const accountLevel = getUserLevel(base);       const progressPercent = clamp(Number(serverProgression.progressPercent ?? serverProgression.accountLevelProgressPct ?? clientProgression.accountLevelProgressPct ?? 0), 0, 100);       const monthlyActivity = Math.max(0, Number(base.monthlyActiveScore ?? serverProgression.monthlyActivity ?? 0) || 0);       const rawSelectedFrame = Number(base.selectedFrame ?? serverProgression.selectedFrame ?? 0);
      const normalizedSelectedFrame = Number.isFinite(rawSelectedFrame) ? normalizeFrameLevel(rawSelectedFrame) : 0;       const selectedFrame = normalizedSelectedFrame > accountLevel ? 0 : normalizedSelectedFrame;       const progression = {         ...clientProgression,         ...serverProgression,         accountXp,         accountXpExact: serverProgression.xp ? normalizeAccountXpExact(serverProgression.xp) : clientProgression.accountXpExact,         xp: serverProgression.xp ?? clientProgression.accountXpExact,         accountLevel,         level: accountLevel,         accountLevelScore: accountXp,         accountLevelProgressPct: progressPercent,         progressPercent,         monthlyActivity,
        accountProgressionVersion: ACCOUNT_PROGRESSION_VERSION,         accountLevelCurveMode: ACCOUNT_LEVEL_CURVE_MODE,         version: ACCOUNT_PROGRESSION_VERSION,         curveMode: ACCOUNT_LEVEL_CURVE_MODE,         source: serverIsCurrent ? 'backend' : 'frontend-safe-fallback'       };       return {         ...base,
        accountXp,         accountXpExact: progression.accountXpExact,         xp: progression.xp ?? progression.accountXpExact,         accountLevel,         level: accountLevel,         accountLevelScore: accountXp,         progressPercent,         accountLevelProgressPct: progressPercent,         accountXpLabel: progression.accountXpLabel || progression.formattedXp || clientProgression.accountXpLabel,         accountXpFullLabel: progression.accountXpFullLabel || progression.formattedXp || clientProgression.accountXpFullLabel,
        accountProgressionVersion: ACCOUNT_PROGRESSION_VERSION,         accountLevelCurveMode: ACCOUNT_LEVEL_CURVE_MODE,         monthlyActiveScore: monthlyActivity,         selectedFrame,         progression
      };     }      function normalizeMemberPayload(member = {}) {       const normalized = normalizeApiUser(member);
      const presence = normalizePresenceState(normalized.presence, !!normalized.online);       const leaderboard = normalized.leaderboard && typeof normalized.leaderboard === 'object' ? {         rank: Math.max(0, Number(normalized.leaderboard.rank) || 0),         metricKey: String(normalized.leaderboard.metricKey || '').trim(),         metricLabel: String(normalized.leaderboard.metricLabel || '').trim(),
        metricValue: Number(normalized.leaderboard.metricValue || 0) || 0       } : null;       return {         ...normalized,         username: String(normalized.username || normalized.displayName || 'Oyuncu').trim() || 'Oyuncu',
        avatar: typeof normalized.avatar === 'string' ? normalized.avatar : '',         selectedFrame: normalizeFrameLevel(Number(normalized.selectedFrame || 0)),         presence,         online: presence.online,         unreadMessages: Math.max(0, Number(normalized.unreadMessages || 0) || 0),
        lastSeen: Math.max(0, Number(normalized.lastSeen || 0) || 0),         totalRank: String(normalized.totalRank || '').trim(),         totalRankClass: String(normalized.totalRankClass || '').trim(),         activityRank: String(normalized.activityRank || '').trim(),         activityRankClass: String(normalized.activityRankClass || '').trim(),
        leaderboard       };     }      function normalizeLeaderboardPayload(payload = {}) {
      const tabs = payload?.tabs && typeof payload.tabs === 'object' ? payload.tabs : {};       const normalizedTabs = {         level: {           key: 'level',           label: String(tabs?.level?.label || 'En Yüksek Hesap Seviyesi').trim(),
          metricKey: String(tabs?.level?.metricKey || 'accountLevelScore').trim(),           items: Array.isArray(tabs?.level?.items) ? tabs.level.items.map(normalizeMemberPayload) : [],           self: tabs?.level?.self ? normalizeMemberPayload(tabs.level.self) : null         },         activity: {
          key: 'activity',           label: String(tabs?.activity?.label || 'En Çok Aktif Oyuncular').trim(),           metricKey: String(tabs?.activity?.metricKey || 'monthlyActiveScore').trim(),           items: Array.isArray(tabs?.activity?.items) ? tabs.activity.items.map(normalizeMemberPayload) : [],           self: tabs?.activity?.self ? normalizeMemberPayload(tabs.activity.self) : null
        }       };       return {         ok: payload?.ok !== false,         generatedAt: Number(payload?.generatedAt || Date.now()) || Date.now(),
        tabs: normalizedTabs       };     }      function normalizeSocialCenterPayload(payload = {}) {
      const source = payload?.summary && typeof payload.summary === "object" ? payload.summary : payload;       return {         ok: payload?.ok !== false,         schemaVersion: Math.max(1, Number(payload?.schemaVersion || 1) || 1),         generatedAt: Math.max(0, Number(payload?.generatedAt || Date.now()) || Date.now()),
        me: source?.me ? { ...source.me, ...normalizeMemberPayload(source.me) } : null,         friends: Array.isArray(source?.friends) ? source.friends.map(normalizeMemberPayload) : [],         recentPlayers: Array.isArray(source?.recentPlayers) ? source.recentPlayers.map(normalizeMemberPayload) : [],         recentMatches: Array.isArray(source?.recentMatches) ? source.recentMatches : [],         matchCenter: source?.matchCenter && typeof source.matchCenter === "object" ? source.matchCenter : { summary: null, items: [], nextCursor: "" },
        activeSessions: Array.isArray(source?.activeSessions) ? source.activeSessions : [],         chatPolicy: source?.chatPolicy && typeof source.chatPolicy === "object" ? source.chatPolicy : null,         rewardCenter: source?.rewardCenter && typeof source.rewardCenter === "object" ? source.rewardCenter : null,         socialStats: Array.isArray(source?.socialStats) ? source.socialStats : [],         activityFeed: Array.isArray(source?.activityFeed) ? source.activityFeed : []
      };     }      function normalizeFriendsPayload(payload = {}) {       const source = payload?.lists && typeof payload.lists === "object"
        ? payload.lists         : payload?.members && typeof payload.members === "object"           ? payload.members           : payload;       const accepted = Array.isArray(source?.accepted) ? source.accepted.map(hydrateFriendEntry) : [];
      const incoming = Array.isArray(source?.incoming) ? source.incoming.map(hydrateFriendEntry) : [];       const outgoing = Array.isArray(source?.outgoing) ? source.outgoing.map(hydrateFriendEntry) : [];       const counts = payload?.counts && typeof payload.counts === "object" ? payload.counts : {};       return {         ok: payload?.ok !== false,
        schemaVersion: Math.max(1, Number(payload?.schemaVersion || 1) || 1),         generatedAt: Math.max(0, Number(payload?.generatedAt || Date.now()) || Date.now()),         lists: { accepted, incoming, outgoing },         counts: {           accepted: Math.max(0, Number(counts.accepted ?? accepted.length) || accepted.length),
          incoming: Math.max(0, Number(counts.incoming ?? incoming.length) || incoming.length),           outgoing: Math.max(0, Number(counts.outgoing ?? outgoing.length) || outgoing.length),           online: Math.max(0, Number(counts.online ?? accepted.filter((item) => item.online).length) || accepted.filter((item) => item.online).length)         }       };
    }     function getLandingCenterData(){ return null; }      function renderLandingCenterSection(){ return; } 
    function getCurrentAccountLevel(){       return getUserLevel(state.userData);     }      function getSelectedFrameLevel(){
      const serverFrame = Number(state.userData?.selectedFrame ?? state.userData?.progression?.selectedFrame ?? Number.NaN);       if (Number.isFinite(serverFrame) && serverFrame >= 0) return normalizeFrameLevel(serverFrame);       return normalizeFrameLevel(state.selectedFrame ?? 0);     } 
    function syncSelectedFrameState(preferredFrame = null){       const rawPreferred = Number(preferredFrame);       const maxUnlockedFrame = state.userData ? getUserLevel(state.userData) : 100;       const candidate = Number.isFinite(rawPreferred) && rawPreferred >= 0         ? normalizeFrameLevel(rawPreferred)
        : getSelectedFrameLevel();       const nextFrame = candidate > maxUnlockedFrame ? 0 : Math.max(0, candidate);        state.selectedFrame = nextFrame;       if (state.userData && typeof state.userData === "object") {
        state.userData.selectedFrame = nextFrame;       }       try {         localStorage.setItem("pm_selected_frame", String(nextFrame));       } catch (_) {}
      return nextFrame;     }      function getDisplayFrameLevel(){       return getSelectedFrameLevel();
    }      function getTopbarAvatarShellId(){       if ($("topbarAvatarShell")) return "topbarAvatarShell";       if ($("headerAvatarShell")) return "headerAvatarShell";
      return null;     }      function mountTopbarAvatar(avatarUrl, frameLevel){       const targetId = getTopbarAvatarShellId();
      if (!targetId) return null;       const host = $(targetId);       const safeAvatar = safeUrl(avatarUrl || DEFAULT_AVATAR);       const img = document.createElement('img');       img.className = 'pm-topbar-avatar-only';       img.src = safeAvatar;       img.alt = 'Profil';       img.loading = 'eager';       img.decoding = 'async';       img.referrerPolicy = 'no-referrer';       img.draggable = false;       bindAvatarImageFallback(img);       host.replaceChildren(img);       host.dataset.frameHidden = 'true';       return img;     }
     function buildAvatarShell(avatarUrl, frameLevel, sizePx = 45, customClass = '') {       if (window.PMAvatar && typeof window.PMAvatar.buildHTML === 'function') {         return window.PMAvatar.buildHTML({           avatarUrl: avatarUrl || DEFAULT_AVATAR,
          level: normalizeFrameLevel(frameLevel),           sizePx,           extraClass: customClass,           imageClass: 'pm-avatar-img',           wrapperClass: 'pm-avatar',
          alt: 'Oyuncu'         });       }        const safeAvatar = safeUrl(avatarUrl || DEFAULT_AVATAR);
      const size = Math.max(18, Number(sizePx) || 45);       return `<div class="pm-avatar ${customClass}" data-pm-avatar="true" data-frame-index="0" data-pm-avatar-size-px="${size}"><img src="${safeAvatar}" alt="Oyuncu" class="pm-avatar-img" loading="lazy" decoding="async" referrerpolicy="no-referrer" draggable="false"></div>`;     }      function createAvatarShellNode(avatarUrl, frameLevel, sizePx = 45, extraClass = "") {
      let node = null;       if (window.PMAvatar && typeof window.PMAvatar.createNode === "function") {         node = window.PMAvatar.createNode({           avatarUrl,           level: normalizeFrameLevel(frameLevel),
          sizePx,           extraClass,           imageClass: 'pm-avatar-img',           wrapperClass: 'pm-avatar',           alt: 'Oyuncu'
        });       } else {         const safeAvatar = safeUrl(avatarUrl || DEFAULT_AVATAR);         const size = Math.max(18, Number(sizePx) || 45);         node = document.createElement('div');
        node.className = ('pm-avatar ' + (extraClass || '')).trim();         node.dataset.pmAvatar = 'true';         node.dataset.frameIndex = String(normalizeFrameLevel(frameLevel));         node.style.width = size + 'px';         node.style.height = size + 'px';
        const imgNode = document.createElement('img');         imgNode.src = safeAvatar;         imgNode.alt = 'Oyuncu';         imgNode.className = 'pm-avatar-img';         imgNode.loading = 'lazy';
        imgNode.decoding = 'async';         imgNode.referrerPolicy = 'no-referrer';         imgNode.draggable = false;         node.appendChild(imgNode);       }
       const img = node?.querySelector?.('img');       bindAvatarImageFallback(img);       if (img) img.referrerPolicy = 'no-referrer';       return node;
    }      function resolveAvatarFrameLevel(entity = null, fallbackFrame = 0) {       if (entity && typeof entity === 'object') {         const explicitFrame = Number(entity.selectedFrame ?? entity.progression?.selectedFrame ?? Number.NaN);
        if (Number.isFinite(explicitFrame) && explicitFrame >= 0) return normalizeFrameLevel(explicitFrame);       }       return normalizeFrameLevel(Number(fallbackFrame ?? 0));     } 
    function mountAvatarShell(target, avatarUrl, frameLevel, sizePx = 45, extraClass = "") {       const host = typeof target === 'string' ? $(target) : target;       if (!host) return null;       const node = createAvatarShellNode(avatarUrl, frameLevel, sizePx, extraClass);       host.replaceChildren(node);
      return node;     }       function syncBodyOverlayState(){
      const hasActiveModal = !!document.querySelector('.ps-modal.active');       document.body.classList.toggle('modal-open', hasActiveModal);     }      function openMatrixModal(id){
      const modal = $(id);       if (!modal) return;       modal.hidden = false;       modal.style.removeProperty('display');       modal.classList.remove('is-closing');       modal.classList.add('active','is-opening');       modal.setAttribute('aria-hidden', 'false');       window.setTimeout(() => modal.classList.remove('is-opening'), 220);       syncBodyOverlayState();
    }      function closeMatrixModal(id){       const modal = $(id);       if (!modal) return;
      modal.classList.remove('is-opening');       modal.classList.add('is-closing');       modal.setAttribute('aria-hidden', 'true');       window.setTimeout(() => { if (modal.classList.contains('is-closing')) { modal.classList.remove('active','is-closing'); modal.hidden = true; } syncBodyOverlayState(); }, 190);     } 
    function showMatrixModal(title, message, tone = 'info'){       const modal = $('matrixInfoModal');       if (!modal) return;       const icons = {         info: 'fa-circle-info',
        warning: 'fa-triangle-exclamation',         success: 'fa-circle-check',         error: 'fa-circle-xmark'       };       modal.dataset.tone = tone;
      $('matrixInfoTitle').textContent = title || 'Bilgi';       $('matrixInfoHeading').textContent = title || 'Bilgi';       $('matrixInfoMessage').textContent = message || '';       setIconHost($('matrixInfoIcon'), icons[tone] || icons.info);       openMatrixModal('matrixInfoModal');
    }      function showMatrixModalHtml(title, html, tone = 'info'){       const modal = $('matrixInfoModal');       if (!modal) return;
      const icons = {         info: 'fa-circle-info',         warning: 'fa-triangle-exclamation',         success: 'fa-circle-check',         error: 'fa-circle-xmark'
      };       modal.dataset.tone = tone;       $('matrixInfoTitle').textContent = title || 'Bilgi';       $('matrixInfoHeading').textContent = title || 'Bilgi';       appendSafeInlineMarkup($('matrixInfoMessage'), html || '');
      setIconHost($('matrixInfoIcon'), icons[tone] || icons.info);       openMatrixModal('matrixInfoModal');     }  
function getSocialRankMeta(){ return { name: "SEVİYE", className: "rank-level" }; }  async function ensureNotificationPermission() {   if (!("Notification" in window)) return "unsupported";
  if (Notification.permission === "granted") return "granted";   if (Notification.permission === "denied") return "denied";   try {     return await Notification.requestPermission();   } catch (_) {
    return Notification.permission || "default";   } }  async function primeRealtimeUX() {
  try {     if ("Notification" in window && Notification.permission === "default") {       await ensureNotificationPermission();     }   } catch (_) {}
}  function showNativeRealtimeNotification({ title = "PlayMatrix", body = "", tag = "", data = null } = {}) {   try {     if (!("Notification" in window) || Notification.permission !== "granted") return null;
    const notification = new Notification(title, {       body,       tag,       silent: false,       icon: "/apple-touch-icon.png",
      badge: "/apple-touch-icon.png",       data: data || null     });     notification.addEventListener("click", () => {       window.focus();
      const payload = notification.data || {};       if (payload.type === "dm" && payload.peerUid) {         if (ensureAuthThen("Sosyal Merkez")) {           openSocialSheet().then(() => {             setSocialTab("friends", { preferredKey: `friend:${payload.peerUid}`, openPanel: true });
            selectSocialItem(`friend:${payload.peerUid}`, { openPanel: true });           }).catch(() => null);         }       }       if (payload.type === "invite") {
        if (ensureAuthThen("Sosyal Merkez")) openSocialSheet().catch(() => null);       }       notification.close();     });     return notification;
  } catch (_) {     return null;   } }     async function fetchPrivate(endpoint, method = "GET", body = null, options = {}) {
      if (!auth.currentUser) throw new Error("Oturum bulunamadı.");       const token = await getIdToken(auth.currentUser, !!options.forceRefresh);       const sessionToken = readServerSessionToken();       const headers = { "Authorization": `Bearer ${token}`, "Content-Type": "application/json" };       if (sessionToken) headers['x-session-token'] = sessionToken;       const requestOptions = { method, headers, credentials: "include" };       if (body) requestOptions.body = JSON.stringify(body);
       let response;       await apiBaseReady;       try {         response = await requestWithApiFallback(endpoint, { ...requestOptions, cache: "no-store" });
        if (response.status === 401) {           const refreshedToken = await getIdToken(auth.currentUser, true).catch(() => "");           if (refreshedToken) {             requestOptions.headers.Authorization = `Bearer ${refreshedToken}`;             response = await requestWithApiFallback(endpoint, { ...requestOptions, cache: "no-store" });
          }         }         if (response.status === 401 && endpoint !== "/api/auth/session/create") {           const bootstrapPayload = await bootstrapServerSession(auth.currentUser, { force: true, forceRefresh: true }).catch(() => null);           if (bootstrapPayload?.ok !== false) {
            const recoveredToken = await getIdToken(auth.currentUser, true).catch(() => "");             if (recoveredToken) {               requestOptions.headers.Authorization = `Bearer ${recoveredToken}`;               response = await requestWithApiFallback(endpoint, { ...requestOptions, cache: "no-store" });             }
          }         }       } catch {         throw new Error("Sunucuyla bağlantı kurulamadı.");       }
       const contentType = response.headers.get("content-type") || "";       let payload = null;        if (!response.ok) {
        if (contentType.includes("application/json")) {           try { payload = await response.json(); } catch(_) {}           if (response.status === 401 && payload?.redirect) {             await forceSecureLogout(payload.error || "Oturum süren doldu.", { silent: true });             throw new Error(payload.error || "Oturum süresi doldu.");
          }           throw new Error((payload && (payload.error || payload.message)) || `Sunucu hatası. (${response.status})`);         }         if (response.status === 401) {           await forceSecureLogout("Oturum süren doldu.", { silent: true });
          throw new Error("Oturum süresi doldu.");         }         throw new Error(`Sunucu hatası. (${response.status})`);       } 
      if (contentType.includes("application/json")) {         payload = await response.json();         if (payload && payload.ok === false) throw new Error(payload.error || payload.message || "İşlem başarısız.");         return payload;       }
       throw new Error("Beklenmeyen sunucu yanıtı.");     }      function maybeShowLevelUpAnimation(userData = {}) {       const eventId = String(userData?.lastLevelUpEventId || userData?.progression?.lastLevelUpEventId || "").trim();       const toLevel = Number(userData?.lastLevelUpTo || userData?.progression?.lastLevelUpTo || userData?.accountLevel || 0);       if (!eventId || state.lastLevelUpEventId === eventId || !Number.isFinite(toLevel) || toLevel <= 1) return;       state.lastLevelUpEventId = eventId;       document.body.classList.add("pm-level-up-flash");       window.setTimeout(() => document.body.classList.remove("pm-level-up-flash"), 1400);       showToast("Seviye atladın", `Hesap seviyen ${toLevel} oldu.`, "success");     }      function applyProfilePayload(payload = {}) {       const source = payload?.user && typeof payload.user === "object" ? payload.user : payload;
      if (!source || typeof source !== "object") return null;       state.userData = normalizeApiUser(source);       state.selectedAvatar = safeUrl(state.userData.avatar || state.selectedAvatar || AVATARS[0]);       syncSelectedFrameState(state.userData?.selectedFrame ?? 0);       refreshCurrentUserAvatarSurfaces();       updateUserShell();       maybeShowLevelUpAnimation(state.userData);       if (state.currentSheet === "profile") refreshProfileSheet();       try { window.dispatchEvent(new CustomEvent("pm:profile-sync", { detail: state.userData })); } catch (_) {}       return state.userData;     }      async function refreshAuthVerificationState(force = false) {       if (!auth.currentUser) return null;
      try {         await reload(auth.currentUser);         if (force) await getIdToken(auth.currentUser, true);       } catch (error) {         reportClientError("auth.refresh_verification_state", error, { force: !!force });       }       return auth.currentUser;     }      async function syncProfileUpdateWithRetry(payload = {}, options = {}) {       const attempts = Math.max(1, Math.min(4, Number(options.attempts || 3) || 3));       let lastError = null;
      state.profileSyncInFlight = true;       for (let attempt = 1; attempt <= attempts; attempt += 1) {         try {           await bootstrapServerSession(auth.currentUser, { force: true, forceRefresh: attempt > 1 || !!options.forceRefresh });           const result = await fetchPrivate("/api/profile/update", "POST", payload, { forceRefresh: attempt > 1 || !!options.forceRefresh });           applyProfilePayload(result);           state.profileSyncRetryCount = attempt - 1;           state.profileSyncInFlight = false;           return result;         } catch (error) {           lastError = error;           state.profileSyncRetryCount = attempt;           if (attempt < attempts) await new Promise((resolve) => window.setTimeout(resolve, 250 * attempt));         }       }       state.profileSyncInFlight = false;       throw lastError || new Error("Profil senkronizasyonu tamamlanamadı.");     }      async function fetchAPI(endpoint, method = "GET", body = null, options = {}) {
      await apiBaseReady;       const headers = { Accept: "application/json", ...(options.headers || {}) };       if (body !== null && body !== undefined) headers["Content-Type"] = "application/json";        const fetchOptions = {
        method,         headers,         cache: "no-store",         ...options       };
       if (body !== null && body !== undefined) {         fetchOptions.body = typeof body === "string" ? body : JSON.stringify(body);       } 
      let response;       try {         response = await requestWithApiFallback(endpoint, fetchOptions);       } catch (_) {         throw new Error("Sunucuyla bağlantı kurulamadı.");
      }        const contentType = response.headers.get("content-type") || "";       let payload = null; 
      if (!response.ok) {         if (contentType.includes("application/json")) {           try { payload = await response.json(); } catch (_) {}           throw new Error((payload && (payload.error || payload.message)) || `Sunucu hatası. (${response.status})`);         }
        throw new Error(`Sunucu hatası. (${response.status})`);       }        if (!contentType.includes("application/json")) {         throw new Error("Beklenmeyen sunucu yanıtı.");
      }        payload = await response.json();       if (payload && payload.ok === false) throw new Error(payload.error || "İşlem başarısız.");       return payload;
    }       async function fetchPublic(endpoint, method = "GET", body = null, options = {}) {       return fetchAPI(endpoint, method, body, { credentials: "omit", ...options });
    }      function setFieldHelp(id, message = "", tone = "") {       const element = $(id);       if (!element) return;
      element.textContent = message;       const baseClass = element.classList.contains("ps-field-help") ? "ps-field-help" : "field-help";       element.className = `${baseClass}${tone ? ` is-${tone}` : ""}`;     } 
    function createToastBase({ tone = "info", iconClass = "fa-circle-info", title = "" }) {       const toast = document.createElement("div");       toast.className = "toast";       toast.dataset.tone = tone; 
      const iconWrap = document.createElement("div");       iconWrap.className = "toast-icon";       const icon = document.createElement("i");       icon.className = `fa-solid ${iconClass}`;       iconWrap.appendChild(icon);
       const body = document.createElement("div");       body.style.minWidth = "0";        const titleEl = document.createElement("div");
      titleEl.className = "toast-title";       titleEl.textContent = title;       body.appendChild(titleEl);        const closeBtn = document.createElement("button");
      closeBtn.className = "toast-close";       closeBtn.setAttribute("aria-label", "Kapat");       const closeIcon = document.createElement("i");       closeIcon.className = "fa-solid fa-xmark";       closeBtn.appendChild(closeIcon);
      closeBtn.addEventListener("click", () => toast.remove());        toast.append(iconWrap, body, closeBtn);       return { toast, body, closeBtn, titleEl };     }
     function appendToast(toast, timeoutMs = 4200) {       $("toastStack").appendChild(toast);       if (timeoutMs > 0) window.setTimeout(() => toast.remove(), timeoutMs);       return toast;
    }      function loadExternalScript(src) {       return new Promise((resolve, reject) => {         const existing = document.querySelector(`script[data-dynamic-src="${src}"]`);
        if (existing && existing.dataset.loaded === "true") {           resolve();           return;         }         if (existing) {
          existing.addEventListener("load", () => resolve(), { once:true });           existing.addEventListener("error", () => reject(new Error("Script yüklenemedi.")), { once:true });           return;         }         const script = document.createElement("script");
        script.src = src;         script.async = true;         script.defer = true;         script.crossOrigin = "anonymous";         script.dataset.dynamicSrc = src;
        script.addEventListener("load", () => {           script.dataset.loaded = "true";           resolve();         }, { once:true });         script.addEventListener("error", () => reject(new Error("Script yüklenemedi.")), { once:true });
        document.head.appendChild(script);       });     }      async function loadSocketClient() {
      await apiBaseReady;       if (typeof window.io === "function") return window.io;       if (!state.socketScriptPromise) {         state.socketScriptPromise = (async () => {           if (typeof PLAYMATRIX_API?.loadSocketClientScript === 'function') {
            try {               await PLAYMATRIX_API.loadSocketClientScript();               if (typeof window.io === 'function') return window.io;             } catch (_) {}           }
          const socketSources = typeof PLAYMATRIX_API?.getSocketClientCandidates === 'function'             ? PLAYMATRIX_API.getSocketClientCandidates()             : [`${API_URL}/socket.io/socket.io.js`];           let lastError = null;           for (const src of socketSources) {
            try {               await loadExternalScript(src);               if (typeof window.io === 'function') return window.io;             } catch (error) {               lastError = error;
            }           }           throw lastError || new Error('Canlı bağlantı bileşeni yüklenemedi.');         })();       }
      try {         await state.socketScriptPromise;       } catch (error) {         state.socketScriptPromise = null;         throw error;
      }       if (typeof window.io !== "function") throw new Error("Socket istemcisi hazır değil.");       return window.io;     } 
    function formatClockTime(ts) {       try {         return new Date(Number(ts) || Date.now()).toLocaleTimeString("tr-TR", { hour: "2-digit", minute: "2-digit" });       } catch (_) {         return "--:--";
      }     }       const DEFAULT_CHAT_POLICY = Object.freeze({         lobbyDays: 7,         directDays: 14,
        lobbyLabel: 'Global 7 Gün',         directLabel: 'DM 14 Gün',         summaryLabel: 'Global 7 Gün · DM 14 Gün',         manualDeleteLabel: 'Kullanıcı tarafından silindi',         cleanupLabel: 'Saklama süresi dolduğu için temizlendi',
        lobbyDisclosure: 'Yerel sohbet mesajları Render in-memory içinde geçici olarak saklanır; restart olursa silinir, en fazla 7 gün görünür.',         directDisclosure: 'DM mesajları Render in-memory içinde geçici olarak saklanır; restart olursa silinir, en fazla 14 gün görünür.',         tombstoneDisclosure: 'Silinen mesajların içeriği boş gösterilir; manuel silme ve saklama süresi temizliği ayrı etiketlenir.',         searchDisclosure: 'Mesaj arama yalnızca aktif in-memory mesajlarda çalışır.',         editWindowHours: 24,
        editWindowLabel: 'Özel mesajlar ilk 24 saat içinde düzenlenebilir.',         transparencyNote: 'Global ve DM saklama süreleri ayrı izlenir.'       });
    function mergeChatPolicy(policy = null) { return { ...DEFAULT_CHAT_POLICY, ...(policy && typeof policy === 'object' ? policy : {}) }; }
    function getActiveChatPolicy() {
      const policy = state.social.chatPolicy || state.social.centerSummary?.chatPolicy || state.userData?.chatPolicy || null;       return mergeChatPolicy(policy);     } 
    function getChatPolicySummary(data = null) {       const policy = (data && typeof data === 'object' ? data.chatPolicy : null) || getActiveChatPolicy();       if (policy?.summaryLabel) return String(policy.summaryLabel);       const lobbyDays = Number(policy?.lobbyDays || 7) || 7;       const directDays = Number(policy?.directDays || 7) || 7;
      return `Global ${lobbyDays} Gün · DM ${directDays} Gün`;     }      function describeMessageLifecycle(item = {}) {       if (!item?.deletedAt) return '';
      if (item.deletionMode === 'retention_cleanup') return item.deletedLabel || 'Süre dolduğu için temizlendi';       return item.deletedLabel || 'Kullanıcı tarafından silindi';     }      function buildChatPolicyNotice(context = 'dm') {
      const policy = getActiveChatPolicy();       const notice = document.createElement('section');       notice.className = `ps-retention-notice is-${context === 'global' ? 'global' : 'dm'}`;        const title = document.createElement('strong');
      title.textContent = context === 'global' ? 'Yerel sohbet saklama politikası' : 'DM saklama politikası';        const text = document.createElement('p');       text.textContent = context === 'global'         ? `${policy.lobbyDisclosure || policy.lobbyLabel} ${policy.tombstoneDisclosure || ''}`.trim()
        : `${policy.directDisclosure || policy.directLabel} ${policy.tombstoneDisclosure || ''}`.trim();        const chips = document.createElement('div');       chips.className = 'ps-retention-chips';       [
        policy.lobbyLabel || 'Global 7 Gün',         policy.directLabel || 'DM 14 Gün',         policy.manualDeleteLabel || 'Kullanıcı tarafından silindi',         policy.cleanupLabel || 'Saklama süresi dolduğu için temizlendi'       ].forEach((label) => {
        const chip = document.createElement('span');         chip.textContent = String(label || '').trim();         chips.appendChild(chip);       }); 
      notice.append(title, text, chips);       return notice;     }  
    function updateSocialConnectionBadge(extraText = "") {       const badge = $("psConnectionBadge");       if (!badge) return;       const shouldShow = state.currentSheet === "social";       badge.style.display = shouldShow ? "inline-flex" : "none";
      if (!shouldShow) return;       const text = extraText || (state.realtimeConnected ? "Bağlı" : (state.social.realtimeMode === "limited" ? "Sınırlı Mod" : "Bağlantı kuruluyor"));       badge.textContent = text;       badge.classList.toggle("is-offline", !state.realtimeConnected || state.social.realtimeMode === "limited");     }
     function isSocialMobile() {       return window.matchMedia("(max-width: 900px)").matches;     } 
    function disconnectRealtime(options = {}) {       clearDirectTypingState(false);       clearRealtimeReconnectTimer();       const preserveIntent = options?.preserveIntent !== false;       state.realtimeShouldBeConnected = preserveIntent && !!auth.currentUser;
      if (state.socket) {         state.socket.__pmIntentionalClose = true;         state.socket.removeAllListeners();         state.socket.disconnect();         state.socket = null;
      }       state.realtimeConnected = false;       updateSocialConnectionBadge();     } 
    function resetSocialState() {       dismissMatchmakingToast();       state.social = createSocialState();       state.friends = { accepted: [], incoming: [], outgoing: [], counts: { accepted:0, incoming:0, outgoing:0, online:0 } };     }
     async function loadSocialCenterSummary(force = false) {       if (!auth.currentUser) return null;       if (state.social.centerLoading && !force) return state.social.centerSummary;       if (!force && state.social.centerSummary) return state.social.centerSummary;
      state.social.centerLoading = true;       state.social.centerError = "";       try {         const payload = await fetchPrivate("/api/social-center/summary");         const normalized = normalizeSocialCenterPayload(payload || {});
        state.social.centerSummary = normalized;         state.social.chatPolicy = normalized.chatPolicy || state.social.chatPolicy;         state.social.centerLoadedAt = Date.now();         return state.social.centerSummary;       } catch (error) {
        state.social.centerError = error.message || "Sosyal merkez özeti yüklenemedi.";         return state.social.centerSummary;       } finally {         state.social.centerLoading = false;         if (state.currentSheet === "social") renderSocialHub();
      }     }      async function loadNotifications(force = false) {       if (!auth.currentUser) return [];
      if (state.social.notificationsLoading && !force) return state.social.notifications;       if (!force && state.social.notifications.length) return state.social.notifications;       state.social.notificationsLoading = true;       state.social.notificationsError = '';       try {
        const payload = await fetchPrivate('/api/notifications?limit=12');         state.social.notifications = Array.isArray(payload?.items) ? payload.items : [];         state.social.notificationsUnread = Number(payload?.unread || 0);         state.social.notificationsSummary = payload?.summary || null;         return state.social.notifications;
      } catch (error) {         state.social.notificationsError = error.message || 'Bildirimler yüklenemedi.';         reportClientError('home.notifications', error, { source: 'legacy-home.runtime.js' });         return [];       } finally {         state.social.notificationsLoading = false;
        if (state.currentSheet === 'social') renderSocialHub();       }     }      async function refreshSocialFeaturePack(force = false) {
      await Promise.allSettled([         loadSocialCenterSummary(force),         loadNotifications(force)       ]);     }
     async function loadDmSettings(targetUid = "") {       const safeTargetUid = String(targetUid || "").trim();       if (!safeTargetUid || !auth.currentUser) return null;       if (state.social.dmSettings[safeTargetUid]) return state.social.dmSettings[safeTargetUid];
      try {         const payload = await fetchPrivate(`/api/chat/settings?targetUid=${encodeURIComponent(safeTargetUid)}`);         state.social.dmSettings[safeTargetUid] = payload || { mine: {}, theirs: {} };         if (state.currentSheet === "social") renderSocialHub();         return state.social.dmSettings[safeTargetUid];
      } catch (_) {         return null;       }     } 
    async function loadDirectUnreadSummary() {       if (!auth.currentUser) return null;       try {         const payload = await fetchPrivate('/api/chat/direct/unread-summary');         state.social.unreadDirect = payload?.byPeer && typeof payload.byPeer === 'object' ? payload.byPeer : {};         if (state.currentSheet === 'social') renderSocialHub({ main: false });         return payload;       } catch (_) {         return null;       }     }

    async function searchDirectMessages(query = "", targetUid = "", options = {}) {       const q = String(query || "").trim();       const append = !!options.append;       state.social.dmSearchQuery = q;       state.social.dmSearchTargetUid = String(targetUid || "").trim();       if (!append) {         state.social.dmSearchNextCursor = "";         state.social.dmSearchHasMore = false;       }       if (q.length < 2) {
        state.social.dmSearchResults = [];         state.social.dmSearchNextCursor = "";         state.social.dmSearchHasMore = false;         if (state.currentSheet === "social") renderSocialHub();         return [];       }       state.social.dmSearchLoading = true;       state.social.dmSearchError = "";
      try {         const suffix = state.social.dmSearchTargetUid ? `&targetUid=${encodeURIComponent(state.social.dmSearchTargetUid)}` : "";         const cursorSuffix = append && state.social.dmSearchNextCursor ? `&cursor=${encodeURIComponent(state.social.dmSearchNextCursor)}` : "";         const payload = await fetchPrivate(`/api/chat/direct/search?q=${encodeURIComponent(q)}${suffix}${cursorSuffix}`);         const nextItems = Array.isArray(payload?.items) ? payload.items : [];         state.social.dmSearchResults = append ? [...(state.social.dmSearchResults || []), ...nextItems] : nextItems;         state.social.dmSearchNextCursor = String(payload?.nextCursor || "");         state.social.dmSearchHasMore = !!state.social.dmSearchNextCursor;         return state.social.dmSearchResults;
      } catch (error) {         showToast("Mesaj arama", error.message || "Arama yapılamadı.", "error");         state.social.dmSearchError = error.message || "Arama yapılamadı.";         if (!append) state.social.dmSearchResults = [];         return [];       } finally {
        state.social.dmSearchLoading = false;         if (state.currentSheet === "social") renderSocialHub();       }     } 
    async function saveShowcaseProfile(payload = {}) {       try {         const result = await fetchPrivate("/api/me/showcase", "POST", payload);         if (state.social.centerSummary?.me) {           state.social.centerSummary.me.showcase = {
            title: result?.showcase?.showcaseTitle || payload.title || "",             bio: result?.showcase?.showcaseBio || payload.bio || "",             favoriteGame: result?.showcase?.favoriteGame || payload.favoriteGame || "",             selectedBadge: result?.showcase?.selectedBadge || payload.selectedBadge || "",             profileBanner: result?.showcase?.profileBanner || payload.profileBanner || "",
          };         }         showToast("Profil vitrini", "Vitrin kaydedildi.", "success");         await loadSocialCenterSummary(true);       } catch (error) {
        showToast("Profil vitrini", error.message || "Kaydedilemedi.", "error");       }     }      async function claimActivityPassReward(level) {
      try {         const payload = await fetchPrivate("/api/activity-pass/claim", "POST", { level });         showToast("Activity Pass", `${payload?.rewardMc || 0} MC hesabına eklendi.`, "success");         await loadSocialCenterSummary(true);         if (typeof loadUserData === "function") loadUserData().catch(() => null);
      } catch (error) {         showToast("Activity Pass", error.message || "Ödül alınamadı.", "error");       }     } 
    async function handleDmRelationAction(action, targetUid) {       const safeTargetUid = String(targetUid || "").trim();       if (!safeTargetUid) return;       const endpointMap = {         archive: "/api/chat/direct/archive",
        unarchive: "/api/chat/direct/unarchive",         mute: "/api/chat/mute",         unmute: "/api/chat/unmute",         block: "/api/chat/block",         unblock: "/api/chat/unblock"
      };       const endpoint = endpointMap[action];       if (!endpoint) return;       try {         await fetchPrivate(endpoint, "POST", { targetUid: safeTargetUid });
        delete state.social.dmSettings[safeTargetUid];         await loadDmSettings(safeTargetUid);         if (action === "archive" || action === "unarchive") await loadFriends();         showToast("Sohbet ayarı", "Sohbet ayarı güncellendi.", "success");       } catch (error) {
        showToast("Sohbet ayarı", error.message || "İşlem başarısız.", "error");       }     }      function getSocialListItems(tab = state.social.activeTab) {
      if (tab === "friends") {         return (state.friends.accepted || []).map((entry) => ({ key: `friend:${entry.uid}`, type: "friend", ...entry, unread: Number(state.social.unreadDirect?.[entry.uid] || 0) }));       }       if (tab === "search") {         return [{ key: "search:dm", type: "search", title: "Mesaj Ara", subtitle: "Özel mesajlar içinde arama yap" }];
      }       if (tab === "invites") {         return (state.friends.accepted || []).map((entry) => ({ key: `invite:${entry.uid}`, type: "invite", ...entry }));       }       if (tab === "requests") {
        const incoming = (state.friends.incoming || []).map((entry) => ({ key: `request:${entry.friendshipId}`, type: "request", direction: "incoming", ...entry }));         const outgoing = (state.friends.outgoing || []).map((entry) => ({ key: `request:${entry.friendshipId}`, type: "request", direction: "outgoing", ...entry }));         return [...incoming, ...outgoing].sort((a, b) => (b.updatedAt || b.requestedAt || 0) - (a.updatedAt || a.requestedAt || 0));       }       if (tab === "add") {
        return [{ key: "add:manual", type: "add", title: "Kullanıcı adına göre ekle", subtitle: "Tam kullanıcı adıyla yeni bağlantı kur" }];       }       return [{ key: "global:lobby", type: "global", title: "Yerel (TR) Lobisi", subtitle: "Tüm çevrimiçi oyuncuların ortak akışı", totalMessages: state.lobbyMessages.length }];     } 
    function getSocialHeaderText(tab = state.social.activeTab) {       if (tab === "friends") return `Arkadaşlar · ${state.friends.counts.accepted || 0}`;       if (tab === "search") return "Mesaj Arama";       if (tab === "invites") return `Davet Et · ${state.friends.counts.accepted || 0}`;       if (tab === "requests") return `İstekler · ${(state.friends.counts.incoming || 0) + (state.friends.counts.outgoing || 0)}`;
      if (tab === "add") return "Yeni bağlantı";       return "Yerel Sohbet";     }      function ensureSocialSelection(preferredKey = "") {
      const items = getSocialListItems(state.social.activeTab);       if (preferredKey) state.social.selectedKey = preferredKey;       if (!items.some((item) => item.key === state.social.selectedKey)) {         state.social.selectedKey = items[0]?.key || "";       }
      return items.find((item) => item.key === state.social.selectedKey) || null;     }      function getSelectedSocialEntry() {       return ensureSocialSelection();
    }      function setSocialTab(tab, options = {}) {       const safeTab = ["global", "friends", "search", "requests", "add", "invites"].includes(tab) ? tab : "global";       const leavingFriends = state.social.activeTab === "friends" && safeTab !== "friends";
      if (leavingFriends) {         clearDirectTypingState(true);         state.social.currentActiveDmUid = null;       }       state.social.activeTab = safeTab;
      if (options.resetSelection) state.social.selectedKey = "";       const entry = ensureSocialSelection(options.preferredKey || "");       if (safeTab === "friends" && entry?.uid) {         state.social.currentActiveDmUid = entry.uid;         loadDirectHistoryForPeer(entry.uid);
      } else if (safeTab !== "friends") {         updateTypingIndicator("", false);       }       if (isSocialMobile()) {         state.social.mobilePanelOpen = !!options.openPanel;
      }       renderSocialHub();     }      function selectSocialItem(key, options = {}) {
      state.social.selectedKey = key || "";       const entry = ensureSocialSelection();       const nextDmUid = entry?.type === "friend" ? entry.uid : null;        if (state.social.currentActiveDmUid && state.social.currentActiveDmUid !== nextDmUid) {
        clearDirectTypingState(true);       }        state.social.currentActiveDmUid = nextDmUid;       if (nextDmUid) {
        state.social.unreadDirect[nextDmUid] = 0;         loadDirectHistoryForPeer(nextDmUid);       } else {         updateTypingIndicator("", false);       }
       if (isSocialMobile()) state.social.mobilePanelOpen = options.openPanel !== false;       renderSocialHub();     } 
    function updateFriendPresence(uid, nextPresence) {       ["accepted", "incoming", "outgoing"].forEach((bucket) => {         state.friends[bucket] = (state.friends[bucket] || []).map((entry) => {           if (entry.uid !== uid) return entry;           const presence = typeof nextPresence === "boolean"
            ? normalizePresenceState({ status: nextPresence ? "IDLE" : "OFFLINE", activity: nextPresence ? "Lobide" : "" }, nextPresence)             : normalizePresenceState(nextPresence, entry.online);           return { ...entry, online: presence.online, presence };         });       });
      state.friends.counts.online = (state.friends.accepted || []).filter((item) => item.online).length;       if (state.currentSheet === "social") renderSocialHub();     }      function getFriendMetaByUid(uid = "") {
      return (state.friends.accepted || []).find((entry) => entry.uid === uid) || null;     }      function getDirectConversation(peerUid = "") {       return Array.isArray(state.social.directMessages[peerUid])
        ? [...state.social.directMessages[peerUid]].sort((a, b) => (a.createdAt || 0) - (b.createdAt || 0))         : [];     }      function normalizeDirectMessagePayload(payload, forcedPeerUid = "") {
      if (!payload) return null;       const selfUid = auth.currentUser?.uid || "";       const senderUid = String(payload.fromUid || payload.senderUid || payload.sender || "").trim();       const rawTargetUid = String(payload.targetUid || payload.toUid || payload.peerUid || "").trim();       const peerUid = forcedPeerUid || (senderUid && senderUid !== selfUid ? senderUid : rawTargetUid);
      const deletedAt = Number(payload.deletedAt || 0);       const messageText = deletedAt > 0 ? "" : String(payload.message ?? payload.text ?? "").trim();       const createdAt = Number(payload.createdAt || payload.timestamp || Date.now());       const friendMeta = getFriendMetaByUid(peerUid);       const username = payload.username || payload.senderName || (senderUid === selfUid ? (state.userData?.username || "Sen") : (friendMeta?.username || "Oyuncu"));
      const avatar = payload.avatar || payload.senderAvatar || (senderUid === selfUid ? state.userData?.avatar : friendMeta?.avatar) || AVATARS[0];       if (!peerUid || (!messageText && !deletedAt)) return null;        return {         id: String(payload.id || payload.messageId || `${senderUid || peerUid}_${createdAt}_${(messageText || 'deleted').slice(0, 12)}`),
        chatId: String(payload.chatId || ""),         clientTempId: String(payload.clientTempId || ""),         fromUid: senderUid || selfUid,         senderUid: senderUid || selfUid,         sender: senderUid || selfUid,
        targetUid: rawTargetUid || (senderUid === selfUid ? peerUid : selfUid),         toUid: rawTargetUid || (senderUid === selfUid ? peerUid : selfUid),         username,         avatar,         message: messageText,
        text: messageText,         createdAt,         timestamp: createdAt,         editedAt: Number(payload.editedAt || 0),         deletedAt,
        deleted: deletedAt > 0,         deletionMode: String(payload.deletionMode || ''),         deletedLabel: String(payload.deletedLabel || ''),         deletedBy: String(payload.deletedBy || ''),         status: String(payload.status || "sent")
      };     }      function rememberDirectMessage(payload, forcedPeerUid = "") {       const normalized = normalizeDirectMessagePayload(payload, forcedPeerUid);
      if (!normalized) return { peerUid: null, inserted: false, message: null };        const selfUid = auth.currentUser?.uid || "";       const peerUid = forcedPeerUid || (normalized.fromUid === selfUid ? normalized.targetUid : normalized.fromUid);       const current = Array.isArray(state.social.directMessages[peerUid]) ? [...state.social.directMessages[peerUid]] : [];
      const exists = current.some((item) => item.id === normalized.id || (normalized.clientTempId && item.clientTempId && item.clientTempId === normalized.clientTempId));        if (!exists) {         current.push(normalized);         current.sort((a, b) => (a.createdAt || 0) - (b.createdAt || 0));
      }        state.social.directMessages[peerUid] = current.slice(-80);       return { peerUid, inserted: !exists, message: normalized };     }
     function updateTypingIndicator(peerUid = "", isTyping = false) {       const indicator = $("typingIndicator");       const label = $("typingIndicatorLabel");       if (!indicator || !label) return;
       const shouldShow = !!isTyping && !!peerUid && state.social.activeTab === "friends" && state.social.currentActiveDmUid === peerUid;       if (!shouldShow) {         indicator.classList.remove("is-visible");         label.textContent = "";
        return;       }        const friend = getFriendMetaByUid(peerUid);       label.textContent = `${friend?.username || "Arkadaşın"} yazıyor`;
      indicator.classList.add("is-visible");     }      function clearDirectTypingState(notifyPeer = false) {       if (state.social.typingTimerId) {
        window.clearTimeout(state.social.typingTimerId);         state.social.typingTimerId = 0;       }       if (notifyPeer && state.socket?.connected && state.social.currentActiveDmUid) {         state.socket.emit("chat:typing", { toUid: state.social.currentActiveDmUid, isTyping: false });
      }       updateTypingIndicator("", false);     }      function handleDirectTypingActivity() {
      if (state.social.activeTab !== "friends" || !state.social.currentActiveDmUid || !state.socket?.connected) return;       const input = $("psChatInput");       const value = (input?.value || "").trim();        if (!value) {
        clearDirectTypingState(true);         return;       }        state.socket.emit("chat:typing", { toUid: state.social.currentActiveDmUid, isTyping: true });
      if (state.social.typingTimerId) window.clearTimeout(state.social.typingTimerId);       state.social.typingTimerId = window.setTimeout(() => {         if (state.socket?.connected && state.social.currentActiveDmUid) {           state.socket.emit("chat:typing", { toUid: state.social.currentActiveDmUid, isTyping: false });         }
        state.social.typingTimerId = 0;       }, 2000);     }      async function loadDirectHistoryByHttp(peerUid) {
      const payload = await fetchPrivate(`/api/chat/direct/history?targetUid=${encodeURIComponent(peerUid)}&limit=60`);       const incoming = Array.isArray(payload?.items) ? payload.items : [];       const normalized = incoming.map((item) => normalizeDirectMessagePayload(item, peerUid)).filter(Boolean);       state.social.directMessages[peerUid] = normalized.sort((a, b) => (a.createdAt || 0) - (b.createdAt || 0)).slice(-80);       state.social.directHistoryLoadedAt[peerUid] = Date.now();
      delete state.social.directHistoryErrors[peerUid];       delete state.social.directHistoryPending[peerUid];       delete state.social.directHistoryRequestedAt[peerUid];       if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();       else if (state.currentSheet === "social") renderSocialHub({ main: false });
      return state.social.directMessages[peerUid];     }      function loadDirectHistoryForPeer(peerUid, options = {}) {       if (!peerUid) return;
      if (state.social.directHistoryPending[peerUid]) return;       if (!options.force && state.social.directHistoryLoadedAt[peerUid]) return;       state.social.directHistoryPending[peerUid] = true;       delete state.social.directHistoryErrors[peerUid];       state.social.directHistoryRequestedAt[peerUid] = Date.now();
       if (state.socket?.connected) {         state.socket.emit("chat:dm_load_history", { targetUid: peerUid });         window.setTimeout(() => {           if (!state.social.directHistoryPending[peerUid]) return;
          if (state.social.directHistoryRequestedAt[peerUid] !== undefined) {             loadDirectHistoryByHttp(peerUid).catch((error) => {               state.social.directHistoryErrors[peerUid] = error.message || "Konuşma geçmişi yüklenemedi.";               delete state.social.directHistoryPending[peerUid];               if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();
              else if (state.currentSheet === "social") renderSocialHub({ main: false });             });           }         }, 2500);         return;
      }        loadDirectHistoryByHttp(peerUid).catch((error) => {         state.social.directHistoryErrors[peerUid] = error.message || "Konuşma geçmişi yüklenemedi.";         delete state.social.directHistoryPending[peerUid];
        delete state.social.directHistoryRequestedAt[peerUid];         if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });       });     }
     function resolveInviteTargetHref(payload = {}) {       const roomId = encodeURIComponent(payload.roomId || "");       const gameKey = payload.gameKey || payload.gameCode || "";       const gamePath = payload.gamePath || ((payload.gameKey || payload.gameCode) === "pisti" ? "/games/pisti" : "/games/chess");
      return `${gamePath}?joinRoom=${roomId}`;     }      let inviteNavigationLock = false;     let inviteNavigationResetTimer = 0;
     function navigateToInviteRoom(payload = {}) {       const roomId = String(payload?.roomId || "").trim();       const gameKey = String(payload?.gameKey || payload?.gameCode || "").trim().toLowerCase();       if (!roomId) return false;
      if (inviteNavigationLock) {         const queuedRoomId = String(sessionStorage.getItem("pm_auto_join_room") || "").trim();         return queuedRoomId === roomId;       } 
      const href = resolveInviteTargetHref({ ...payload, roomId, gameKey });       if (!href) return false;        inviteNavigationLock = true;       if (inviteNavigationResetTimer) window.clearTimeout(inviteNavigationResetTimer);
      sessionStorage.setItem("pm_auto_join_room", roomId);       sessionStorage.setItem("pm_auto_join_game", gameKey || payload.gameKey || payload.gameCode || "");       sessionStorage.setItem("pm_auto_join_at", String(Date.now()));       inviteNavigationResetTimer = window.setTimeout(() => {         inviteNavigationLock = false;
        inviteNavigationResetTimer = 0;       }, 1800);       window.location.replace(href);       return true;     }
     window.addEventListener("pagehide", () => {       inviteNavigationLock = false;       if (inviteNavigationResetTimer) {         window.clearTimeout(inviteNavigationResetTimer);
        inviteNavigationResetTimer = 0;       }     });      function buildSocialEmptyState(title, message) {
      const box = document.createElement("div");       box.className = "ps-empty-state";        const titleEl = document.createElement("strong");       titleEl.textContent = title;
       const messageEl = document.createElement("div");       messageEl.textContent = message;        box.append(titleEl, messageEl);
      return box;     }      function buildSocialActionState(title, message, actionText = "Tekrar dene", onAction = null, tone = "") {       const box = buildSocialEmptyState(title, message);
      if (tone) box.dataset.tone = tone;       if (typeof onAction === "function") {         const actions = document.createElement("div");         actions.className = "ps-inline-actions ps-inline-actions--compact";         const button = document.createElement("button");
        button.type = "button";         button.className = "btn btn-primary";         button.textContent = actionText;         button.addEventListener("click", onAction);         actions.appendChild(button);
        box.appendChild(actions);       }       return box;     } 
    async function retrySocialBootstrap() {       await Promise.allSettled([loadFriends(true), refreshSocialFeaturePack(true), loadDirectUnreadSummary()]);       renderSocialHub({ force: true });     } 
    function getSocialPanelFailure(tab = state.social.activeTab) {       if (["friends", "requests", "invites"].includes(tab) && state.social.friendsError) {         return { title: "Sosyal liste yüklenemedi", message: state.social.friendsError || "Arkadaş verileri hazırlanamadı." };       }       if (tab === "global" && state.social.centerError && !state.lobbyMessages.length) {
        return { title: "Sosyal merkez özeti alınamadı", message: state.social.centerError || "Sosyal merkez verileri yüklenemedi." };       }       return null;     } 
    function createMiniTag(text, extraClass = "") {       const tag = document.createElement("span");       tag.className = `ps-mini-tag${extraClass ? ` ${extraClass}` : ""}`;       tag.textContent = text;       return tag;
    }      function normalizePresenceState(presence = null, fallbackOnline = false) {       const online = typeof presence?.online === "boolean" ? presence.online : !!fallbackOnline;       const rawStatus = String(presence?.status || (online ? "IDLE" : "OFFLINE")).trim().toUpperCase();
      const status = ["IDLE", "MATCHMAKING", "IN_GAME", "OFFLINE"].includes(rawStatus) ? rawStatus : (online ? "IDLE" : "OFFLINE");       const fallbackActivity = status === "IN_GAME"         ? "Oyunda"         : status === "MATCHMAKING"           ? "Eşleşme Aranıyor..."
          : status === "OFFLINE"             ? "Çevrimdışı"             : "Lobide";        return {
        status,         activity: String(presence?.activity || fallbackActivity).trim() || fallbackActivity,         online       };     }
     function getPresenceMeta(source = null) {       const presence = normalizePresenceState(source?.presence || source || null, !!source?.online);       if (presence.status === "IN_GAME") {         return { ...presence, dotClass: "is-busy", labelClass: "is-busy", shortLabel: "Oyunda", canInvite: false, canJoin: true };
      }       if (presence.status === "MATCHMAKING") {         return { ...presence, dotClass: "is-matchmaking", labelClass: "is-matchmaking", shortLabel: "Sırada", canInvite: false, canJoin: false };       }       if (presence.status === "OFFLINE") {
        return { ...presence, dotClass: "is-offline", labelClass: "is-offline", shortLabel: "Pasif", canInvite: false, canJoin: false };       }       return { ...presence, dotClass: "is-online", labelClass: "is-online", shortLabel: "Aktif", canInvite: true, canJoin: false };     } 
    function hydrateFriendEntry(entry = {}) {       const normalized = normalizeMemberPayload(entry);       return {         ...normalized,         friendshipId: String(entry.friendshipId || '').trim(),
        status: String(entry.status || 'pending').trim() || 'pending',         requestedAt: Math.max(0, Number(entry.requestedAt || 0) || 0),         updatedAt: Math.max(0, Number(entry.updatedAt || 0) || 0),         requesterUid: String(entry.requesterUid || '').trim(),         recipientUid: String(entry.recipientUid || '').trim()
      };     }      function updateMatchmakingToastMessage(message = "Uygun bir rakip aranıyor...") {       const toast = state.social.matchmakingToastEl;
      const el = toast?.querySelector?.("[data-matchmake-message]");       if (el) el.textContent = message;     }      function dismissMatchmakingToast() {
      if (state.social?.matchmakingToastEl?.remove) state.social.matchmakingToastEl.remove();       if (state.social) state.social.matchmakingToastEl = null;     }      function getMatchmakingPagePath(gameType = "") {
      return gameType === "pisti" ? "/games/pisti" : "/games/chess";     }      function showMatchmakingToast(gameType = "") {       dismissMatchmakingToast();
      const title = gameType === "pisti" ? "Pişti Eşleşmesi" : "Satranç Eşleşmesi";       const { toast, body, closeBtn } = createToastBase({         tone: "info",         iconClass: "fa-bolt",         title: `${title} aranıyor`
      });        closeBtn.addEventListener("click", () => cancelMatchmaking(gameType));        const message = document.createElement("div");
      message.className = "toast-message";       message.dataset.matchmakeMessage = "true";       message.textContent = "Uygun bir rakip aranıyor...";        const actions = document.createElement("div");
      actions.className = "invite-toast-actions";        const cancelBtn = document.createElement("button");       cancelBtn.className = "ghost-btn";       cancelBtn.type = "button";
      cancelBtn.textContent = "İptal Et";       cancelBtn.addEventListener("click", () => cancelMatchmaking(gameType));        actions.appendChild(cancelBtn);       body.append(message, actions);
      state.social.matchmakingToastEl = toast;       appendToast(toast, 0);       return toast;     } 
    async function startMatchmaking(gameType, options = {}) {       try {         if (!ensureAuthThen("Hızlı Eşleşme")) return;         const safeGameType = gameType === "chess" ? "chess" : gameType === "pisti" ? "pisti" : "";         if (!safeGameType) throw new Error("Geçersiz oyun türü.");
         const socket = await ensureRealtimeConnection();         if (!socket) throw new Error("Canlı bağlantı kurulamadı.");          const payload = { gameType: safeGameType };
         if (safeGameType === "pisti") {           payload.mode = ["2-52", "2-104", "4-104"].includes(String(options.mode || "").trim()) ? String(options.mode).trim() : "2-52";           const rawBet = Number(options.bet || 1000);           payload.bet = Number.isFinite(rawBet) ? Math.min(10000000, Math.max(1, Math.floor(rawBet))) : 1000;
        }          state.social.pendingMatchmaking = {           gameType: safeGameType,           mode: payload.mode || "",
          bet: payload.bet || 0,           startedAt: Date.now()         };          showMatchmakingToast(safeGameType);
        socket.emit("game:matchmake_join", payload);       } catch (error) {         dismissMatchmakingToast();         state.social.pendingMatchmaking = null;         showToast("Eşleşme hatası", error.message || "Hızlı eşleşme başlatılamadı.", "error");
      }     }      function cancelMatchmaking(gameType = "") {       const activeGameType = gameType || state.social.pendingMatchmaking?.gameType || "";
      dismissMatchmakingToast();        if (state.socket?.connected && activeGameType) {         state.socket.emit("game:matchmake_leave", { gameType: activeGameType });       }
       if (state.social.pendingMatchmaking) {         state.social.pendingMatchmaking = null;         showToast("Eşleşme iptal edildi", "Hızlı eşleşme araması durduruldu.", "info");       }
    }      window.startMatchmaking = startMatchmaking;     window.cancelMatchmaking = cancelMatchmaking; 
    function createSocialListItem(item, isActive) {       const button = document.createElement("button");       button.type = "button";       button.className = `ps-list-item${isActive ? " is-active" : ""}`;       button.dataset.socialKey = item.key;
      button.addEventListener("click", () => selectSocialItem(item.key));        let visual = null;       if (["friend", "request", "invite"].includes(item.type)) {         visual = document.createElement("div");
        visual.className = "ps-list-avatar";         visual.appendChild(createAvatarShellNode(item.avatar || AVATARS[0], resolveAvatarFrameLevel(item, 0), 42, "pm-avatar--social-list"));       } else {         visual = document.createElement("div");         visual.className = "ps-list-icon";
        const icon = document.createElement("i");         const iconMap = {           add: "fa-user-plus",           search: "fa-magnifying-glass",           global: "fa-earth-europe"
        };         icon.className = `fa-solid ${iconMap[item.type] || "fa-earth-europe"}`;         visual.appendChild(icon);       } 
      const copy = document.createElement("div");       copy.className = "ps-list-copy";        const title = document.createElement("div");       title.className = "ps-list-title";
      title.textContent = item.title || item.username || "Sosyal öğe";        const sub = document.createElement("div");       sub.className = "ps-list-sub"; 
      if (item.type === "friend") {         const presenceMeta = getPresenceMeta(item);         const dot = document.createElement("span");         dot.className = `ps-presence ${presenceMeta.dotClass}`.trim();         const text = document.createElement("span");
        text.className = `ps-presence-label ${presenceMeta.labelClass}`.trim();         text.textContent = presenceMeta.activity;         sub.append(dot, text);       } else if (item.type === "invite") {         const presenceMeta = getPresenceMeta(item);
        const text = document.createElement("span");         text.className = `ps-presence-label ${presenceMeta.labelClass}`.trim();         text.textContent = presenceMeta.canInvite ? "Davet gönderilebilir" : presenceMeta.activity;         sub.appendChild(text);       } else if (item.type === "request") {
        const text = document.createElement("span");         text.textContent = item.direction === "incoming" ? "Seni eklemek istiyor" : "Onay bekleniyor";         sub.appendChild(text);       } else if (item.type === "add") {         const text = document.createElement("span");
        text.textContent = item.subtitle || "Yeni arkadaşlık isteği oluştur";         sub.appendChild(text);       } else {         const text = document.createElement("span");         text.textContent = item.subtitle || "Gerçek zamanlı ortak akış";
        sub.appendChild(text);       }        copy.append(title, sub); 
      const trailing = document.createElement("div");       trailing.className = "ps-list-trailing";        if (item.type === "friend") {         const presenceMeta = getPresenceMeta(item);
        trailing.appendChild(createMiniTag(presenceMeta.shortLabel));         const unread = Number(item.unread || 0);         if (unread > 0) {           const badge = document.createElement("span");           badge.className = "ps-unread-badge";
          badge.textContent = unread > 99 ? "99+" : String(unread);           trailing.appendChild(badge);         }       } else if (item.type === "invite") {         const badge = document.createElement("span");
        badge.className = "ps-request-chip is-outgoing";         badge.textContent = "Seç";         trailing.appendChild(badge);       } else if (item.type === "request") {         const badge = document.createElement("span");
        badge.className = `ps-request-chip ${item.direction === "incoming" ? "is-incoming" : "is-outgoing"}`;         badge.textContent = item.direction === "incoming" ? "Gelen" : "Giden";         trailing.appendChild(badge);       } else if (item.type === "global") {         trailing.appendChild(createMiniTag(`${item.totalMessages || 0} mesaj`));
      }        button.append(visual, copy, trailing);       return button;     }
     function renderSocialList() {       const title = $("psMidTitle");       const wrap = $("psListContainer");       if (!title || !wrap) return;
       title.textContent = getSocialHeaderText();       const needsFriendLists = ["friends", "requests", "invites"].includes(state.social.activeTab);       wrap.replaceChildren(); 
      if (needsFriendLists && state.social.friendsLoading && !state.friends.accepted.length && !state.friends.incoming.length && !state.friends.outgoing.length) {         wrap.appendChild(buildSocialEmptyState("Liste hazırlanıyor", "Arkadaş ve davet verileri yükleniyor..."));         return;       } 
      if (needsFriendLists && state.social.friendsError && !state.friends.accepted.length && !state.friends.incoming.length && !state.friends.outgoing.length) {         wrap.appendChild(buildSocialActionState("Sosyal liste yüklenemedi", state.social.friendsError, "Tekrar Dene", () => retrySocialBootstrap(), "error"));         return;       } 
      if (state.social.activeTab === "global" && state.social.centerLoading && !state.social.centerSummary && !state.lobbyMessages.length) {         wrap.appendChild(buildSocialEmptyState("Sosyal merkez hazırlanıyor", "Özet ve sohbet alanı yükleniyor..."));         return;       } 
      const items = getSocialListItems(state.social.activeTab);       const selected = ensureSocialSelection();        if (!items.length) {         const emptyMessage = state.social.activeTab === "friends"
          ? "Henüz arkadaş eklemedin. Sağdaki ekleme sekmesinden yeni bağlantı kurabilirsin."           : state.social.activeTab === "invites"             ? "Davet gönderebilmek için önce en az bir arkadaş eklemelisin."             : state.social.activeTab === "search"                 ? "Mesaj aramak için en az 2 karakter gir."
                : state.social.activeTab === "requests"                   ? "Şu an bekleyen bir sosyal işlem görünmüyor."                   : "Bu bölüm için gösterilecek kayıt bulunamadı.";         wrap.appendChild(buildSocialEmptyState("Liste boş", emptyMessage));         return;
      }        items.forEach((item) => wrap.appendChild(createSocialListItem(item, item.key === selected?.key)));     } 
    function setSocialHeader(entry, subtitle = "") {       const title = $("psChatTitle");       const subtitleEl = $("psChatSubtitle");       const avatarWrap = $("psChatAvatar");       const avatarImg = avatarWrap?.querySelector("img");
      const backBtn = $("psMobileBackBtn");       const mainPanel = $("psMainPanel");       const socialLayout = document.querySelector('.sheet-shell.is-social .pm-social-layout');       const chatIsActive = !isSocialMobile() || !!state.social.mobilePanelOpen; 
      if (title) {         title.textContent = entry?.title || entry?.username || "Sosyal Merkez";         title.style.display = "block";       }       if (subtitleEl) {
        subtitleEl.textContent = subtitle;         subtitleEl.style.display = subtitle ? "block" : "none";       }        if (avatarWrap) {
        if (entry?.avatar) {           avatarWrap.style.display = "inline-flex";           mountAvatarShell(avatarWrap, entry.avatar || AVATARS[0], resolveAvatarFrameLevel(entry), 44, "pm-avatar--social-header");           avatarWrap.setAttribute('aria-label', `${entry.username || entry.title || "Profil"} avatarı`);         } else {
          avatarWrap.style.display = "none";           avatarWrap.replaceChildren();         }       } 
      if (backBtn) backBtn.style.display = isSocialMobile() && state.social.mobilePanelOpen ? "inline-flex" : "none";       if (mainPanel) mainPanel.classList.toggle("mobile-active", chatIsActive);       if (socialLayout) socialLayout.classList.toggle("is-chat-active", isSocialMobile() && !!state.social.mobilePanelOpen);     } 
    function setSocialHeaderActions(actions = []) {       const wrap = $("psChatActions");       if (!wrap) return;       wrap.replaceChildren();       const list = Array.isArray(actions) ? actions.filter(Boolean) : [];
      list.forEach((node) => wrap.appendChild(node));       wrap.style.display = wrap.childElementCount ? "flex" : "none";     }      function getActiveComposerDraft() {
      if (state.social.activeTab === "friends" && state.social.currentActiveDmUid) {         return state.social.composerDrafts?.direct?.[state.social.currentActiveDmUid] || "";       }       return state.social.composerDrafts?.global || "";     }
     function setActiveComposerDraft(value = "") {       const safeValue = String(value || "");       if (state.social.activeTab === "friends" && state.social.currentActiveDmUid) {         state.social.composerDrafts.direct[state.social.currentActiveDmUid] = safeValue;
        return;       }       state.social.composerDrafts.global = safeValue;     } 
    function clearActiveComposerDraft() {       if (state.social.activeTab === "friends" && state.social.currentActiveDmUid) {         delete state.social.composerDrafts.direct[state.social.currentActiveDmUid];         return;       }
      state.social.composerDrafts.global = "";     }      function replaceSocialStreamContent(stream, panelType, ...nodes) {       if (!stream) return;
      stream.dataset.panelType = panelType || "";       stream.replaceChildren(...nodes.filter(Boolean));     }      function setComposerState({ visible, placeholder = "", help = "", tone = "" }) {
      const inputArea = $("psInputArea");       const input = $("psChatInput");       const helpEl = $("psChatHelp");       if (inputArea) inputArea.style.display = visible ? "flex" : "none";       if (input) {
        input.placeholder = placeholder;         if (!visible) {           if (document.activeElement !== input) input.value = "";         } else {           const draftValue = getActiveComposerDraft();
          if (document.activeElement !== input && input.value !== draftValue) input.value = draftValue;         }       }       if (helpEl) {         helpEl.textContent = help;
        helpEl.className = `ps-chat-help${tone ? ` is-${tone}` : ""}`;         helpEl.style.display = visible || help ? "block" : "none";       }     } 
    function createSocialRenderSignature(payload) {       try {         return JSON.stringify(payload);       } catch (_) {         return `${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
      }     }      function captureSocialTransientState() {       const friendTargetInput = $("friendTargetInput");
      if (friendTargetInput) state.social.friendTargetDraft = friendTargetInput.value || "";        const dmSearchInput = $("dmSearchInput");       if (dmSearchInput) state.social.dmSearchQuery = dmSearchInput.value || ""; 
      const dmSearchTargetSelect = $("dmSearchTargetSelect");       if (dmSearchTargetSelect) state.social.dmSearchTargetUid = dmSearchTargetSelect.value || "";        const inviteGame = $("socialInviteGame");       if (inviteGame) state.social.inviteDraft.gameKey = inviteGame.value === "pisti" ? "pisti" : "chess";
       const inviteMode = $("socialInviteMode");       if (inviteMode) state.social.inviteDraft.mode = ["2-52", "2-104", "4-104"].includes(inviteMode.value) ? inviteMode.value : "2-52";       const inviteBet = $("socialInviteBet");       if (inviteBet) state.social.inviteDraft.bet = String(inviteBet.value || state.social.inviteDraft.bet || "10000");        const active = document.activeElement;
      const focusableIds = new Set(["friendTargetInput", "dmSearchInput", "dmSearchTargetSelect", "socialInviteGame", "socialInviteMode", "socialInviteBet", "psChatInput"]);       if (active && focusableIds.has(active.id)) {         state.social.transientFocus = {           id: active.id,           selectionStart: typeof active.selectionStart === "number" ? active.selectionStart : null,
          selectionEnd: typeof active.selectionEnd === "number" ? active.selectionEnd : null         };         return;       } 
      state.social.transientFocus = {         id: "",         selectionStart: null,         selectionEnd: null       };
    }      function restoreSocialTransientState() {       const friendTargetInput = $("friendTargetInput");       if (friendTargetInput && friendTargetInput.value !== state.social.friendTargetDraft) {
        friendTargetInput.value = state.social.friendTargetDraft || "";       }        const dmSearchInput = $("dmSearchInput");       if (dmSearchInput && dmSearchInput.value !== state.social.dmSearchQuery) {
        dmSearchInput.value = state.social.dmSearchQuery || "";       }        const dmSearchTargetSelect = $("dmSearchTargetSelect");       if (dmSearchTargetSelect && dmSearchTargetSelect.value !== (state.social.dmSearchTargetUid || "")) {
        dmSearchTargetSelect.value = state.social.dmSearchTargetUid || "";       }        const inviteGame = $("socialInviteGame");       if (inviteGame && inviteGame.value !== (state.social.inviteDraft.gameKey || "chess")) {
        inviteGame.value = state.social.inviteDraft.gameKey || "chess";       }        const inviteMode = $("socialInviteMode");       if (inviteMode && inviteMode.value !== (state.social.inviteDraft.mode || "2-52")) {         inviteMode.value = state.social.inviteDraft.mode || "2-52";       }        const inviteBet = $("socialInviteBet");       if (inviteBet && inviteBet.value !== String(state.social.inviteDraft.bet || "10000")) {
        inviteBet.value = String(state.social.inviteDraft.bet || "10000");       }        const focusMeta = state.social.transientFocus || {};       if (!focusMeta.id) return;
      const target = $(focusMeta.id);       if (!target || document.activeElement === target || target.disabled || !target.getClientRects().length) return;        try {         target.focus({ preventScroll: true });
      } catch (_) {         try { target.focus(); } catch (_) {}       }        if (typeof focusMeta.selectionStart === "number" && typeof target.setSelectionRange === "function") {
        try {           target.setSelectionRange(focusMeta.selectionStart, typeof focusMeta.selectionEnd === "number" ? focusMeta.selectionEnd : focusMeta.selectionStart);         } catch (_) {}       }     }
     function getSocialListSignature() {       const tab = state.social.activeTab;       const items = getSocialListItems(tab);       return createSocialRenderSignature({
        tab,         selectedKey: state.social.selectedKey,         friendsLoading: ["friends", "requests", "invites"].includes(tab) ? !!state.social.friendsLoading : false,         friendsError: ["friends", "requests", "invites"].includes(tab) ? state.social.friendsError || "" : "",         centerLoading: tab === "global" ? !!state.social.centerLoading : false,
        centerError: tab === "global" ? state.social.centerError || "" : "",         items: items.map((item) => ({           key: item.key,           type: item.type,           title: item.title || item.username || "",
          subtitle: item.subtitle || "",           unread: Number(item.unread || 0),           online: !!item.online,           presence: item.presence?.status || "",           direction: item.direction || "",
          totalMessages: Number(item.totalMessages || 0),           updatedAt: Number(item.updatedAt || item.requestedAt || 0)         }))       });     }
     function getSocialMainSignature() {       const tab = state.social.activeTab;       const entry = ensureSocialSelection();       const base = {
        tab,         selectedKey: state.social.selectedKey,         mobilePanelOpen: !!state.social.mobilePanelOpen,         panelFailure: getSocialPanelFailure(tab),         realtimeConnected: !!state.realtimeConnected,
        realtimeMode: state.social.realtimeMode || "full",         realtimeError: state.social.realtimeError || ""       };        if (tab === "global") {
        return createSocialRenderSignature({           ...base,           centerLoading: !!state.social.centerLoading,           centerError: state.social.centerError || "",           lobbyMessages: (state.lobbyMessages || []).map((item) => ({
            id: item?.id || item?.clientTempId || item?.createdAt || "",             createdAt: Number(item?.createdAt || item?.timestamp || 0),             editedAt: Number(item?.editedAt || 0),             deletedAt: Number(item?.deletedAt || 0),             status: item?.status || ""
          }))         });       }        if (tab === "friends") {
        const peerUid = entry?.uid || "";         const settings = state.social.dmSettings?.[peerUid] || { mine: {}, theirs: {} };         const conversation = Array.isArray(state.social.directMessages?.[peerUid]) ? state.social.directMessages[peerUid] : [];         return createSocialRenderSignature({           ...base,
          peerUid,           presence: entry?.presence?.status || "",           activity: entry?.presence?.activity || "",           unread: Number(state.social.unreadDirect?.[peerUid] || 0),           historyPending: !!state.social.directHistoryPending?.[peerUid],
          historyError: state.social.directHistoryErrors?.[peerUid] || "",           settings: {             mine: {               archived: !!settings?.mine?.archived,               muted: !!settings?.mine?.muted,
              blocked: !!settings?.mine?.blocked             },             theirs: {               blocked: !!settings?.theirs?.blocked             }
          },           messages: conversation.map((item) => ({             id: item?.id || item?.clientTempId || item?.createdAt || "",             createdAt: Number(item?.createdAt || item?.timestamp || 0),             editedAt: Number(item?.editedAt || 0),
            deletedAt: Number(item?.deletedAt || 0),             status: item?.status || ""           }))         });       }
       if (tab === "search") {         return createSocialRenderSignature({           ...base,           loading: !!state.social.dmSearchLoading,
          error: state.social.dmSearchError || "",           friendOptions: (state.friends.accepted || []).map((item) => ({ uid: item.uid, username: item.username || "", online: !!item.online })),           results: (state.social.dmSearchResults || []).map((item) => ({             peerUid: item?.peerUid || "",             username: item?.peer?.username || "",
            createdAt: Number(item?.createdAt || 0),             text: item?.text || ""           }))         });       }
       if (tab === "requests") {         const requests = [...(state.friends.incoming || []), ...(state.friends.outgoing || [])];         return createSocialRenderSignature({           ...base,
          entryKey: entry?.key || "",           requests: requests.map((item) => ({             friendshipId: item.friendshipId || "",             uid: item.uid || "",             username: item.username || "",
            updatedAt: Number(item.updatedAt || item.requestedAt || 0)           }))         });       } 
      if (tab === "invites") {         return createSocialRenderSignature({           ...base,           entryUid: entry?.uid || "",           online: !!entry?.online,
          presence: entry?.presence?.status || "",           activity: entry?.presence?.activity || ""         });       } 
      if (tab === "add") {         return createSocialRenderSignature(base);       }        return createSocialRenderSignature(base);
    }    
let activeSocialMenu = null; let activeSocialMenuCleanup = null; let lastSocialViewportWidth = window.innerWidth; let lastSocialViewportHeight = window.innerHeight; 
function isSocialKeyboardSensitiveFocus() {   const activeId = document.activeElement?.id || "";   return new Set(["friendTargetInput", "dmSearchInput", "psChatInput", "socialInviteBet"]).has(activeId); } 
function closeSocialMenu() {   if (typeof activeSocialMenuCleanup === 'function') {     try { activeSocialMenuCleanup(); } catch (_) {}   }   activeSocialMenuCleanup = null;
  if (activeSocialMenu?.parentNode) activeSocialMenu.parentNode.removeChild(activeSocialMenu);   activeSocialMenu = null; }  function openSocialMenu(anchor, items = []) {
  closeSocialMenu();   const actions = Array.isArray(items) ? items.filter((item) => item && typeof item.onClick === 'function') : [];   if (!anchor || !actions.length) return;    const menu = document.createElement('div');
  menu.className = 'ps-floating-menu';   actions.forEach((item) => {     const button = document.createElement('button');     button.type = 'button';     button.className = `ps-floating-menu__item${item.danger ? ' is-danger' : ''}`;
    if (item.icon) button.appendChild(createFaIcon(item.icon));     button.appendChild(createTextElement('span', '', item.label || 'İşlem'));     button.addEventListener('click', async (event) => {       event.preventDefault();       event.stopPropagation();
      closeSocialMenu();       try { await item.onClick(); } catch (_) {}     });     menu.appendChild(button);   });
   document.body.appendChild(menu);   activeSocialMenu = menu;    const placeMenu = () => {
    const rect = anchor.getBoundingClientRect();     const menuRect = menu.getBoundingClientRect();     const spacing = 10;     let left = rect.right - menuRect.width;     let top = rect.bottom + spacing;
    if (left < 8) left = 8;     if ((left + menuRect.width) > (window.innerWidth - 8)) left = window.innerWidth - menuRect.width - 8;     if ((top + menuRect.height) > (window.innerHeight - 8)) top = rect.top - menuRect.height - spacing;     if (top < 8) top = 8;     menu.style.left = `${Math.round(left)}px`;
    menu.style.top = `${Math.round(top)}px`;   };    const handleOutside = (event) => {     if (!menu.contains(event.target) && !anchor.contains(event.target)) closeSocialMenu();
  };    activeSocialMenuCleanup = () => {     document.removeEventListener('pointerdown', handleOutside, true);     window.removeEventListener('resize', placeMenu);
    window.removeEventListener('scroll', placeMenu, true);   };   document.addEventListener('pointerdown', handleOutside, true);   window.addEventListener('resize', placeMenu);   window.addEventListener('scroll', placeMenu, true);
  requestAnimationFrame(placeMenu); }  function buildHeaderIconButton(iconClass, label, onClick) {   const button = document.createElement('button');
  button.className = 'icon-btn';   button.type = 'button';   button.setAttribute('aria-label', label);   button.title = label;   button.replaceChildren(createFaIcon(iconClass));
  button.addEventListener('click', onClick);   return button; }  function buildOverflowActionButton(label, actions = []) {
  return buildHeaderIconButton('fa-ellipsis-vertical', label, (event) => {     event.preventDefault();     event.stopPropagation();     openSocialMenu(event.currentTarget, actions);   });
}  async function reportChatMessage(item = {}, context = 'dm') {   const targetUid = String(item?.fromUid || item?.senderUid || item?.uid || '').trim();   const messageId = String(item?.id || item?.messageId || '').trim();
  if (!targetUid || !messageId) throw new Error('Rapor için yeterli kayıt yok.');   const reason = window.prompt('Rapor nedeni: spam / hakaret / tehdit / rahatsiz_etme / dolandiricilik / diger', 'spam');   if (reason === null) return false;   const details = window.prompt('İsteğe bağlı kısa not', '') || '';   await fetchPrivate('/api/chat/report', 'POST', { targetUid, messageId, context, reason, details });
  showToast('Mesaj raporu', 'Rapor moderasyon kuyruğuna alındı.', 'success');   return true; }  
function buildMessageBubble(item, isSelf = false, context = "dm") {   const card = document.createElement("article");   card.className = `ps-chat-bubble ${context === "global" ? "is-global" : "is-dm"}${isSelf ? " is-self" : ""}`;   if (item?.deletedAt) card.classList.add('is-retention'); 
  const shell = document.createElement("div");   shell.className = "ps-chat-shell";    const targetUid = item.fromUid || item.senderUid || item.uid || item.targetUid || "";   const frameLevel = isSelf ? getDisplayFrameLevel() : resolveAvatarFrameLevel(item, 0);
  const avatarUrl = isSelf     ? safeUrl(state.selectedAvatar || state.userData?.avatar || AVATARS[0])     : safeUrl(item.avatar || AVATARS[0]);    const avatar = createAvatarShellNode(
    avatarUrl,     frameLevel,     38,     "ps-chat-avatar-shell pm-avatar--chat"   );
  avatar.classList.add("ps-chat-avatar");   if (targetUid) {     avatar.style.cursor = "pointer";     avatar.title = "Profili aç";     avatar.addEventListener("click", () => window.showPlayerStats?.(targetUid));
  }    const main = document.createElement("div");   main.className = "ps-chat-main"; 
  const topLine = document.createElement("div");   topLine.className = "ps-chat-topline";    const name = document.createElement("strong");   name.className = "ps-chat-name";
  name.textContent = isSelf ? "Sen" : (item.username || "Oyuncu");   if (targetUid) {     name.style.cursor = "pointer";     name.title = "Profili aç";     name.addEventListener("click", () => window.showPlayerStats?.(targetUid));
  }   topLine.appendChild(name);     const body = document.createElement("div");
  body.className = "ps-chat-text";   if (item?.deletedAt) body.classList.add('is-retention');   body.textContent = item.deletedAt ? describeMessageLifecycle(item) || "Bu mesaj silindi." : (item.message || item.text || "");    const bottom = document.createElement("div");
  bottom.className = "ps-chat-bottom";    const time = document.createElement("span");   time.className = "ps-chat-time";   time.textContent = formatClockTime(item.createdAt);
  bottom.appendChild(time);    if (item.editedAt && !item.deletedAt) {     const edited = document.createElement("span");     edited.className = "ps-chat-time";
    edited.textContent = "düzenlendi";     bottom.appendChild(edited);   }    if (item.deletedAt) {
    const lifecycle = document.createElement('span');     lifecycle.className = 'ps-chat-time ps-chat-lifecycle';     lifecycle.textContent = describeMessageLifecycle(item) || 'Silindi';     bottom.appendChild(lifecycle);   }
   if (context === "dm" && isSelf && item.id && !item.deletedAt) {     const actions = document.createElement("div");     actions.className = "ps-chat-inline-actions"; 
    const moreBtn = document.createElement("button");     moreBtn.type = "button";     moreBtn.className = "ps-chat-mini-btn";     moreBtn.setAttribute("aria-label", "Mesaj seçenekleri");     moreBtn.title = "Mesaj seçenekleri";
    moreBtn.replaceChildren(createFaIcon('fa-ellipsis'));     moreBtn.addEventListener("click", (event) => {       event.preventDefault();       event.stopPropagation();       openSocialMenu(event.currentTarget, [
        {           label: 'Düzenle',           icon: 'fa-pen',           onClick: async () => {             const nextText = window.prompt('Mesajı düzenle', item.message || item.text || '');
            if (nextText === null) return;             try {               await fetchPrivate('/api/chat/direct/edit', 'POST', { targetUid: state.social.currentActiveDmUid, messageId: item.id, text: nextText });               await loadDirectHistoryForPeer(state.social.currentActiveDmUid, { force: true });             } catch (error) {
              showToast('DM Düzenleme', error.message || 'Mesaj güncellenemedi.', 'error');             }           }         },         {
          label: 'Sil',           icon: 'fa-trash',           danger: true,           onClick: async () => {             try {
              await fetchPrivate('/api/chat/direct/delete', 'POST', { targetUid: state.social.currentActiveDmUid, messageId: item.id });               await loadDirectHistoryForPeer(state.social.currentActiveDmUid, { force: true });             } catch (error) {               showToast('DM Silme', error.message || 'Mesaj silinemedi.', 'error');             }
          }         }       ]);     }); 
    actions.append(moreBtn);     bottom.appendChild(actions);   }    if (!isSelf && item.id && !item.deletedAt) {
    const actions = document.createElement('div');     actions.className = 'ps-chat-inline-actions';     const moreBtn = document.createElement('button');     moreBtn.type = 'button';     moreBtn.className = 'ps-chat-mini-btn';
    moreBtn.setAttribute('aria-label', 'Mesaj seçenekleri');     moreBtn.title = 'Mesaj seçenekleri';     moreBtn.replaceChildren(createFaIcon('fa-ellipsis'));     moreBtn.addEventListener('click', (event) => {       event.preventDefault();
      event.stopPropagation();       openSocialMenu(event.currentTarget, [{         label: 'Mesajı bildir',         icon: 'fa-flag',         danger: true,
        onClick: async () => {           try {             await reportChatMessage(item, context === 'global' ? 'global' : 'dm');           } catch (error) {             showToast('Mesaj raporu', error.message || 'Rapor gönderilemedi.', 'error');
          }         }       }]);     });     actions.append(moreBtn);
    bottom.appendChild(actions);   }    main.append(topLine, body, bottom);   if (isSelf) shell.append(main, avatar);
  else shell.append(avatar, main);   card.appendChild(shell);   return card; } 
function renderMessageStack(stream, messages, emptyTitle, emptyMessage, getIsSelf, context = "dm") {        const nearBottom = (stream.scrollHeight - stream.scrollTop - stream.clientHeight) < 60;       const stack = document.createElement("div");       stack.className = "ps-conversation-stack";
       if (context === 'global' || context === 'dm') {         stack.appendChild(buildChatPolicyNotice(context));       } 
      if (!messages.length) {         stack.appendChild(buildSocialEmptyState(emptyTitle, emptyMessage));       } else {         messages.forEach((item) => stack.appendChild(buildMessageBubble(item, !!getIsSelf(item), context)));       }
       const currentStack = stream.querySelector('.ps-conversation-stack');       if (currentStack) {         currentStack.replaceWith(stack);       } else {
        replaceSocialStreamContent(stream, stream.dataset.panelType || context, stack);       }       if (nearBottom) stream.scrollTop = stream.scrollHeight;     } 
    function syncInviteModeVisibility() {       const gameSelect = $("socialInviteGame");       const modeField = $("socialInviteMode")?.closest(".ps-field");       if (!gameSelect || !modeField) return;       modeField.style.display = gameSelect.value === "pisti" ? "grid" : "none";
    }      function getSelectedInviteConfig() {       const gameKey = $("socialInviteGame")?.value === "pisti" ? "pisti" : "chess";       const mode = $("socialInviteMode")?.value || "2-52";
      const rawBet = Number($("socialInviteBet")?.value || 1000);       const bet = Number.isFinite(rawBet) ? Math.min(10000000, Math.max(1, Math.floor(rawBet))) : 1000;       return {         gameKey,         gameName: gameKey === "pisti" ? "Online Pişti" : "Satranç",
        mode,         bet       };     } 
    function renderGlobalPanel(stream) {       if (!stream) return;       stream.dataset.panelType = "global";       const isLimited = state.social.realtimeMode === "limited" || !state.realtimeConnected;       setSocialHeader({ title: "#Yerel Sohbet (TR)" }, isLimited ? "Bağlantı sınırlı modda" : "99+ Kişi");
      setSocialHeaderActions();       setComposerState({         visible: true,         placeholder: "Saygılı ve kısa bir mesaj yaz...",         help: state.realtimeConnected ? `${getActiveChatPolicy().lobbyDisclosure || getActiveChatPolicy().lobbyLabel} · ${getActiveChatPolicy().cleanupLabel}` : "Canlı bağlantı kurulamadıysa tekrar deneyebilir veya temel sosyal alanları kullanmaya devam edebilirsin."
      });        if (isLimited && !state.lobbyMessages.length) {         replaceSocialStreamContent(stream, "global", buildSocialActionState("Yerel sohbet sınırlı modda", state.social.realtimeError || "Canlı lobi akışı şu an eşitlenemiyor. Tekrar deneyerek gerçek zamanlı bağlantıyı yenileyebilirsin.", "Bağlantıyı Yenile", () => openSocialSheet().catch(() => null), "warning"));         return;
      }        renderMessageStack(         stream,         state.lobbyMessages,
        "Lobi şu an sakin",         "İlk mesajı göndererek akışı sen başlatabilirsin.",         (item) => !!auth.currentUser && item.uid === auth.currentUser.uid,         "global"       );
    }       function appendLobbyMessage(msg) {       const stream = $("psChatStream");
      if (!stream) return null;        let stack = stream.querySelector('.ps-conversation-stack');       if (!stack) {         stream.replaceChildren();
        stack = document.createElement('div');         stack.className = 'ps-conversation-stack';         stream.appendChild(stack);       } 
      const emptyState = stack.querySelector('.ps-empty-state');       if (emptyState) emptyState.remove();        const isSelf = !!auth.currentUser && msg?.uid === auth.currentUser.uid;       const bubble = buildMessageBubble(msg || {}, isSelf, 'global');
      stack.appendChild(bubble);       stream.scrollTop = stream.scrollHeight;       return bubble;     } 
    function appendLobbyMessageToActiveStream(msg) {       if (state.currentSheet !== 'social' || state.social.activeTab !== 'global') return false;       if (isSocialMobile() && !state.social.mobilePanelOpen) return false;       return !!appendLobbyMessage(msg);     }
     function renderActiveLobbyStream() {       if (state.currentSheet !== 'social' || state.social.activeTab !== 'global') return false;       if (isSocialMobile() && !state.social.mobilePanelOpen) return false;       const stream = $("psChatStream");
      if (!stream) return false;       renderGlobalPanel(stream);       return true;     } 
    function renderActiveDirectStream(peerUid = state.social.currentActiveDmUid) {       if (state.currentSheet !== 'social' || state.social.activeTab !== 'friends') return false;       if (!peerUid || state.social.currentActiveDmUid !== peerUid) return false;       if (isSocialMobile() && !state.social.mobilePanelOpen) return false;       const stream = $("psChatStream");
      const entry = getFriendMetaByUid(peerUid);       if (!stream || !entry) return false;       renderFriendPanel(stream, entry);       return true;     }
     function renderFriendPanel(stream, entry) {       if (!stream || !entry?.uid) return;       stream.dataset.panelType = `friend:${entry.uid}`;       const settings = state.social.dmSettings?.[entry.uid] || { mine: {}, theirs: {} };
      if (!state.social.dmSettings?.[entry.uid]) loadDmSettings(entry.uid).catch(() => null);        const removeBtn = document.createElement("button");       removeBtn.className = "icon-btn";       removeBtn.type = "button";
      removeBtn.setAttribute("aria-label", "Arkadaşı kaldır");       removeBtn.title = "Arkadaşı kaldır";       removeBtn.replaceChildren(createFaIcon('fa-user-minus'));       removeBtn.addEventListener("click", () => removeFriend(entry)); 
      const searchBtn = buildHeaderIconButton('fa-magnifying-glass', 'Mesaj ara', () => {         state.social.dmSearchTargetUid = entry.uid;         state.social.dmSearchQuery = '';         state.social.dmSearchResults = [];         setSocialTab('search', { preferredKey: 'search:dm', openPanel: true });
      });        const overflowBtn = buildOverflowActionButton('Sohbet seçenekleri', [         {           label: settings?.mine?.archived ? 'Arşivden çıkar' : 'Sohbeti arşivle',
          icon: settings?.mine?.archived ? 'fa-box-open' : 'fa-box-archive',           onClick: () => handleDmRelationAction(settings?.mine?.archived ? 'unarchive' : 'archive', entry.uid)         },         {           label: settings?.mine?.muted ? 'Sesi aç' : 'Sessize al',
          icon: settings?.mine?.muted ? 'fa-bell' : 'fa-bell-slash',           onClick: () => handleDmRelationAction(settings?.mine?.muted ? 'unmute' : 'mute', entry.uid)         },         {           label: settings?.mine?.blocked ? 'Engeli kaldır' : 'Engelle',
          icon: settings?.mine?.blocked ? 'fa-user-check' : 'fa-user-lock',           onClick: () => handleDmRelationAction(settings?.mine?.blocked ? 'unblock' : 'block', entry.uid)         },         {           label: 'Arkadaşı kaldır',
          icon: 'fa-user-minus',           danger: true,           onClick: () => removeFriend(entry)         }       ]);
       state.social.currentActiveDmUid = entry.uid;       state.social.unreadDirect[entry.uid] = 0;       loadDirectHistoryForPeer(entry.uid); 
      const presenceMeta = getPresenceMeta(entry);       const relationSummary = [         settings?.mine?.archived ? 'Arşivli' : '',         settings?.mine?.muted ? 'Sessiz' : '',         settings?.mine?.blocked ? 'Engelli' : ''
      ].filter(Boolean).join(' · ');       setSocialHeader({ title: entry.username || 'Arkadaş', avatar: entry.avatar }, `Özel Sohbet · ${presenceMeta.activity}${relationSummary ? ` · ${relationSummary}` : ''}`);       setSocialHeaderActions([searchBtn, overflowBtn]);       setComposerState({ visible: !settings?.mine?.blocked && !settings?.theirs?.blocked, placeholder: `${entry.username || "Arkadaşın"} için mesaj yaz...`, help: settings?.mine?.blocked ? "Bu kullanıcıyı engelledin. Mesaj göndermek için engeli kaldır." : settings?.theirs?.blocked ? "Bu kullanıcı mesajlarını kabul etmiyor." : `${getActiveChatPolicy().directDisclosure || getActiveChatPolicy().directLabel} · ${getActiveChatPolicy().tombstoneDisclosure || 'Silinen mesajlar bilgi etiketiyle görünür.'}` }); 
      const conversation = getDirectConversation(entry.uid);       const historyPending = !!state.social.directHistoryPending[entry.uid];       const historyError = state.social.directHistoryErrors?.[entry.uid] || "";       if (historyError && !conversation.length) {         replaceSocialStreamContent(stream, `friend:${entry.uid}`, buildSocialActionState("Özel sohbet yüklenemedi", historyError, "Sohbeti Yenile", () => loadDirectHistoryForPeer(entry.uid, { force: true }), "error"));
      } else {         renderMessageStack(           stream,           conversation,           historyPending ? "Geçmiş yükleniyor" : "Henüz özel mesaj yok",
          historyPending ? "Son mesajlar güvenli şekilde senkronize ediliyor." : `${entry.username || "Arkadaşın"} ile ilk sohbeti başlatabilirsin.`,           (item) => !!auth.currentUser && item.fromUid === auth.currentUser.uid,           "dm"         );       }
       updateTypingIndicator(entry.uid, false);     }      function renderRequestPanel(stream, entry) {
      const title = entry.direction === "incoming" ? "Gelen İstek" : "Giden İstek";       setSocialHeader({ title: entry.username || "Arkadaşlık İsteği", avatar: entry.avatar }, title);       setSocialHeaderActions();       setComposerState({ visible: false, help: entry.direction === "incoming" ? "İsteği kabul ederek doğrudan özel mesaj ve oyun daveti açarsın." : "Karşı tarafın onayı geldiğinde bağlantı otomatik güncellenir." }); 
      const card = document.createElement("section");       card.className = "ps-request-card";        const profileRow = document.createElement("div");       profileRow.className = "ps-profile-row";
      const avatarWrap = document.createElement("div");       avatarWrap.className = "friend-avatar";       avatarWrap.appendChild(createAvatarShellNode(entry.avatar || AVATARS[0], resolveAvatarFrameLevel(entry), 58, "pm-avatar--social-card"));        const meta = document.createElement("div");
      meta.className = "ps-profile-meta";       const name = document.createElement("strong");       name.textContent = entry.username || "Oyuncu";       const subtitle = document.createElement("span");       subtitle.textContent = entry.direction === "incoming"
        ? "Seninle arkadaş olmak istiyor."         : "Yanıt bekleniyor.";       meta.append(name, subtitle);       profileRow.append(avatarWrap, meta); 
      const actions = document.createElement("div");       actions.className = "ps-request-actions";       if (entry.direction === "incoming") {         const acceptBtn = document.createElement("button");         acceptBtn.className = "btn btn-primary";
        acceptBtn.type = "button";         acceptBtn.textContent = "Kabul Et";         acceptBtn.addEventListener("click", () => respondFriendRequest(entry.friendshipId, "accept"));          const declineBtn = document.createElement("button");
        declineBtn.className = "ghost-btn";         declineBtn.type = "button";         declineBtn.textContent = "Reddet";         declineBtn.addEventListener("click", () => respondFriendRequest(entry.friendshipId, "decline"));         actions.append(acceptBtn, declineBtn);
      } else {         const cancelBtn = document.createElement("button");         cancelBtn.className = "ghost-btn";         cancelBtn.type = "button";         cancelBtn.textContent = "İsteği Geri Çek";
        cancelBtn.addEventListener("click", () => removeFriend(entry));         actions.appendChild(cancelBtn);       }        card.append(profileRow, actions);
      stream.appendChild(card);     }      function renderAddPanel(stream) {       if (!stream) return;
      const panelType = "add";       const panelChanged = stream.dataset.panelType !== panelType;       if (panelChanged) replaceSocialStreamContent(stream, panelType);       else stream.dataset.panelType = panelType; 
      setSocialHeader({ title: "Yeni Arkadaş Ekle" }, "Bağlantı Başlat");       setSocialHeaderActions();       setComposerState({ visible: false, help: "Eklediğin kişi çevrimiçiyse anlık bildirim alır. Doğrudan davet ve DM sadece onaylı arkadaşlarda açılır." });        let card = stream.querySelector("#socialAddPanelCard");
      if (!card) {         card = document.createElement("section");         card.id = "socialAddPanelCard";         card.className = "ps-panel-card"; 
        const formGrid = document.createElement("div");         formGrid.className = "ps-form-grid";         formGrid.style.gridTemplateColumns = "1fr";          const userField = document.createElement("div");
        userField.className = "ps-field";         const userLabel = document.createElement("label");         userLabel.setAttribute("for", "friendTargetInput");         userLabel.textContent = "Kullanıcı adı";         const userInput = document.createElement("input");
        userInput.id = "friendTargetInput";         userInput.type = "text";         userInput.placeholder = "Örn: oyuncu_adi";         userInput.maxLength = 20;         userInput.autocomplete = "off";
        userField.append(userLabel, userInput);         formGrid.append(userField);          const actions = document.createElement("div");         actions.className = "ps-inline-actions";
        const addBtn = document.createElement("button");         addBtn.className = "btn btn-primary";         addBtn.id = "friendAddBtn";         addBtn.type = "button";         addBtn.textContent = "Arkadaşlık İsteği Gönder";
        addBtn.addEventListener("click", handleFriendAdd);         actions.appendChild(addBtn);          const help = document.createElement("div");         help.className = "ps-field-help";
        help.id = "friendActionHelp";          card.append(formGrid, actions, help);         stream.appendChild(card); 
        userInput.addEventListener("input", () => {           state.social.friendTargetDraft = userInput.value || "";         });         userInput.addEventListener("keydown", (event) => {           if (event.key === "Enter") {
            event.preventDefault();             handleFriendAdd();           }         });       }
       const userInput = card.querySelector("#friendTargetInput");       if (userInput && document.activeElement !== userInput && userInput.value !== (state.social.friendTargetDraft || "")) {         userInput.value = state.social.friendTargetDraft || "";       }
      const help = card.querySelector("#friendActionHelp");       if (help && !help.textContent) {         help.textContent = "Kullanıcı adını eksiksiz girin. İstek gönderimi arkadaş koleksiyonuna yazılır ve karşı taraf çevrimiçiyse anlık uyarı düşer.";       }     }
     function renderInvitePanel(stream, entry) {       if (!stream || !entry?.uid) return;       const panelType = `invite:${entry.uid}`;       const panelChanged = stream.dataset.panelType !== panelType;
      if (panelChanged) replaceSocialStreamContent(stream, panelType);       else stream.dataset.panelType = panelType;        setSocialHeader({ title: entry.username || "Oyuncu", avatar: entry.avatar }, "Oyuna Davet Et");       setSocialHeaderActions();
      setComposerState({ visible: false, help: "" });        let card = stream.querySelector("#socialInvitePanelCard");       if (!card) {         card = document.createElement("section");
        card.id = "socialInvitePanelCard";         card.className = "ps-request-card";          const profileRow = document.createElement("div");         profileRow.className = "ps-profile-row";
        profileRow.id = "socialInviteProfileRow";          const avatarWrap = document.createElement("div");         avatarWrap.className = "friend-avatar";         avatarWrap.id = "socialInviteProfileAvatar";
         const meta = document.createElement("div");         meta.className = "ps-profile-meta";         const name = document.createElement("strong");         name.id = "socialInviteProfileName";
        const subtitle = document.createElement("span");         subtitle.id = "socialInviteProfileSubtitle";         meta.append(name, subtitle);         profileRow.append(avatarWrap, meta); 
        const formGrid = document.createElement("div");         formGrid.className = "ps-form-grid";         formGrid.style.marginTop = "14px";         formGrid.style.marginBottom = "14px";         formGrid.style.gridTemplateColumns = "1fr";
         const gameField = document.createElement("div");         gameField.className = "ps-field";         const gameLabel = document.createElement("label");         gameLabel.textContent = "Hangi Oyunu Oynayacaksınız?";
        const gameSelect = document.createElement("select");         gameSelect.id = "socialInviteGame";         gameSelect.className = "field-select";         gameSelect.style.height = "48px";         gameSelect.style.border = "1px solid rgba(255,255,255,0.15)";
        gameSelect.style.backgroundColor = "rgba(0,0,0,0.4)";         [           ['chess', 'Satranç (Sıralamalı)'],           ['pisti', 'Online Pişti (Giriş Ücretli)']         ].forEach(([value, label]) => {
          const option = document.createElement('option');           option.value = value;           option.textContent = label;           gameSelect.appendChild(option);         });
        gameField.append(gameLabel, gameSelect);          const betField = document.createElement("div");         betField.className = "ps-field";         betField.id = "socialInviteBetField";
        const modeField = document.createElement("div");         modeField.className = "ps-field";         modeField.id = "socialInviteModeField";         const modeLabel = document.createElement("label");         modeLabel.setAttribute("for", "socialInviteMode");         modeLabel.textContent = "Pişti Modu";         const modeSelect = document.createElement("select");         modeSelect.id = "socialInviteMode";         modeSelect.className = "field-select";         [['2-52', '2 Kişi · 52 Deste'], ['2-104', '2 Kişi · 104 Deste'], ['4-104', '4 Kişi · 104 Deste']].forEach(([value, label]) => {           const option = document.createElement('option');           option.value = value;           option.textContent = label;           modeSelect.appendChild(option);         });         modeField.append(modeLabel, modeSelect);          const betLabel = document.createElement("label");         betLabel.textContent = "Maç Ücreti (MC)";         const betInput = document.createElement("input");         betInput.type = "number";         betInput.id = "socialInviteBet";
        betInput.className = "field-input";         betInput.min = "1";         betInput.max = "10000000";         betInput.step = "1";         betInput.inputMode = "numeric";         betInput.style.height = "48px";         betInput.style.border = "1px solid rgba(255,255,255,0.15)";         betInput.style.backgroundColor = "rgba(0,0,0,0.4)";         betInput.placeholder = "Örn: 1000";
        betField.append(betLabel, betInput);          gameSelect.addEventListener("change", () => {           state.social.inviteDraft.gameKey = gameSelect.value === "pisti" ? "pisti" : "chess";           modeField.style.display = gameSelect.value === "pisti" ? "grid" : "none";           betField.style.display = gameSelect.value === "pisti" ? "grid" : "none";
        });         modeSelect.addEventListener("change", () => {           state.social.inviteDraft.mode = ["2-52", "2-104", "4-104"].includes(modeSelect.value) ? modeSelect.value : "2-52";         });         betInput.addEventListener("input", () => {           state.social.inviteDraft.bet = String(betInput.value || "");         }); 
        formGrid.append(gameField, modeField, betField);          const actions = document.createElement("div");         actions.className = "ps-request-actions";         const inviteBtn = document.createElement("button");
        inviteBtn.className = "btn btn-primary";         inviteBtn.id = "socialInviteSubmitBtn";         inviteBtn.type = "button";         inviteBtn.style.width = "100%";         inviteBtn.style.justifyContent = "center";
        inviteBtn.style.height = "52px";         inviteBtn.style.fontSize = "1rem";         inviteBtn.append(createFaIcon('fa-gamepad', { marginRight: '8px' }), document.createTextNode('Hemen Davet Gönder'));         actions.appendChild(inviteBtn); 
        card.append(profileRow, formGrid, actions);         stream.appendChild(card);       }        const avatarWrap = card.querySelector("#socialInviteProfileAvatar");
      if (avatarWrap) {         avatarWrap.replaceChildren();         avatarWrap.appendChild(createAvatarShellNode(entry.avatar || AVATARS[0], resolveAvatarFrameLevel(entry), 58, "pm-avatar--social-card"));       }       const name = card.querySelector("#socialInviteProfileName");
      if (name) name.textContent = entry.username || "Oyuncu";       const subtitle = card.querySelector("#socialInviteProfileSubtitle");       const invitePresence = getPresenceMeta(entry);       if (subtitle) {         subtitle.textContent = invitePresence.canInvite
          ? "Şu an Aktif - Bildirim anında iletilir"           : invitePresence.status === "OFFLINE"             ? "Çevrimdışı - Davet gönderilemez"             : `Meşgul - ${invitePresence.activity}`;       }
       const gameSelect = card.querySelector("#socialInviteGame");       const modeSelect = card.querySelector("#socialInviteMode");       const modeField = card.querySelector("#socialInviteModeField");       const betInput = card.querySelector("#socialInviteBet");       const betField = card.querySelector("#socialInviteBetField");       if (gameSelect && gameSelect.value !== (state.social.inviteDraft.gameKey === "pisti" ? "pisti" : "chess")) {
        gameSelect.value = state.social.inviteDraft.gameKey === "pisti" ? "pisti" : "chess";       }       if (modeSelect && modeSelect.value !== (state.social.inviteDraft.mode || "2-52")) {         modeSelect.value = state.social.inviteDraft.mode || "2-52";       }       if (betInput && document.activeElement !== betInput && betInput.value !== String(state.social.inviteDraft.bet || "10000")) {         betInput.value = String(state.social.inviteDraft.bet || "10000");       }
      if (modeField) modeField.style.display = gameSelect?.value === "pisti" ? "grid" : "none";       if (betField) betField.style.display = gameSelect?.value === "pisti" ? "grid" : "none";        const inviteBtn = card.querySelector("#socialInviteSubmitBtn");       if (inviteBtn) {         inviteBtn.disabled = !invitePresence.canInvite;
        inviteBtn.style.opacity = invitePresence.canInvite ? "1" : ".55";         inviteBtn.style.cursor = invitePresence.canInvite ? "pointer" : "not-allowed";         if (!inviteBtn.dataset.inviteBound) {           inviteBtn.dataset.inviteBound = 'true';           inviteBtn.addEventListener('click', () => {             if (!invitePresence.canInvite) {             showToast(
              invitePresence.status === "OFFLINE" ? "Oyuncu çevrimdışı" : "Oyuncu meşgul",               invitePresence.status === "OFFLINE"                 ? "Bu arkadaş şu anda aktif değil. Davet yalnızca çevrimiçi oyunculara iletilir."                 : `${entry.username || "Bu oyuncu"} şu anda meşgul (${invitePresence.activity}).`,               "info"
            );             return;           }           createInviteForFriend(entry);           });         }       }     }      function buildMetricChip(label, value) {       const chip = document.createElement("div");
      chip.className = "ps-metric-chip";       chip.append(createTextElement('span', '', label), createTextElement('strong', '', String(value ?? '-')));       return chip;     } 
    function renderSearchPanel(stream) {       if (!stream) return;       const panelType = "search";       const panelChanged = stream.dataset.panelType !== panelType;       if (panelChanged) replaceSocialStreamContent(stream, panelType);
      else stream.dataset.panelType = panelType;        setSocialHeader({ title: "Mesaj Arama" }, state.social.dmSearchTargetUid ? "Seçili arkadaş konuşmasında arama" : "Tüm DM konuşmalarında arama");       setSocialHeaderActions();       setComposerState({ visible: false, help: getActiveChatPolicy().searchDisclosure || "En az 2 karakter yazarak DM geçmişinde arama yapabilirsin." });
       let card = stream.querySelector("#socialSearchPanelCard");       let resultWrap = stream.querySelector("#socialSearchResults");        if (!card) {
        card = document.createElement("section");         card.id = "socialSearchPanelCard";         card.className = "ps-panel-card ps-dashboard-card";         card.appendChild(buildChatPolicyNotice('dm'));         const inlineActions = document.createElement('div');
        inlineActions.className = 'ps-inline-actions';         const searchInputEl = document.createElement('input');         searchInputEl.id = 'dmSearchInput';         searchInputEl.className = 'field-input';         searchInputEl.type = 'text';
        searchInputEl.maxLength = 80;         searchInputEl.placeholder = 'Mesajlarda ara';         const targetSelectEl = document.createElement('select');         targetSelectEl.id = 'dmSearchTargetSelect';         targetSelectEl.className = 'field-select';
        const searchButtonEl = document.createElement('button');         searchButtonEl.id = 'dmSearchBtn';         searchButtonEl.className = 'btn btn-primary';         searchButtonEl.type = 'button';         searchButtonEl.textContent = 'Ara';
        inlineActions.append(searchInputEl, targetSelectEl, searchButtonEl);         card.appendChild(inlineActions);         resultWrap = document.createElement("div");         resultWrap.id = "socialSearchResults";         resultWrap.className = "ps-mini-list";
        stream.append(card, resultWrap);          const searchInput = card.querySelector("#dmSearchInput");         const targetSelect = card.querySelector("#dmSearchTargetSelect");         const searchBtn = card.querySelector("#dmSearchBtn");
         searchInput?.addEventListener("input", (event) => {           state.social.dmSearchQuery = event.currentTarget?.value || "";         });         targetSelect?.addEventListener("change", (event) => {
          state.social.dmSearchTargetUid = event.currentTarget?.value || "";         });         searchBtn?.addEventListener("click", () => searchDirectMessages(searchInput?.value || "", targetSelect?.value || ""));         searchInput?.addEventListener("keydown", (event) => {           if (event.key === "Enter") {
            event.preventDefault();             searchDirectMessages(searchInput?.value || "", targetSelect?.value || "");           }         });       }
       const searchInput = card.querySelector("#dmSearchInput");       const targetSelect = card.querySelector("#dmSearchTargetSelect");       if (searchInput && document.activeElement !== searchInput && searchInput.value !== (state.social.dmSearchQuery || "")) {         searchInput.value = state.social.dmSearchQuery || "";
      }        if (targetSelect) {         const selectedValue = state.social.dmSearchTargetUid || '';         const nextSignature = JSON.stringify((state.friends.accepted || []).map((item) => [item.uid, item.username || item.uid]));
        if ((targetSelect.dataset.signature || '') !== nextSignature) {           targetSelect.replaceChildren();           const allOption = document.createElement('option');           allOption.value = '';           allOption.textContent = 'Tüm arkadaşlar';
          targetSelect.appendChild(allOption);           (state.friends.accepted || []).forEach((item) => {             const option = document.createElement('option');             option.value = String(item.uid || '');             option.textContent = String(item.username || item.uid || 'Oyuncu');
            targetSelect.appendChild(option);           });           targetSelect.dataset.signature = nextSignature;         }         if (targetSelect.value !== selectedValue) targetSelect.value = selectedValue;
      }        if (!resultWrap) {         resultWrap = document.createElement("div");         resultWrap.id = "socialSearchResults";
        resultWrap.className = "ps-mini-list";         stream.appendChild(resultWrap);       }        resultWrap.replaceChildren();
      if (state.social.dmSearchLoading) {         resultWrap.appendChild(buildSocialEmptyState("Aranıyor", "Mesajlar taranıyor..."));       } else if (state.social.dmSearchError) {         resultWrap.appendChild(buildSocialActionState("Mesaj araması tamamlanamadı", state.social.dmSearchError, "Tekrar Dene", () => searchDirectMessages(searchInput?.value || state.social.dmSearchQuery || "", targetSelect?.value || state.social.dmSearchTargetUid || ""), "error"));       } else if (!(state.social.dmSearchResults || []).length) {
        resultWrap.appendChild(buildSocialEmptyState("Sonuç yok", state.social.dmSearchQuery ? "Eşleşen mesaj bulunamadı." : "Aramak istediğin kelimeyi yaz."));       } else {         (state.social.dmSearchResults || []).forEach((item) => {           const row = document.createElement("button");           row.type = "button";
          row.className = "ps-search-result";           row.appendChild(createAvatarShellNode(item?.peer?.avatar || AVATARS[0], resolveAvatarFrameLevel(item?.peer, 0), 34, "pm-avatar--social-list"));           const copy = document.createElement("div");           copy.append(createTextElement('strong', '', item?.peer?.username || 'Oyuncu'), createTextElement('span', '', item.text || ''), createTextElement('small', '', formatClockTime(item.createdAt || Date.now())));           row.appendChild(copy);
          row.addEventListener("click", () => {             setSocialTab("friends", { preferredKey: `friend:${item.peerUid}`, openPanel: true });             selectSocialItem(`friend:${item.peerUid}`, { openPanel: true });           });           resultWrap.appendChild(row);
        });         if (state.social.dmSearchHasMore) {           const moreBtn = document.createElement('button');           moreBtn.type = 'button';           moreBtn.className = 'ghost-btn ps-search-more';           moreBtn.textContent = state.social.dmSearchLoading ? 'Yükleniyor...' : 'Daha Fazla Sonuç';           moreBtn.disabled = !!state.social.dmSearchLoading;           moreBtn.addEventListener('click', () => searchDirectMessages(state.social.dmSearchQuery, state.social.dmSearchTargetUid, { append: true }));           resultWrap.appendChild(moreBtn);         }       }     }  function renderSocialMain() {
      const stream = $("psChatStream");       if (!stream) return;        const panelFailure = getSocialPanelFailure(state.social.activeTab);       if (panelFailure) {
        setSocialHeader({ title: "Sosyal Merkez" }, "Sorun giderildikten sonra içerik yeniden yüklenecek");         setSocialHeaderActions();         setComposerState({ visible: false, help: "Bağlantı ve veri yükleme hatalarında buradan tekrar deneyebilirsin." });         replaceSocialStreamContent(stream, "error", buildSocialActionState(panelFailure.title, panelFailure.message, "Tekrar Dene", () => retrySocialBootstrap(), "error"));         return;
      }        const entry = ensureSocialSelection();       if (!entry) {         setSocialHeader({ title: "Sosyal Merkez" }, "Bir öğe seçildiğinde ayrıntılar burada açılır");
        setSocialHeaderActions();         setComposerState({ visible: false, help: "Sol taraftan bir sekme ve kayıt seçerek devam et." });         replaceSocialStreamContent(stream, "empty", buildSocialEmptyState("İçerik bekleniyor", "Listeden bir öğe seçtiğinde sağ panel o görünüm için hazırlanır."));         return;       }
       if (state.social.activeTab === "friends" && !entry.uid) {         setSocialHeader({ title: "Arkadaşlar" }, "Aktif bir arkadaş seçilmedi");         setSocialHeaderActions();         setComposerState({ visible: false, help: "Özel sohbete başlamak için orta listeden bir arkadaş seç." });
        replaceSocialStreamContent(stream, "empty-friend", buildSocialEmptyState("Arkadaş seç", "Çevrimiçi durum ve özel mesaj alanı seçimin sonrası yüklenir."));         return;       }        if (state.social.activeTab === "invites" && !entry.uid) {
        setSocialHeader({ title: "Oyun Daveti" }, "Kimseye davet gönderilmiyor");         setSocialHeaderActions();         setComposerState({ visible: false, help: "Davet etmek için listeden bir arkadaşını seç." });         replaceSocialStreamContent(stream, "empty-invite", buildSocialEmptyState("Arkadaş seç", "Oyun daveti gönderebilmek için sol listeden bir arkadaşını seçmelisin."));         return;
      }        if (state.social.activeTab === "requests" && !entry.friendshipId) {         setSocialHeader({ title: "İstekler" }, "Bekleyen sosyal işlem yok");         setSocialHeaderActions();
        setComposerState({ visible: false, help: "Yeni istek geldiğinde bu ekran otomatik güncellenir." });         replaceSocialStreamContent(stream, "empty-request", buildSocialEmptyState("Bekleyen kayıt yok", "Şu an yönetilecek bir arkadaşlık isteği görünmüyor."));         return;       } 
      if (state.social.activeTab === "friends") {         renderFriendPanel(stream, entry);         return;       }       if (state.social.activeTab === "search") {
        renderSearchPanel(stream);         return;       }       if (state.social.activeTab === "invites") {         renderInvitePanel(stream, entry);
        return;       }       if (state.social.activeTab === "requests") {         renderRequestPanel(stream, entry);         return;
      }       if (state.social.activeTab === "add") {         renderAddPanel(stream);         return;       }
      renderGlobalPanel(stream);     }      function renderSocialTabs() {       document.querySelectorAll(".ps-tab").forEach((tabButton) => {
        tabButton.classList.toggle("is-active", tabButton.dataset.socialTab === state.social.activeTab);       });     }      function renderSocialHub(options = {}) {
      if (!$("psListContainer") || !$("psMainPanel")) return;       captureSocialTransientState();       ensureSocialSelection();        const force = !!options?.force;
      const shouldRenderTabs = options.tabs !== false;       const shouldRenderList = options.list !== false;       const shouldRenderMain = options.main !== false;       const tabSignature = state.social.activeTab;       const listSignature = getSocialListSignature();
      const mainSignature = getSocialMainSignature();        if (shouldRenderTabs && (force || state.social.renderCache.tabs !== tabSignature)) {         renderSocialTabs();         state.social.renderCache.tabs = tabSignature;
      }       if (shouldRenderList && (force || state.social.renderCache.list !== listSignature)) {         renderSocialList();         state.social.renderCache.list = listSignature;       }
      if (shouldRenderMain && (force || state.social.renderCache.main !== mainSignature)) {         closeSocialMenu();         renderSocialMain();         state.social.renderCache.main = mainSignature;       }
       updateSocialConnectionBadge();       handleSocialComposerInput();       restoreSocialTransientState();     }
     async function loadFriends(force = false) {       if (!auth.currentUser) return state.friends;       if (state.social.friendsLoading && !force) return state.friends;       state.social.friendsLoading = true;
      state.social.friendsError = "";       try {         const payload = await fetchPrivate("/api/friends/list");         const normalized = normalizeFriendsPayload(payload || {});         state.friends = {
          accepted: normalized.lists.accepted,           incoming: normalized.lists.incoming,           outgoing: normalized.lists.outgoing,           counts: normalized.counts         };
        state.social.friendsLoadedAt = Date.now();         if (state.currentSheet === "social") refreshSocialFeaturePack().catch(() => null);         renderSocialHub();         return state.friends;       } catch (error) {
        state.social.friendsError = error.message || "Arkadaş listesi yüklenemedi.";         const helpEl = $("friendActionHelp");         if (helpEl) {           helpEl.textContent = state.social.friendsError;           helpEl.className = "ps-field-help is-error";
        }         if (state.currentSheet === "social") renderSocialHub();         return state.friends;       } finally {         state.social.friendsLoading = false;
      }     }      async function handleFriendAdd() {       try {
        const target = ($("friendTargetInput")?.value || "").trim();         if (!target) throw new Error("Lütfen tam kullanıcı adını yazın.");         setFieldHelp("friendActionHelp", "", "");         const payload = await fetchPrivate("/api/friends/request", "POST", { target });         state.social.friendTargetDraft = "";
        if ($("friendTargetInput")) $("friendTargetInput").value = "";         setFieldHelp("friendActionHelp", payload.message || "Arkadaşlık isteği gönderildi.", "success");         showToast("Arkadaşlık işlemi", payload.message || "İstek gönderildi.", "success");         await loadFriends();       } catch (error) {
        setFieldHelp("friendActionHelp", error.message || "İstek gönderilemedi.", "error");       }     }      window.sendFriendRequest = async function sendFriendRequest(uid) {
      try {         if (!ensureAuthThen("Arkadaş ekleme")) return;         const targetUid = String(uid || "").trim();         if (!targetUid) throw new Error("Kullanıcı bilgisi eksik.");         const payload = await fetchPrivate("/api/friends/request", "POST", { targetUid });
        showToast("Arkadaşlık işlemi", payload.message || "İstek gönderildi.", "success");         await loadFriends();         closeMatrixModal("playerStatsModal");       } catch (error) {         showToast("Arkadaşlık hatası", error.message || "İstek gönderilemedi.", "error");
      }     };      async function respondFriendRequest(friendshipId, action) {       try {
        await fetchPrivate("/api/friends/respond", "POST", { friendshipId, action });         showToast("Arkadaşlık işlemi", action === "accept" ? "İstek kabul edildi." : "İstek reddedildi.", action === "accept" ? "success" : "info");         await loadFriends();       } catch (error) {         showToast("Arkadaşlık hatası", error.message || "İşlem tamamlanamadı.", "error");
      }     }      async function removeFriend(entry) {       try {
        const confirmed = await showActionDialog({           title: "Bu bağlantı kaldırılsın mı?",           message: entry.status === "accepted"             ? `${entry.username} artık arkadaş listende görünmeyecek.`             : `${entry.username} için bekleyen istek kaldırılacak.`,
          confirmText: entry.status === "accepted" ? "Arkadaşı Kaldır" : "İsteği Kaldır",           cancelText: "Vazgeç"         });         if (!confirmed) return;         await fetchPrivate("/api/friends/remove", "POST", { friendshipId: entry.friendshipId, targetUid: entry.uid });
        showToast("Sosyal Merkez", entry.status === "accepted" ? "Arkadaş listesi güncellendi." : "Bekleyen istek kaldırıldı.", "info");         if (entry.uid && state.social.unreadDirect[entry.uid]) delete state.social.unreadDirect[entry.uid];         await loadFriends();       } catch (error) {         showToast("Sosyal Merkez", error.message || "Kaldırılamadı.", "error");
      }     }      async function createInviteForFriend(entry) {       try {
        if (!ensureAuthThen("Sosyal Merkez")) return;         const socket = await ensureRealtimeConnection();         if (!socket) throw new Error("Canlı bağlantı kurulamadı.");          if (!entry?.online) throw new Error("Bu oyuncu şu anda çevrimdışı.");
         const config = getSelectedInviteConfig();         let roomId = "";          if (config.gameKey === "chess") {
          const payload = await fetchPrivate("/api/chess/create", "POST", {});           roomId = payload?.room?.id || "";         } else {           const payload = await fetchPrivate("/api/pisti-online/create-private", "POST", {             mode: config.mode,
            bet: config.bet,             roomName: `${state.userData?.username || "PlayMatrix"} Özel Oda`,             password: ""           });           roomId = payload?.room?.id || "";
        }          if (!roomId) throw new Error("Davet odası hazırlanamadı.");          state.social.pendingInviteNavigation = {
          roomId,           gameKey: config.gameKey,           gameCode: config.gameKey,           gamePath: config.gameKey === "pisti" ? "/games/pisti" : "/games/chess",           targetUid: entry.uid
        };          await emitSocketAck(socket, "game:invite_send", {           targetUid: entry.uid,           roomId,
          gameKey: config.gameKey,           gameName: config.gameName         });          showToast("Davet gönderildi", "Sunucu onayı alındı. Oyun odasına aktarılıyorsun.", "success");
      } catch (error) {         state.social.pendingInviteNavigation = null;         showToast("Davet hatası", error.message || "Davet gönderilemedi.", "error");       }     }
     function handleSocialComposerInput() {       const input = $("psChatInput");       const helpEl = $("psChatHelp");       if (!input || !helpEl || helpEl.classList.contains("is-error")) return;
      const size = (input.value || "").trim().length;       const base = state.social.activeTab === "friends" ? "" : "";       const text = size > 0 ? (base ? `${size}/280 · ${base}` : `${size}/280`) : base;       helpEl.textContent = text;       helpEl.className = "ps-chat-help";
      helpEl.style.display = text ? "block" : "none";     }      function emitSocketAck(socket, eventName, payload = {}, timeoutMs = 6000) { return new Promise((resolve, reject) => { let settled = false; const timer = window.setTimeout(() => { if (settled) return; settled = true; reject(new Error('SOCKET_ACK_TIMEOUT')); }, Math.max(1000, Number(timeoutMs) || 6000)); try { socket.emit(eventName, payload, (response = {}) => { if (settled) return; settled = true; window.clearTimeout(timer); if (response && response.ok === false) reject(new Error(response.message || response.error || 'SOCKET_REQUEST_REJECTED')); else resolve(response || { ok:true }); }); } catch (error) { if (!settled) { settled = true; window.clearTimeout(timer); reject(error); } } }); }      async function sendSocialMessage() {       try {
        if (!ensureAuthThen("Sosyal Merkez")) return;         const socket = await ensureRealtimeConnection();         if (!socket) throw new Error("Canlı bağlantı kurulamadı.");          const input = $("psChatInput");
        const message = (input?.value || "").trim();         if (!message) throw new Error("Mesaj boş bırakılamaz.");         if (message.length > 280) throw new Error("Mesaj en fazla 280 karakter olabilir.");          await primeRealtimeUX();
         if (state.social.activeTab === "friends") {           const entry = getSelectedSocialEntry();           if (!entry?.uid) throw new Error("Önce bir arkadaş seçmelisin."); 
          const clientTempId = window.crypto?.randomUUID?.() || `dm_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;           await emitSocketAck(socket, "chat:dm_send", { toUid: entry.uid, targetUid: entry.uid, message, clientTempId });           if (socket.connected) socket.emit("chat:typing", { toUid: entry.uid, isTyping: false });           clearDirectTypingState(false);           playSound("send");
          clearActiveComposerDraft();           if (input) {             input.value = "";             input.focus({ preventScroll: true });           }
          handleSocialComposerInput();           setComposerState({ visible: true, placeholder: `${entry.username || "Arkadaşın"} için mesaj yaz...`, help: "Mesaj sunucuya iletiliyor...", tone: "" });           return;         } 
        await emitSocketAck(socket, "chat:lobby_send", { message, username: state.userData?.username || auth.currentUser?.email || "Oyuncu", avatar: state.userData?.avatar || "" });         playSound("send");         clearActiveComposerDraft();         if (input) {           input.value = "";
          input.focus({ preventScroll: true });         }         handleSocialComposerInput();         setComposerState({ visible: true, placeholder: "Lobiye kısa ve saygılı bir mesaj yaz...", help: "Mesaj gönderildi. Lobi akışında görünüyor.", tone: "success" });       } catch (error) {
        const helpEl = $("psChatHelp");         if (helpEl) {           helpEl.textContent = error.message || "Mesaj gönderilemedi.";           helpEl.className = "ps-chat-help is-error";         }
      }     }      async function showActionDialog({ title, message, confirmText = "Tamam", cancelText = "Vazgeç" }) {       return new Promise((resolve) => {
        const backdrop = document.createElement("div");         backdrop.className = "pm-dialog-backdrop";          const dialog = document.createElement("div");         dialog.className = "pm-dialog";
        dialog.setAttribute("role", "dialog");         dialog.setAttribute("aria-modal", "true");          const titleEl = document.createElement("h4");         titleEl.textContent = title;
         const messageEl = document.createElement("p");         messageEl.textContent = message;          const actions = document.createElement("div");
        actions.className = "pm-dialog-actions";          const cancelBtn = document.createElement("button");         cancelBtn.className = "ghost-btn";         cancelBtn.textContent = cancelText;
         const confirmBtn = document.createElement("button");         confirmBtn.className = "btn btn-primary";         confirmBtn.textContent = confirmText; 
        const cleanup = (result) => {           backdrop.remove();           resolve(result);         }; 
        cancelBtn.addEventListener("click", () => cleanup(false));         confirmBtn.addEventListener("click", () => cleanup(true));         backdrop.addEventListener("click", (event) => {           if (event.target === backdrop) cleanup(false);         });
         actions.append(cancelBtn, confirmBtn);         dialog.append(titleEl, messageEl, actions);         backdrop.appendChild(dialog);         document.body.appendChild(backdrop);
      });     }      async function showInputDialog({ title, message, value = '', placeholder = '', inputType = 'text', confirmText = 'Devam Et', cancelText = 'Vazgeç' }) {       return new Promise((resolve) => {         const backdrop = document.createElement('div');         backdrop.className = 'pm-dialog-backdrop';         const dialog = document.createElement('div');         dialog.className = 'pm-dialog pm-input-dialog';         dialog.setAttribute('role','dialog');         dialog.setAttribute('aria-modal','true');         const titleEl = document.createElement('h4');         titleEl.textContent = title;         const messageEl = document.createElement('p');         messageEl.textContent = message;         const input = document.createElement('input');         input.className = 'field-input pm-dialog-input';         input.type = inputType;         input.placeholder = placeholder;         input.value = value;         input.autocomplete = inputType === 'email' ? 'email' : 'off';         const actions = document.createElement('div');         actions.className = 'pm-dialog-actions';         const cancelBtn = document.createElement('button');         cancelBtn.className = 'ghost-btn';         cancelBtn.type = 'button';         cancelBtn.textContent = cancelText;         const confirmBtn = document.createElement('button');         confirmBtn.className = 'btn btn-primary';         confirmBtn.type = 'button';         confirmBtn.textContent = confirmText;         const cleanup = (result) => { backdrop.remove(); resolve(result); };         cancelBtn.addEventListener('click', () => cleanup(null));         confirmBtn.addEventListener('click', () => cleanup(input.value));         input.addEventListener('keydown', (event) => { if (event.key === 'Enter') cleanup(input.value); if (event.key === 'Escape') cleanup(null); });         backdrop.addEventListener('click', (event) => { if (event.target === backdrop) cleanup(null); });         actions.append(cancelBtn, confirmBtn);         dialog.append(titleEl, messageEl, input, actions);         backdrop.appendChild(dialog);         document.body.appendChild(backdrop);         window.setTimeout(() => input.focus({ preventScroll:true }), 30);       });     }      async function confirmPotentialCrashExit() {       try {
        if (!auth.currentUser) return true;         const payload = await fetchPrivate("/api/crash/active-bets");         if (!payload?.hasActiveBet) return true;         const warning = payload.hasRiskyBet           ? "Şu an otomatik çıkış tanımı olmayan aktif bir Crash turun var. Sayfadan çıkarsan tur devam eder ve sonuç riski sana ait olur. Yine de davete gitmek istiyor musun?"
          : "Şu an aktif Crash bahsin bulunuyor. Sayfadan ayrıldığında tur arka planda devam eder. Davete geçmek istediğinden emin misin?";         return await showActionDialog({           title: "Aktif Crash Bahsi Tespit Edildi",           message: warning,           confirmText: "Yine de Devam Et",
          cancelText: "Kal"         });       } catch (_) {         return true;       }
    }      function removeInviteToast(inviteId) {       const toast = state.inviteToasts.get(inviteId);       if (toast) toast.remove();
      state.inviteToasts.delete(inviteId);     }      async function handleIncomingInviteResponse(data, response) {       try {
        if (!data?.inviteId) return;         if (response === "accepted") {           const canContinue = await confirmPotentialCrashExit();           if (!canContinue) return; 
          if (data.gameKey === "chess") {             await fetchPrivate("/api/chess/join", "POST", { roomId: data.roomId });           } else if (data.gameKey === "pisti") {             await fetchPrivate("/api/pisti-online/join", "POST", { roomId: data.roomId });           } else {
            throw new Error("Bilinmeyen davet türü.");           }         }          if (state.socket) {
          state.socket.emit("game:invite_response", {             inviteId: data.inviteId,             hostUid: data.hostUid,             roomId: data.roomId,             gameKey: data.gameKey,
            response           });         }          removeInviteToast(data.inviteId);
         if (response === "accepted") {           showToast("Oyuna Bağlanılıyor", "Oyun odasına aktarılıyorsunuz...", "success");                      setTimeout(() => {
            sessionStorage.setItem("pm_auto_join_room", data.roomId);             sessionStorage.setItem("pm_auto_join_game", data.gameKey);                          const targetHref = data.gameKey === "pisti"               ? `/games/pisti?joinRoom=${encodeURIComponent(data.roomId)}`
              : `/games/chess?joinRoom=${encodeURIComponent(data.roomId)}`;             window.location.href = targetHref;           }, 1000);          } else {
          showToast("Oyun daveti", `${data.hostName || "Arkadaşın"} tarafından gönderilen davet kapatıldı.`, "info");         }       } catch (error) {         showToast("Davet katılımı başarısız", error.message || "Odaya katılım sağlanamadı.", "error");       }
    }      function showInviteToast(data) {       if (!data?.inviteId) return;       removeInviteToast(data.inviteId);
       const { toast, body, closeBtn } = createToastBase({         tone: "info",         iconClass: "fa-gamepad",         title: "Oyun Daveti"
      });        closeBtn.addEventListener("click", () => {         handleIncomingInviteResponse(data, "declined");       });
       const message = document.createElement("div");       message.className = "toast-message";       message.textContent = `${data.hostName || "Bir oyuncu"} seni ${data.gameName || "oyuna"} çağırıyor.`;        const ttl = document.createElement('div');       ttl.className = 'invite-toast-ttl';       const expiresAt = Number(data.expiresAt || 0);       const ttlSeconds = expiresAt > Date.now() ? Math.max(0, Math.ceil((expiresAt - Date.now()) / 1000)) : 0;       ttl.textContent = ttlSeconds > 0 ? `Davet süresi: ${ttlSeconds} sn` : 'Davet süresi kontrol ediliyor'; 
      const actions = document.createElement("div");       actions.className = "invite-toast-actions";        const acceptBtn = document.createElement("button");       acceptBtn.className = "btn btn-primary";
      acceptBtn.textContent = "Katıl";       acceptBtn.addEventListener("click", () => handleIncomingInviteResponse(data, "accepted"));        const declineBtn = document.createElement("button");       declineBtn.className = "ghost-btn";
      declineBtn.textContent = "Reddet";       declineBtn.addEventListener("click", () => handleIncomingInviteResponse(data, "declined"));        actions.append(acceptBtn, declineBtn);       body.append(message, ttl, actions);
       state.inviteToasts.set(data.inviteId, toast);       appendToast(toast, 0);       window.setTimeout(() => removeInviteToast(data.inviteId), Math.max(5000, Math.min(90000, Number(data.expiresAt || 0) - Date.now())));       if (typeof playSound === "function") playSound("incoming");
    }      function registerSocketEvents(socket) {       socket.on("connect", () => {         clearRealtimeReconnectTimer();
        state.realtimeConnected = true;         state.realtimeReconnectAttempts = 0;         state.realtimeShouldBeConnected = true;         updateSocialConnectionBadge();         try { socket.emit("pm:pong", { ts: Date.now(), visible: document.visibilityState === "visible", page: location.pathname }); } catch (_) {}
        if (state.social.activeTab === "friends" && state.social.currentActiveDmUid) {           loadDirectHistoryForPeer(state.social.currentActiveDmUid, { force: true });         }       }); 
      socket.on("disconnect", (reason) => {         state.realtimeConnected = false;         clearDirectTypingState(false);         updateSocialConnectionBadge();         if (socket.__pmIntentionalClose) return;
        if (!auth.currentUser || !state.authReady) return;         if (["io client disconnect", "io server disconnect"].includes(String(reason || ""))) return;         scheduleRealtimeReconnect(reason || "disconnect");       }); 
      socket.on("connect_error", async (error) => {         state.realtimeConnected = false;         updateSocialConnectionBadge("Bağlantı yenileniyor");         const message = String(error?.message || "");         if (message.includes("BAD_TOKEN") && auth.currentUser) {
          try {             await bootstrapServerSession(auth.currentUser, { force: true, forceRefresh: true }).catch(() => null);             await getIdToken(auth.currentUser, true).catch(() => "");           } catch (_) {}         }
        scheduleRealtimeReconnect(message || "connect_error");       });        socket.on("pm:ping", (payload = {}) => {         try {
          socket.emit("pm:pong", {             ts: Number(payload?.ts || Date.now()),             visible: document.visibilityState === "visible",             page: location.pathname           });
        } catch (_) {}       });        socket.on("pm:stale", () => {         scheduleRealtimeReconnect("stale");
      });        socket.on("chat:lobby_history", (payload) => {         if (payload?.policy && typeof payload.policy === 'object') state.social.chatPolicy = mergeChatPolicy(payload.policy);         state.lobbyMessages = Array.isArray(payload?.messages) ? payload.messages.slice(-60) : [];
        if (!renderActiveLobbyStream() && state.currentSheet === "social") renderSocialHub();       });        socket.on("chat:lobby_new", (payload) => {         state.lobbyMessages = [...state.lobbyMessages.filter((item) => item.id !== payload?.id), payload].slice(-60);
        if (!appendLobbyMessageToActiveStream(payload) && state.currentSheet === "social") renderSocialHub();       });        socket.on("chat:lobby_error", (payload) => {         const helpEl = $("psChatHelp");
        if (helpEl) {           helpEl.textContent = payload?.message || "Mesaj gönderilemedi.";           helpEl.className = "ps-chat-help is-error";         }       });
       const applyIncomingDirectMessage = (payload) => {         const result = rememberDirectMessage(payload);         const peerUid = result?.peerUid;         if (!peerUid || !result.inserted) return;
         const isActiveConversation = state.currentSheet === "social"           && state.social.activeTab === "friends"           && state.social.currentActiveDmUid === peerUid           && (!isSocialMobile() || state.social.mobilePanelOpen);
         if (isActiveConversation) {           state.social.unreadDirect[peerUid] = 0;           updateTypingIndicator(peerUid, false);         } else {
          state.social.unreadDirect[peerUid] = Number(state.social.unreadDirect[peerUid] || 0) + 1;           playSound("incoming");           showToast("Yeni özel mesaj", `${payload?.username || "Arkadaşın"} sana mesaj gönderdi.`, "info");           if (document.hidden) {             showNativeRealtimeNotification({
              title: `${payload?.username || "Arkadaşın"} sana yazdı`,               body: payload?.message || payload?.text || "PlayMatrix DM bildirimi",               tag: `pm-dm-${peerUid}`,               data: { type: "dm", peerUid }             });
          }         }          if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });
      };        socket.on("chat:dm_history", (payload) => {         if (payload?.policy && typeof payload.policy === 'object') state.social.chatPolicy = mergeChatPolicy(payload.policy);         const peerUid = payload?.targetUid || payload?.peerUid || "";
        if (!peerUid) return;         const incoming = Array.isArray(payload?.messages) ? payload.messages : [];         const normalized = incoming.map((item) => normalizeDirectMessagePayload(item, peerUid)).filter(Boolean);         state.social.directMessages[peerUid] = normalized.sort((a, b) => (a.createdAt || 0) - (b.createdAt || 0)).slice(-80);         state.social.directHistoryLoadedAt[peerUid] = Date.now();
        delete state.social.directHistoryPending[peerUid];         delete state.social.directHistoryRequestedAt[peerUid];         delete state.social.directHistoryErrors[peerUid];         if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });
      });        socket.on("chat:dm_new", applyIncomingDirectMessage);        socket.on("chat:dm_success", (payload) => {
        const result = rememberDirectMessage(payload);         if (!result?.peerUid) return;         state.social.unreadDirect[result.peerUid] = 0;         const helpEl = $("psChatHelp");         if (helpEl && state.social.currentActiveDmUid === result.peerUid) {
          helpEl.textContent = "Mesaj teslim edildi.";           helpEl.className = "ps-chat-help is-success";         }         if (!renderActiveDirectStream(result.peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });
      });        socket.on("chat:dm_edited", (payload) => {         const peerUid = payload?.byUid === auth.currentUser?.uid ? state.social.currentActiveDmUid : (payload?.byUid || "");         const list = Array.isArray(state.social.directMessages[peerUid]) ? [...state.social.directMessages[peerUid]] : [];
        state.social.directMessages[peerUid] = list.map((item) => item.id === payload?.messageId ? { ...item, message: payload?.text || item.message, text: payload?.text || item.text, editedAt: payload?.editedAt || Date.now() } : item);         if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });       }); 
      socket.on("chat:dm_deleted", (payload) => {         const peerUid = payload?.byUid === auth.currentUser?.uid ? (payload?.targetUid || state.social.currentActiveDmUid) : (payload?.byUid || "");         if (!peerUid) return;         const list = Array.isArray(state.social.directMessages[peerUid]) ? [...state.social.directMessages[peerUid]] : [];         state.social.directMessages[peerUid] = list.map((item) => item.id === payload?.messageId ? { ...item, message: '', text: '', deletedAt: payload?.deletedAt || Date.now(), deleted: true, deletionMode: payload?.deletionMode || 'manual_delete', deletedLabel: payload?.deletedLabel || 'Kullanıcı tarafından silindi' } : item);
        if (!renderActiveDirectStream(peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });       });        socket.on("chat:direct_sent", (payload) => {
        const result = rememberDirectMessage(payload);         if (!renderActiveDirectStream(result?.peerUid) && state.currentSheet === "social") renderSocialHub();         else if (state.currentSheet === "social") renderSocialHub({ main: false });       }); 
      socket.on("chat:typing_status", (payload) => {         const peerUid = payload?.fromUid || payload?.sender || "";         updateTypingIndicator(peerUid, !!payload?.isTyping);       }); 
      const handleDirectError = (payload) => {         const helpEl = $("psChatHelp");         if (helpEl) {           helpEl.textContent = payload?.message || "Özel mesaj gönderilemedi.";           helpEl.className = "ps-chat-help is-error";
        }       };        socket.on("chat:direct_error", handleDirectError);       socket.on("chat:dm_error", handleDirectError);
      socket.on("chat:error", handleDirectError);        socket.on('chat:unread_count', (payload) => { state.social.unreadTotal = Math.max(0, Number(payload?.unread || 0) || 0); });        socket.on("friends:updated", () => {         if (auth.currentUser) loadFriends();       });
       socket.on("friends:presence", (payload) => {         if (!payload?.uid) return;         updateFriendPresence(payload.uid, payload.isOnline);       });
       socket.on("social:presence_update", (payload) => {         if (!payload?.uid) return;         updateFriendPresence(payload.uid, payload.presence || null);       });
       socket.on("friends:request_received", () => {         showToast("Yeni arkadaşlık isteği", "Sosyal Merkez panelinde bekleyen yeni bir istek var.", "info");         if (auth.currentUser) loadFriends();       });
       socket.on("friends:request_result", (payload) => {         showToast("Arkadaşlık güncellendi", payload?.accepted ? "Gönderdiğin istek kabul edildi." : "Gönderdiğin istek reddedildi.", payload?.accepted ? "success" : "info");         if (auth.currentUser) loadFriends();       });
       socket.on("friends:request_auto_accepted", () => {         showToast("Arkadaş eklendi", "Karşılıklı istek bulundu ve bağlantı anında kuruldu.", "success");         if (auth.currentUser) loadFriends();       });
       socket.on("game:matchmake_joined", (payload) => {         const gameType = payload?.gameType || state.social.pendingMatchmaking?.gameType || "";         if (!gameType) return;         state.social.pendingMatchmaking = {
          ...(state.social.pendingMatchmaking || {}),           gameType,           mode: payload?.mode || state.social.pendingMatchmaking?.mode || "",           bet: payload?.bet || state.social.pendingMatchmaking?.bet || 0,           queuedAt: payload?.queuedAt || Date.now()
        };         showMatchmakingToast(gameType);         updateMatchmakingToastMessage("Uygun bir rakip aranıyor... Sistem seni ilk uygun odada eşleştirecek.");       }); 
      socket.on("game:matchmake_success", (payload) => {         const gameType = payload?.gameType || state.social.pendingMatchmaking?.gameType || "";         dismissMatchmakingToast();         state.social.pendingMatchmaking = null;         showToast("Eşleşme bulundu", "Rakip bulundu. Oyun odasına aktarılıyorsun...", "success");
        window.setTimeout(() => navigateToInviteRoom({           roomId: payload?.roomId,           gameKey: gameType,           gameCode: gameType,           gamePath: payload?.gamePath || getMatchmakingPagePath(gameType)
        }), 220);       });        socket.on("game:matchmake_left", () => {         dismissMatchmakingToast();
        state.social.pendingMatchmaking = null;       });        socket.on("game:matchmake_error", (payload) => {         dismissMatchmakingToast();
        state.social.pendingMatchmaking = null;         showToast("Eşleşme hatası", payload?.message || "Hızlı eşleşme kurulamadı.", "error");       });        socket.on("game:invite_receive", (payload) => {
        if (document.hidden) {           showNativeRealtimeNotification({             title: `${payload?.hostName || "Arkadaşın"} seni davet ediyor`,             body: `${payload?.gameName || "Oyun"} daveti hazır.`,             tag: `pm-invite-${payload?.inviteId || payload?.roomId || "invite"}`,
            data: { type: "invite" }           });         }         showInviteToast(payload);       });
       socket.on("game:invite_success", (payload) => {         const pending = state.social.pendingInviteNavigation;         const targetPayload = { ...(pending || {}), ...(payload || {}) };         state.social.pendingInviteNavigation = null;
        showToast("Davet gönderildi", "Sunucu onayı alındı. Oyun odasına yönlendiriliyorsunuz...", "success");         window.setTimeout(() => navigateToInviteRoom(targetPayload), 350);       });        socket.on("game:invite_error", (payload) => {
        state.social.pendingInviteNavigation = null;         showToast("Davet hatası", payload?.message || "Davet iletilemedi.", "error");       });        socket.on("game:invite_sent", () => {});
      socket.on("game:invite_response", (payload) => {         const guestName = payload?.guestName || "Arkadaşın";         const accepted = payload?.response === "accepted";         showToast(           accepted ? "Davet kabul edildi" : "Davet reddedildi",
          accepted             ? `${guestName} davetini kabul etti.`             : `${guestName} davetini şu an kabul etmedi.`,           accepted ? "success" : "info"         );
         if (accepted) {           const pending = state.social.pendingInviteNavigation;           const targetPayload = { ...(pending || {}), ...(payload || {}) };           state.social.pendingInviteNavigation = null;
          window.setTimeout(() => navigateToInviteRoom(targetPayload), 450);         } else {           state.social.pendingInviteNavigation = null;         }       });
    }      async function ensureRealtimeConnection(forceRefresh = false, options = {}) {       if (!auth.currentUser || !state.authReady) return null;       state.realtimeShouldBeConnected = true;
      if (state.socket && state.socket.connected && !forceRefresh) return state.socket;       const ioFactory = await loadSocketClient();       const token = await getIdToken(auth.currentUser, !!forceRefresh).catch(() => getIdToken(auth.currentUser, true));       if (state.socket) disconnectRealtime({ preserveIntent: true });       clearRealtimeReconnectTimer();
      const socket = ioFactory(API_URL, {         auth: { token },         transports: ["websocket", "polling"],         reconnection: false,         timeout: 10000,
        autoConnect: true       });       socket.__pmReason = options?.reason || '';       state.socket = socket;       registerSocketEvents(socket);
      return socket;     }      async function openSocialSheet() {       if (!ensureAuthThen("Sosyal Merkez")) return;
      openSheet("social", "Sosyal Merkez", "Arkadaşlarını yönet, özel sohbetleri başlat, mesaj ara ve aktivite özetini tek merkezden gör.");       state.social.mobilePanelOpen = !isSocialMobile();       state.social.realtimeMode = "full";       state.social.realtimeError = "";       state.social.renderCache = { tabs: "", list: "", main: "" };
      renderSocialHub({ force: true });       await primeRealtimeUX();       try {         await ensureRealtimeConnection();         state.social.realtimeMode = "full";
        state.social.realtimeError = "";       } catch (error) {         state.social.realtimeMode = "limited";         state.social.realtimeError = error?.message || 'Canlı bağlantı bileşeni yüklenemedi.';         showToast('Sosyal Merkez', 'Canlı bağlantı kurulamadı. Temel sosyal alanlar sınırlı modda açılıyor.', 'warning');
      }       await Promise.allSettled([loadFriends(true), refreshSocialFeaturePack(true), loadDirectUnreadSummary()]);       renderSocialHub();       updateSocialConnectionBadge();     }
     window.PlayMatrixRealtime = {       ensureRealtimeConnection,       showInviteToast,       confirmPotentialCrashExit,
      openSocialSheet,       startMatchmaking,       cancelMatchmaking     }; 
    function captureReferralFromUrl() {       try {         const url = new URL(window.location.href);         const ref = (url.searchParams.get("ref") || "").trim().toUpperCase();         if (ref && /^[A-Z0-9]{6,12}$/.test(ref)) {
          sessionStorage.setItem(REF_STORAGE_KEY, ref);           url.searchParams.delete("ref");           window.history.replaceState({}, document.title, url.toString());         }       } catch(_) {}
    }      async function tryClaimPendingReferral() {       try {         if (!auth.currentUser) return;
        const code = (sessionStorage.getItem(REF_STORAGE_KEY) || "").trim().toUpperCase();         if (!code) return;         if (!auth.currentUser.emailVerified) return;         const out = await fetchPrivate("/api/referral/claim", "POST", { code });         if (out && out.ok) {
          sessionStorage.removeItem(REF_STORAGE_KEY);           showToast("Davet ödülü", "Davet kodu başarıyla işlendi.", "success");           await loadUserData();         }       } catch(_) {}
    }      function getRankData(){       return { name:"SEVİYE", className:"rank-level", progress: 0, nextAt: null };     }
      function formatRatingValue(value){       return Math.max(100, Math.round(Number(value) || 1000));     }
     function showToast(title, message, tone = "info"){       const icons = {         success: "fa-circle-check",         error: "fa-circle-exclamation",
        info: "fa-circle-info"       };       const { toast, body } = createToastBase({         tone,         iconClass: icons[tone] || icons.info,
        title       });       const messageEl = document.createElement("div");       messageEl.className = "toast-message";       messageEl.textContent = message;
      body.appendChild(messageEl);       appendToast(toast, 4200);     }      function playSound(kind = "tap"){
      if (!state.soundEnabled) return;       try {         const AudioContextRef = window.AudioContext || window.webkitAudioContext;         if (!AudioContextRef) return;         const audio = new AudioContextRef();
        const now = audio.currentTime;         const master = audio.createGain();         master.gain.value = 0.025;         master.connect(audio.destination); 
        const pulse = (type, freq, start, duration, volume) => {           const osc = audio.createOscillator();           const gain = audio.createGain();           osc.type = type;           osc.frequency.setValueAtTime(freq, start);
          gain.gain.setValueAtTime(0.0001, start);           gain.gain.exponentialRampToValueAtTime(volume, start + 0.01);           gain.gain.exponentialRampToValueAtTime(0.0001, start + duration);           osc.connect(gain);           gain.connect(master);
          osc.start(start);           osc.stop(start + duration + 0.02);         };          if (kind === "send") {
          pulse("sine", 660, now, 0.05, 0.55);           pulse("triangle", 880, now + 0.04, 0.06, 0.38);           return;         }         if (kind === "incoming") {
          pulse("triangle", 560, now, 0.06, 0.45);           pulse("sine", 760, now + 0.07, 0.08, 0.34);           return;         }         if (kind === "pop") {
          pulse("triangle", 520, now, 0.06, 0.42);           return;         }         pulse("sine", 420, now, 0.05, 0.32);       } catch(_) {}
    }      function updateSoundUI(){       $("soundIcon").className = `fa-solid ${state.soundEnabled ? "fa-volume-high" : "fa-volume-xmark"}`;     }
     const COMPACT_SHEETS = new Set(["auth", "forgot", "wheel", "promo", "market"]);      function openSheet(name, title, subtitle){       state.currentSheet = name;
      const sheetShell = $("sheetShell");       sheetShell.classList.add("is-open");       sheetShell.classList.toggle("is-social", name === "social");       sheetShell.classList.toggle("is-compact", COMPACT_SHEETS.has(name));       sheetShell.setAttribute("aria-hidden", "false");
      document.body.classList.add("sheet-open");       $("sheetTitle").textContent = title;       $("sheetSubtitle").textContent = subtitle;       document.querySelectorAll(".sheet-section").forEach((section) => section.classList.toggle("is-active", section.dataset.sheet === name));       $("sheetPanel").style.transform = "";
      playSound("pop");     }      window.openPlayMatrixSheet = openSheet;      function closeSheet(){       $("sheetShell").classList.remove("is-open", "is-social", "is-compact");
      $("sheetShell").setAttribute("aria-hidden", "true");       document.body.classList.remove("sheet-open");       state.currentSheet = null;       state.drag.active = false;       state.drag.deltaY = 0;
      state.social.mobilePanelOpen = !isSocialMobile();       state.social.renderCache = { tabs: "", list: "", main: "" };       document.querySelectorAll('.pm-social-layout.is-chat-active').forEach((layout) => layout.classList.remove('is-chat-active'));       $("sheetPanel").style.transform = "";     }
     function setAuthMode(mode){       state.authMode = mode;       $("authSegment").querySelectorAll("button").forEach((button) => button.classList.toggle("is-active", button.dataset.authMode === mode));       $("authFullNameGroup").classList.toggle("hidden", mode !== "register");
      $("authUsernameGroup").classList.toggle("hidden", mode !== "register");       $("authHelp").textContent = "";       $("authHelp").className = "field-help";       $("authSubmitBtn").textContent = mode === "login" ? "Giriş Yap" : "Hesap Oluştur";     }
     function ensureAuthThen(actionName){       if (auth.currentUser) return true;       setAuthMode("login");       openSheet("auth", "Hesabına giriş yap", `${actionName} için önce hesabına giriş yapmalısın.`);
      return false;     }      function openProfileSheet(){       if (!ensureAuthThen("Profil paneli")) return;
      refreshProfileSheet();       openSheet("profile", "Profil Yönetimi", "Avatar, Kullanıcı Adı Ve Hesap İşlemleri.");     }      function openWheelSheet(){
      if (!ensureAuthThen("Günlük çark")) return;       drawWheel();       refreshWheelUI();       openSheet("wheel", "Günlük Çark", "E-posta doğrulaması tamamlandıktan sonra her 24 saatte bir ücretsiz spin yapılabilir.");     }
     function openPromoSheet(){       if (!ensureAuthThen("Promosyon kodu")) return;       $("promoHelp").textContent = "";       $("promoHelp").className = "field-help";
      openSheet("promo", "Ödül Merkezi", "Promosyon kodunu gir, ödülünü güvenli şekilde aktif et.");     }      function openMarketSheet(){       if (!ensureAuthThen("Market")) return;       openSheet("market", "Market", "Avatar, çerçeve ve profil görünüm ürünlerini güvenli şekilde incele.");       loadMarketItems().catch((error) => { reportClientError("home.market", error, { source: "legacy-home.runtime.js" }); const box = $("marketItems"); if (box) box.innerHTML = '<div class="empty-state compact">Market verisi şu anda yüklenemedi.</div>'; });     }      async function loadMarketItems(){       const box = $("marketItems");       if (!box) return;       box.innerHTML = '<div class="skeleton-line"></div><div class="skeleton-line"></div>';       const payload = await fetchPrivate("/api/market/items");       const items = Array.isArray(payload?.items) ? payload.items : [];       if (!items.length) { box.innerHTML = '<div class="empty-state compact">Markette aktif ürün bulunamadı.</div>'; return; }       const fragment = document.createDocumentFragment();       items.slice(0, 24).forEach((item) => {         const card = document.createElement("article");         card.className = "market-item-card";         const title = createTextElement("strong", "market-item-title", item.title || item.name || "Market Ürünü");         const meta = createTextElement("span", "market-item-meta", `${formatNumber(item.price || item.amount || 0)} MC`);         card.append(title, meta);         fragment.appendChild(card);       });       box.replaceChildren(fragment);     }      function openSupportSheet(){       if (!ensureAuthThen("Destek talebi")) return;
      $("supportHelp").textContent = "";       $("supportHelp").className = "field-help";       openSheet("support", "Canlı Destek", "Sorununu detaylı biçimde ilet, destek kaydın eksiksiz oluşturulsun.");     } 
    function getIstanbulActivityContext() {       const formatter = new Intl.DateTimeFormat('tr-TR', {         timeZone: 'Europe/Istanbul',         year: 'numeric',         month: 'numeric'
      });       const parts = formatter.formatToParts(new Date());       const values = Object.create(null);       for (const part of parts) {         if (part.type !== 'literal') values[part.type] = part.value;
      }       const year = Number(values.year || new Date().getUTCFullYear());       const month = Number(values.month || (new Date().getUTCMonth() + 1));       const nextMonth = month === 12 ? 1 : month + 1;       const nextYear = month === 12 ? year + 1 : year;
      const monthNames = ['Ocak', 'Şubat', 'Mart', 'Nisan', 'Mayıs', 'Haziran', 'Temmuz', 'Ağustos', 'Eylül', 'Ekim', 'Kasım', 'Aralık'];       return {         periodKey: `${year}-${String(month).padStart(2, '0')}`,         nextResetLabel: `01 ${monthNames[nextMonth - 1]} ${nextYear} 00:00`       };
    }      function setOverviewText(id, value) {       const element = $(id);       if (element) element.textContent = String(value ?? '');
    }      function getRewardPolicy(data = {}) {       const systemOverview = data?.systemOverview && typeof data.systemOverview === 'object' ? data.systemOverview : null;       if (data?.rewardPolicy && typeof data.rewardPolicy === 'object') return data.rewardPolicy;
      if (systemOverview?.rewardPolicy && typeof systemOverview.rewardPolicy === 'object') return systemOverview.rewardPolicy;       return null;     }      function formatRewardAmountLabel(amount = 0, suffix = 'MC') {
      return `${formatNumber(Number(amount) || 0)} ${suffix}`;     }      function composeHomepageOverviewModel(data = {}) {
      const safeData = normalizeApiUser(data || {});
      const rewardPolicy = getRewardPolicy(safeData) || {};
      const systemOverview = safeData?.systemOverview && typeof safeData.systemOverview === "object" ? safeData.systemOverview : null;
      const chatSummary = systemOverview?.chatPolicy?.summaryLabel || getChatPolicySummary(safeData);
      const activityLadder = Array.isArray(rewardPolicy?.monthlyActive?.ladder) ? rewardPolicy.monthlyActive.ladder : [50000, 20000, 10000, 5000, 2500];
      const activityLines = activityLadder.length
        ? activityLadder.map((amount, index) => `${index + 1}. Oyuncuya <b>${formatRewardAmountLabel(amount)}</b>`).join("<br>")
        : "İlk 5 oyuncu aylık aktiflik ödülü kazanır.";
      const signupLabel = rewardPolicy?.signupLabel || formatRewardAmountLabel(50000);
      const emailLabel = rewardPolicy?.emailVerifyLabel || formatRewardAmountLabel(100000);
      return {
        signupTitle: "Yeni Üyelik Bonusu",
        signupAmountHtml: `<b>${escapeHtml(signupLabel)}</b> Anında`,
        signupMeta: "Hesabın Açılır Açılmaz Tanımlanır.",
        emailTitle: "E-posta Onay Ödülü",
        emailAmountHtml: `<b>${escapeHtml(emailLabel)}</b> Anında`,
        emailMeta: "Onay Sonrası Çark ve Promo Aktif Olur.",
        activityTitle: "Aylık Aktiflik Ödülleri",
        activityValueHtml: activityLines,
        activityMeta: "Her ay sıfırlanır ve otomatik hesaplara yatar.",
        heroRewardBadge: rewardPolicy?.rewardFlowBadge || rewardPolicy?.registrationFlowLabel || "Kayıt + Doğrulama",
        heroRetentionBadge: chatSummary,
        statsUpdatedLabel: safeData?.lastSeen || safeData?.lastActiveAt ? new Date(Number(safeData.lastSeen || safeData.lastActiveAt)).toLocaleString("tr-TR") : "Canlı veri bekleniyor"
      };
    }

    function setHomeWidgetStatus(id, stateName, message) {
      const status = $(id);
      const normalized = String(stateName || "idle");
      if (window.PMHomeWidgetContract?.setWidgetState) window.PMHomeWidgetContract.setWidgetState(status, normalized, message || "");
      else if (status) { status.dataset.widgetState = normalized; status.textContent = message || ""; }
      return status;
    }

    function buildHomeStatsModel(data = {}) {
      const normalized = normalizeApiUser(data || {});
      const progression = getProgressionData(normalized);
      const stats = normalized.statistics && typeof normalized.statistics === "object" ? normalized.statistics : {};
      const leaderboardLevelItems = Array.isArray(currentLeaderboardData?.tabs?.level?.items) ? currentLeaderboardData.tabs.level.items.length : 0;
      const leaderboardActivityItems = Array.isArray(currentLeaderboardData?.tabs?.activity?.items) ? currentLeaderboardData.tabs.activity.items.length : 0;
      const monthlyActivity = Number(normalized.monthlyActiveScore ?? stats.monthlyActiveScore ?? progression.monthlyActivity ?? 0) || 0;
      const totalRounds = Number(normalized.totalRounds ?? stats.totalRounds ?? 0) || 0;
      const totalWins = Number(normalized.totalWins ?? stats.totalWins ?? normalized.matchWins ?? normalized.chessWins ?? 0) || 0;
      const winRate = Number(normalized.winRatePct ?? stats.winRatePct ?? stats.winRate ?? 0) || 0;
      return [
        { key: "level", label: "Hesap Seviyesi", value: String(getUserLevel(normalized)), hint: "Tek progression modelinden" },
        { key: "balance", label: "Bakiye", value: `${formatNumber(Number(normalized.balance || stats.balance || 0) || 0)} MC`, hint: "Canlı hesap bakiyesi" },
        { key: "activity", label: "Aylık Aktiflik", value: formatNumber(monthlyActivity), hint: "Aylık ödül sıralaması" },
        { key: "rounds", label: "Toplam Oyun", value: formatNumber(totalRounds), hint: `${formatNumber(totalWins)} galibiyet · %${Number(winRate).toFixed(1)} başarı` },
        { key: "leaderboard", label: "Liderlik Verisi", value: formatNumber(leaderboardLevelItems + leaderboardActivityItems), hint: "Seviye + aktiflik listesi" },
        { key: "xp", label: "Hesap XP", value: formatNumber(getUserAccountXp(normalized)), hint: "v7 seviye eğrisi" }
      ];
    }

    function renderHomeStats(data = state.userData, options = {}) {
      const grid = $("homeStatsGrid");
      if (!grid) return;
      const statusText = $("homeStatsStatus");
      const isLoading = options.state === "loading";
      grid.setAttribute("aria-busy", isLoading ? "true" : "false");
      grid.classList.toggle("is-loading", isLoading);
      if (isLoading) {
        const fragment = document.createDocumentFragment();
        Array.from({ length: 4 }).forEach(() => {
          const card = document.createElement("article");
          card.className = "home-stat-card is-skeleton";
          card.append(createTextElement("span", "", ""), createTextElement("strong", "", ""), createTextElement("small", "", ""));
          fragment.appendChild(card);
        });
        grid.replaceChildren(fragment);
        setHomeWidgetStatus("homeStatsStatus", "loading", "Veriler hazırlanıyor");
        return;
      }
      const model = buildHomeStatsModel(data || {});
      if (!model.length) {
        grid.replaceChildren(createTextElement("div", "home-widget-empty", "İstatistik verisi bulunamadı."));
        setHomeWidgetStatus("homeStatsStatus", "empty", "Veri yok");
        return;
      }
      const fragment = document.createDocumentFragment();
      model.forEach((item) => {
        const card = document.createElement("article");
        card.className = "home-stat-card";
        card.dataset.statKey = item.key;
        card.append(createTextElement("span", "", item.label), createTextElement("strong", "", item.value), createTextElement("small", "", item.hint));
        fragment.appendChild(card);
      });
      grid.replaceChildren(fragment);
      setHomeWidgetStatus("homeStatsStatus", "ready", auth.currentUser ? "Canlı hesap verisi" : "Misafir görünümü");
      if (statusText) statusText.title = options.updatedLabel || composeHomepageOverviewModel(data || {}).statsUpdatedLabel;
    }
function buildHeroPromoSlides(overview = {}) {
      const ladder = String(overview.activityValueHtml || '').split(/<br\s*\/?\s*>/i).filter(Boolean).map((line) => stripHtml(line));       return [         {           key: 'signup',           theme: 'violet',
          icon: 'fa-plus',           chip: 'Hemen Tanımlanır',           eyebrow: overview.signupTitle || 'Yeni Üyelik Bonusu',           titleHtml: overview.signupAmountHtml || '<b>50.000 MC</b> Anında',           meta: overview.signupMeta || 'Hesabın Açılır Açılmaz Tanımlanır.'
        },         {           key: 'email',           theme: 'cyan',           icon: 'fa-envelope-open-text',
          chip: 'Doğrulama Sonrası',           eyebrow: overview.emailTitle || 'E-posta Onay Ödülü',           titleHtml: overview.emailAmountHtml || '<b>100.000 MC</b> Anında',           meta: overview.emailMeta || 'Onay Sonrası Çark ve Promo Aktif Olur.'         },
        {           key: 'activity',           theme: 'gold',           icon: 'fa-trophy',           chip: 'Her Ay Otomatik',
          eyebrow: overview.activityTitle || 'Aylık Aktiflik Ödülleri',           titleHtml: '<b>İlk 5 Oyuncuya</b> MC Dağılımı',           meta: overview.activityMeta || 'Her ay sıfırlanır ve otomatik hesaplara yatar.',           ladder         }
      ];     }      function stopHeroCarouselAutoplay() {       if (heroCarouselState.timer) {
        clearTimeout(heroCarouselState.timer);         heroCarouselState.timer = 0;       }     } 
    function scheduleHeroCarouselAutoplay() {       stopHeroCarouselAutoplay();       if (document.hidden) return;       if ((heroCarouselState.slides || []).length <= 1) return;       heroCarouselState.timer = window.setTimeout(() => {
        setHeroCarouselIndex(heroCarouselState.index + 1, { resetTimer: true });       }, heroCarouselState.autoplayMs);     }      function recalcHeroCarouselMetrics() {
      const track = $('heroPromoTrack');       if (!track) return;       const firstSlide = track.querySelector('.hero-slide');       if (!firstSlide) {         heroCarouselState.slideSpan = 0;
        return;       }       const trackStyles = window.getComputedStyle(track);       const gap = parseFloat(trackStyles.columnGap || trackStyles.gap || '14') || 14;       heroCarouselState.slideSpan = firstSlide.getBoundingClientRect().width + gap;
    }      function applyHeroCarouselPosition(offset = null, animate = true) {       const track = $('heroPromoTrack');       if (!track) return;
      recalcHeroCarouselMetrics();       const targetOffset = Number.isFinite(offset) ? offset : heroCarouselState.index * heroCarouselState.slideSpan;       track.classList.toggle('is-dragging', !animate);       track.style.transform = `translate3d(${-targetOffset}px, 0, 0)`;     }
     function updateHeroCarouselDots() {       const dots = document.querySelectorAll('#heroPromoDots .hero-slider-dot');       dots.forEach((dot, index) => {         const isActive = index === heroCarouselState.index;
        dot.classList.toggle('is-active', isActive);         dot.setAttribute('aria-current', isActive ? 'true' : 'false');       });     } 
    function setHeroCarouselIndex(nextIndex = 0, { animate = true, resetTimer = false } = {}) {       const count = (heroCarouselState.slides || []).length;       if (!count) return;       const normalized = ((Number(nextIndex) || 0) % count + count) % count;       heroCarouselState.index = normalized;
      applyHeroCarouselPosition(null, animate);       updateHeroCarouselDots();       if (resetTimer) scheduleHeroCarouselAutoplay();     } 
    function bindHeroCarousel() {       if (heroCarouselState.bound) return;       const viewport = $('heroPromoViewport');       const dots = $('heroPromoDots');       if (!viewport || !dots) return;
      heroCarouselState.bound = true;        let touchStartX = 0;       let touchLastX = 0; 
      viewport.addEventListener('touchstart', (event) => {         if (!(heroCarouselState.slides || []).length) return;         const touch = event.touches?.[0];         if (!touch) return;         stopHeroCarouselAutoplay();
        heroCarouselState.touchActive = true;         touchStartX = touch.clientX;         touchLastX = touch.clientX;         recalcHeroCarouselMetrics();         heroCarouselState.startOffset = heroCarouselState.index * heroCarouselState.slideSpan;
      }, { passive: true });        viewport.addEventListener('touchmove', (event) => {         if (!heroCarouselState.touchActive) return;         const touch = event.touches?.[0];
        if (!touch) return;         touchLastX = touch.clientX;         const deltaX = touch.clientX - touchStartX;         heroCarouselState.dragOffset = deltaX;         applyHeroCarouselPosition(heroCarouselState.startOffset - deltaX, false);
      }, { passive: true });        const finishTouch = () => {         if (!heroCarouselState.touchActive) return;         const threshold = Math.max(48, (heroCarouselState.slideSpan || 0) * 0.16);
        const deltaX = touchLastX - touchStartX;         heroCarouselState.touchActive = false;         heroCarouselState.dragOffset = 0;         if (Math.abs(deltaX) > threshold) {           setHeroCarouselIndex(heroCarouselState.index + (deltaX < 0 ? 1 : -1), { animate: true, resetTimer: true });
        } else {           applyHeroCarouselPosition(null, true);           scheduleHeroCarouselAutoplay();         }       };
       viewport.addEventListener('touchend', finishTouch, { passive: true });       viewport.addEventListener('touchcancel', finishTouch, { passive: true });        dots.addEventListener('click', (event) => {
        const button = event.target.closest('.hero-slider-dot');         if (!button) return;         const nextIndex = Number(button.dataset.slideIndex || 0) || 0;         setHeroCarouselIndex(nextIndex, { animate: true, resetTimer: true });       });
       window.addEventListener('resize', () => {         recalcHeroCarouselMetrics();         applyHeroCarouselPosition(null, true);       }, { passive: true });
       document.addEventListener('visibilitychange', () => {         if (document.hidden) stopHeroCarouselAutoplay();         else scheduleHeroCarouselAutoplay();       });
    }      function renderHeroPromoCarousel(overview = {}) {       const track = $('heroPromoTrack');       const dots = $('heroPromoDots');
      if (!track || !dots) return;       const slides = buildHeroPromoSlides(overview);       heroCarouselState.slides = slides;       const slideFragment = document.createDocumentFragment();       slides.forEach((slide) => {
        const article = document.createElement('article');         article.className = 'hero-slide';         article.dataset.theme = String(slide.theme || 'violet');         const glow = document.createElement('div');         glow.className = 'hero-slide-glow';
        const content = document.createElement('div');         content.className = 'hero-slide-content';         const top = document.createElement('div');         top.className = 'hero-slide-top';         const icon = document.createElement('div');
        icon.className = 'hero-slide-icon';         icon.appendChild(createFaIcon(slide.icon || 'fa-gift'));         const chip = createTextElement('span', 'hero-slide-chip', slide.chip || '');         top.append(icon, chip);         const copy = document.createElement('div');
        copy.className = 'hero-slide-copy';         copy.appendChild(createTextElement('div', 'hero-slide-eyebrow', slide.eyebrow || ''));         const title = document.createElement('h3');         title.className = 'hero-slide-title';         appendSafeInlineMarkup(title, slide.titleHtml || slide.title || '');
        copy.appendChild(title);         copy.appendChild(createTextElement('div', 'hero-slide-meta', slide.meta || ''));         if (Array.isArray(slide.ladder) && slide.ladder.length) {           const ladder = document.createElement('div');           ladder.className = 'hero-slide-ladder';
          slide.ladder.forEach((line) => {             const clean = stripHtml(line);             const parts = clean.split('Oyuncuya');             const rank = (parts[0] || '').trim();             const amount = (parts[1] || '').trim();
            const row = document.createElement('div');             row.className = 'hero-slide-row';             row.append(createTextElement('span', 'hero-slide-rank', rank ? rank + ' Oyuncu' : 'Oyuncu'), createTextElement('span', 'hero-slide-value', amount));             ladder.appendChild(row);           });
          copy.appendChild(ladder);         }         content.append(top, copy);         article.append(glow, content);         slideFragment.appendChild(article);
      });       track.replaceChildren(slideFragment);       const dotFragment = document.createDocumentFragment();       slides.forEach((slide, index) => {         const button = document.createElement('button');
        button.className = 'hero-slider-dot' + (index === heroCarouselState.index ? ' is-active' : '');         button.type = 'button';         button.setAttribute('aria-label', `${stripHtml(slide.eyebrow || 'Promosyon')} slaytına git`);         button.setAttribute('aria-current', index === heroCarouselState.index ? 'true' : 'false');         button.dataset.slideIndex = String(index);
        dotFragment.appendChild(button);       });       dots.replaceChildren(dotFragment);       bindHeroCarousel();       const count = slides.length || 1;
      heroCarouselState.index = Math.min(heroCarouselState.index, count - 1);       requestAnimationFrame(() => {         recalcHeroCarouselMetrics();         applyHeroCarouselPosition(null, true);         updateHeroCarouselDots();
        scheduleHeroCarouselAutoplay();       });     }     function updateSystemOverview(){
      const data = state.userData = normalizeApiUser(state.userData || {});
      const overview = composeHomepageOverviewModel(data);
      setOverviewText("heroRewardBadge", overview.heroRewardBadge);
      setOverviewText("heroRetentionBadge", overview.heroRetentionBadge);
      renderHeroPromoCarousel(overview);
      renderHomeStats(data, { updatedLabel: overview.statsUpdatedLabel });
    }
    async function openInviteSheet(){       if (!ensureAuthThen("Davet sistemi")) return;       $("inviteHelp").textContent = "";
      $("inviteHelp").className = "field-help";       openSheet("invite", "Davet Et ve Kazan", "Davet kodunu ve bağlantını üret, tek dokunuşla paylaş.");       await loadInviteLink();     } 
         function renderProfileStats(data = {}) {       const grid = $('profileStatsGrid');       const updated = $('profileStatsUpdated');       if (!grid) return;
      const normalized = normalizeApiUser(data || {});       const progression = getProgressionData(normalized);       const stats = normalized.statistics && typeof normalized.statistics === 'object'         ? normalized.statistics         : (normalized.stats && typeof normalized.stats === 'object' ? normalized.stats : {});
      const rows = [         ['Seviye', String(getUserLevel(normalized))],         ['Hesap XP', formatNumber(getUserAccountXp(normalized))],         ['Aylık Aktiflik', formatNumber(Number(normalized.monthlyActiveScore ?? stats.monthlyActiveScore ?? progression.monthlyActivity ?? 0) || 0)],         ['Toplam Raund', formatNumber(Number(normalized.totalRounds ?? stats.totalRounds ?? 0) || 0)],
        ['Galibiyet', formatNumber(Number(normalized.totalWins ?? stats.totalWins ?? normalized.matchWins ?? normalized.chessWins ?? 0) || 0)],         ['Okunmamış Mesaj', formatNumber(Number(normalized.unread_messages ?? normalized.unreadMessages ?? stats.unreadMessages ?? 0) || 0)]       ];       const fragment = document.createDocumentFragment();       rows.forEach(([label, value]) => {
        const card = document.createElement('div');         card.className = 'profile-stat-card';         card.append(createTextElement('span', '', label), createTextElement('strong', '', value));         fragment.appendChild(card);       });
      grid.replaceChildren(fragment);       if (updated) {         const ts = Number(normalized.lastSeen || normalized.lastActiveAt || Date.now()) || Date.now();         updated.textContent = 'Backend verisi: ' + new Date(ts).toLocaleString('tr-TR');       }
    }     function updateUserShell(){       const user = auth.currentUser;       const data = state.userData = normalizeApiUser(state.userData || {});       const progression = getProgressionData(data);
      const accountLevel = clamp(Number(data.level ?? progression.level ?? data.accountLevel ?? progression.accountLevel ?? getUserLevel(data)) || 1, 1, 100);       const accountProgressPct = clamp(Number(data.progressPercent ?? progression.progressPercent ?? progression.accountLevelProgressPct ?? 0), 0, 100);       const monthlyActivity = Number(data.monthlyActiveScore ?? progression.monthlyActivity ?? 0);       const avatar = safeUrl(data.avatar || state.selectedAvatar || AVATARS[0]);       const username = data.username || user?.email || "Oyuncu";
      const displayFrame = getDisplayFrameLevel();        $("loginBtn").style.display = user ? "none" : "inline-flex";       $("registerBtn").style.display = user ? "none" : "inline-flex";       $("topUser").classList.toggle("is-visible", !!user);
       if (user) {         $("headerBalance").textContent = formatNumber(data.balance || 0);         mountTopbarAvatar(avatar, displayFrame);         $("headerUsername").textContent = username;
        $("headerRankText").textContent = `Hesap Seviyesi ${accountLevel}`;         $("ddUsername").textContent = username;         $("ddLevel").textContent = `Seviye ${accountLevel}`;         $("ddNext").textContent = accountLevel < 100 ? `Seviye ${accountLevel + 1}` : 'MAX';         $("ddPct").textContent = `%${accountProgressPct.toFixed(1)}`;
        $("ddBar").style.width = `${accountProgressPct}%`;       }        const heroMeta = user         ? `Bakiye: ${formatNumber(data.balance || 0)} MC · ${user.emailVerified ? "E-posta doğrulandı" : "E-posta doğrulanmadı"} · Aylık Aktiflik: ${formatNumber(monthlyActivity)} · Hesap Seviyesi: ${accountLevel}`
        : "Kayıt ol, bakiyeni ve hesap seviyeni canlı takip et.";        mountAvatarShell("heroProfileAvatarShell", avatar, displayFrame, 56, "pm-avatar--profile");       $("heroProfileName").textContent = user ? username : "Misafir Modu";       $("heroProfileMeta").textContent = heroMeta;
      if ($("heroProgressText")) $("heroProgressText").textContent = accountLevel < 100 ? `%${accountProgressPct.toFixed(1)}` : "MAX";       if ($("heroProgressFill")) $("heroProgressFill").style.width = `${accountProgressPct}%`;       const accountLevelEl = document.getElementById('ui-account-level');       const monthlyActivityEl = document.getElementById('ui-monthly-activity');       if (accountLevelEl) accountLevelEl.textContent = String(accountLevel);
      if (monthlyActivityEl) monthlyActivityEl.textContent = formatNumber(monthlyActivity);       renderProfileStats(data);        const heroAvatarShell = $("heroProfileAvatarShell");       if (heroAvatarShell) {
        heroAvatarShell.style.cursor = user ? "pointer" : "default";         if (!heroAvatarShell.dataset.statsBound) {           heroAvatarShell.dataset.statsBound = 'true';           heroAvatarShell.addEventListener('click', () => {             const currentUser = auth.currentUser;             if (currentUser) window.showPlayerStats?.(currentUser.uid);           });         }       }        updateAppearanceSummary();
      updateSystemOverview();     }      function updateAppearanceSummary(){       const avatar = safeUrl(state.selectedAvatar || state.userData?.avatar || AVATARS[0]);
      const currentFrame = getDisplayFrameLevel();       const username = state.userData?.username || auth.currentUser?.email || 'Oyuncu';       const titleEl = $("appearanceSummaryTitle");       const textEl = $("appearanceSummaryText"); 
      mountAvatarShell("appearancePreviewShell", avatar, currentFrame, 76, "pm-avatar--picker");        if (titleEl) titleEl.textContent = `${username} · ${getFrameLevelLabel(currentFrame)}`;       if (textEl) textEl.textContent = 'Avatar ve çerçeve profil, sohbet, liderlik, sosyal merkez ve oyun yüzeylerinde senkron görünür.';     }
     function refreshCurrentUserAvatarSurfaces(){       const avatar = safeUrl(state.selectedAvatar || state.userData?.avatar || AVATARS[0]);       const displayFrame = getDisplayFrameLevel();       mountAvatarShell("profileSheetAvatarShell", avatar, displayFrame, 56, "pm-avatar--profile");
      mountAvatarShell("heroProfileAvatarShell", avatar, displayFrame, 56, "pm-avatar--profile");       mountAvatarShell("appearancePreviewShell", avatar, displayFrame, 76, "pm-avatar--picker");       mountTopbarAvatar(avatar, displayFrame);     } 
    async function persistSelectedFramePreference(frameLevel){       const nextFrame = normalizeFrameLevel(frameLevel);       syncSelectedFrameState(nextFrame);       if (!auth.currentUser) return nextFrame;       const response = await syncProfileUpdateWithRetry({ selectedFrame: nextFrame }, { attempts: 2 });
      if (response?.user) applyProfilePayload(response);       syncSelectedFrameState(response?.selectedFrame ?? nextFrame);       return getSelectedFrameLevel();     }      const avatarPickerController = createAvatarPicker({
      documentRef: document,       categories: AVATAR_CATEGORIES,       normalizeAvatarUrl,       defaultAvatar: DEFAULT_AVATAR,       fallbackAvatar: AVATAR_FALLBACK,
      getSelectedAvatar: () => state.selectedAvatar || state.userData?.avatar || DEFAULT_AVATAR,       openModal: openMatrixModal,       closeModal: closeMatrixModal,       onSelect: handleAvatarCatalogSelection,     });
     const framePickerController = createFramePicker({       documentRef: document,       fallbackAvatar: AVATAR_FALLBACK,       getSelectedFrame: () => getSelectedFrameLevel(),
      getCurrentLevel: () => getCurrentAccountLevel(),       getCurrentAvatar: () => state.selectedAvatar || state.userData?.avatar || DEFAULT_AVATAR,       openModal: openMatrixModal,       closeModal: closeMatrixModal,       onSelect: selectFrame,
    });      function findAvatarCatalogItem(categoryId, avatarId){       const category = AVATAR_CATEGORIES.find((entry) => entry.id === categoryId);       if (!category) return null;
      return category.items.find((item) => item.id === avatarId) || null;     }      function syncAvatarEverywhere(src){       const normalized = normalizeAvatarUrl(src || DEFAULT_AVATAR, DEFAULT_AVATAR);
      state.selectedAvatar = normalized;       if (state.userData) state.userData.avatar = normalized;       try { localStorage.setItem('pm_selected_avatar', normalized); } catch (_) {}       refreshCurrentUserAvatarSurfaces();       updateAppearanceSummary();
      avatarPickerController.updateActiveSelection();       if (typeof framePickerController !== 'undefined') framePickerController.updateActiveSelection();       return normalized;     } 
    async function persistSelectedAvatar(src){       const normalized = normalizeAvatarUrl(src || DEFAULT_AVATAR, DEFAULT_AVATAR);       if (!isCatalogAvatarUrl(normalized)) throw new Error('Geçersiz avatar seçimi.');       if (!auth.currentUser) return normalized;       const response = await syncProfileUpdateWithRetry({ avatar: normalized }, { attempts: 2 });
      if (!response?.ok) throw new Error(response?.error || response?.message || 'Avatar sunucuya kaydedilemedi.');       if (response?.user) applyProfilePayload(response);       else if (state.userData) state.userData.avatar = normalized;       return normalized;     } 
    async function handleAvatarCatalogSelection({ categoryId, avatarId, item, src }){       return selectAvatarFromCatalog(categoryId, avatarId, item || { src });     }      async function selectAvatarFromCatalog(categoryId, avatarId, item = null){
      const catalogItem = item?.src ? item : findAvatarCatalogItem(categoryId, avatarId);       if (!catalogItem?.src) return null;        const previousAvatar = state.selectedAvatar || state.userData?.avatar || DEFAULT_AVATAR;       const normalized = syncAvatarEverywhere(catalogItem.src);
       try {         await persistSelectedAvatar(normalized);         showToast('Avatar kaydedildi', 'Profil fotoğrafın güncellendi.', 'success');         return normalized;
      } catch (error) {         syncAvatarEverywhere(previousAvatar);         showToast('Avatar kaydedilemedi', error.message || 'Seçili avatar sunucuya kaydedilemedi.', 'error');         return null;       }
    }      function selectAvatar(src){       return syncAvatarEverywhere(src || DEFAULT_AVATAR);     }
     async function selectFrame(level) {       const nextFrame = normalizeFrameLevel(level);       const unlockedMax = getCurrentAccountLevel();       
      if (nextFrame > unlockedMax) {         showMatrixModal('Şuanda Aktif Değil', `Bu çerçeveyi kullanmak için en az Seviye ${nextFrame} olmalısın. Mevcut seviyen: ${getUserLevel(state.userData)}.`, 'warning');         return;       } 
      const previousFrame = getSelectedFrameLevel();       syncSelectedFrameState(nextFrame);        renderFrameOptions();       updateAppearanceSummary();
      refreshCurrentUserAvatarSurfaces();        if (!auth.currentUser) return;        try {
        await persistSelectedFramePreference(nextFrame);       } catch (error) {         syncSelectedFrameState(previousFrame);         renderFrameOptions();         updateAppearanceSummary();
        refreshCurrentUserAvatarSurfaces();         showToast('Çerçeve kaydedilemedi', error.message || 'Seçili çerçeve sunucuya kaydedilemedi.', 'error');       }     } 
    function renderAvatarCategories(){       return avatarPickerController.renderAvatarCategories();     }      function openAvatarPicker(){
      return avatarPickerController.openAvatarPicker();     }      function closeAvatarPicker(){       return avatarPickerController.closeAvatarPicker();
    }      function renderFrameOptions(){       return framePickerController.renderFrameOptions();     }
     function openFramePicker(){       return framePickerController.openFramePicker();     } 
    function closeFramePicker(){       return framePickerController.closeFramePicker();     }      function renderAvatarSelectionBox(){
      renderFrameOptions();       return renderAvatarCategories();     }      function openAvatarSelectionModal(){
      return openAvatarPicker();     }      function openFrameSelectionModal(){       return openFramePicker();
    }      function switchAvatarTab(){       return openAvatarPicker();     }
     window.closeMatrixModal = closeMatrixModal;     window.openAvatarPicker = openAvatarPicker;     window.closeAvatarPicker = closeAvatarPicker;     window.openFramePicker = openFramePicker;
    window.closeFramePicker = closeFramePicker;     window.renderFrameOptions = renderFrameOptions;     window.renderAvatarCategories = renderAvatarCategories;     window.selectAvatarFromCatalog = selectAvatarFromCatalog;     window.persistSelectedAvatar = persistSelectedAvatar;
    window.syncAvatarEverywhere = syncAvatarEverywhere;     window.openAvatarSelectionModal = openAvatarSelectionModal;     window.openFrameSelectionModal = openFrameSelectionModal;     window.switchAvatarTab = switchAvatarTab;      function syncVerifyButtonState(){       const button = $("verifyEmailBtn");
      if (!button) return;       const verified = !!auth.currentUser?.emailVerified;       button.textContent = verified ? "E-posta Adresini Güncelle" : "Doğrulama Maili Gönder";     } 
    function refreshProfileSheet(){       const user = auth.currentUser;       const data = state.userData = normalizeApiUser(state.userData || {});       const progression = getProgressionData(data);       const currentLevel = getUserLevel(data);
      const progress = clamp(Number(progression.accountLevelProgressPct ?? 0), 0, 100);       const monthlyActivity = Number(data.monthlyActiveScore ?? progression.monthlyActivity ?? 0);       const fullNameLocked = !!data.fullNameLocked || !!(data.fullName && String(data.fullName).trim());        mountAvatarShell("profileSheetAvatarShell", safeUrl(data.avatar || AVATARS[0]), getDisplayFrameLevel(), 56, "pm-avatar--profile");
      $("profileSheetName").textContent = data.username || user?.email || "Oyuncu";       $("profileSheetMeta").textContent = `Bakiye: ${formatNumber(data.balance || 0)} MC · ${user?.emailVerified ? "E-posta doğrulandı" : "E-posta doğrulanmadı"} · Aylık Aktiflik: ${formatNumber(monthlyActivity)} · Hesap Seviyesi: ${currentLevel}`;       $("profileProgressText").textContent = currentLevel < 100 ? `%${progress.toFixed(1)}` : "MAX";       $("profileProgressFill").style.width = `${progress}%`;       $("profileFullName").value = data.fullName || "";
      $("profileFullName").disabled = fullNameLocked;       $("profileFullName").classList.toggle("is-locked", fullNameLocked);       $("profileFullNameHelp").textContent = fullNameLocked         ? "Ad Soyad Alanı İlk Kayıt Sonrası Kilitlendi Ve Değiştirilemez."         : "İlk kayıt tamamlandıktan sonra bu alan güvenlik nedeniyle kilitlenir.";
      $("profileUsername").value = data.username || "";       $("profileEmail").value = data.email || user?.email || "";       const usernameChangeLimit = Number(data.usernameChangeLimit || 3);       const usernameRemaining = Math.max(0, Number(data.usernameChangeRemaining ?? (usernameChangeLimit - Number(data.userChangeCount || 0))));       const usernameLocked = usernameRemaining <= 0;
      $("profileUsername").disabled = usernameLocked;       $("profileUsername").classList.toggle("is-locked", usernameLocked);       $("usernameLimitHelp").textContent = usernameLocked         ? "Kullanıcı adı değiştirme hakkın doldu. Bu alan artık kilitli."         : `Kullanıcı Adı Toplam 3 Kez Değiştirilebilir. Kalan Hakkın: ${usernameRemaining}.`;
      $("usernameLimitHelp").className = `field-help ${usernameLocked ? "is-error" : ""}`;       $("usernameHelp").textContent = "";       $("usernameHelp").className = "field-help";       state.selectedAvatar = safeUrl(data.avatar || AVATARS[0]);       syncSelectedFrameState(data.selectedFrame ?? 0);
      renderAvatarSelectionBox();       updateAppearanceSummary();       renderProfileStats(data);       syncVerifyButtonState(); 
      const profileSheetAvatarShell = $("profileSheetAvatarShell");       if (profileSheetAvatarShell) {         profileSheetAvatarShell.style.cursor = user ? "pointer" : "default";         if (!profileSheetAvatarShell.dataset.statsBound) {           profileSheetAvatarShell.dataset.statsBound = 'true';           profileSheetAvatarShell.addEventListener('click', () => {             const currentUser = auth.currentUser;             if (currentUser) window.showPlayerStats?.(currentUser.uid);           });         }       }
    }       async function maybeShowPendingReward(){
      return;
    }      async function loadUserData(){       if (!auth.currentUser) {         state.userData = null;
                state.monthlyRewardShownKey = "";         updateUserShell();         return;       }       try {
        await refreshAuthVerificationState(true);         const payload = await fetchPrivate("/api/me", "GET", null, { forceRefresh: true });         applyProfilePayload(payload);         refreshProfileSheet();
        syncVerifyButtonState();         await maybeShowPendingReward();       } catch (error) {
        const message = String(error?.message || "Veri senkron hatası oluştu.");         if (/Oturum|zaman aşım|Geçersiz token|Geçersiz oturum/i.test(message)) {           notifySessionIssueOnce("Veri senkron hatası", message, "error");           return;         }
        showToast("Veri senkron hatası", message, "error");       }     }      function renderGames(){
      const grid = $('gamesGrid');       const empty = $('gamesEmpty');       const searchInput = $('gameSearch');       const metricGames = $('metricGamesCount');       if (!grid || !empty || !searchInput || !metricGames) return;
       const query = (searchInput.value || "").trim().toLowerCase();       const filter = state.activeFilter || 'all';       const user = auth.currentUser;       const games = GAMES.filter((game) => {
        const matchesSearch = !query || `${game.name} ${game.desc} ${game.keywords}`.toLowerCase().includes(query);         if (!matchesSearch) return false;         if (filter === "all") return true;         if (filter === "auth") return game.access === "auth";         if (filter === "free") return game.access === "free";
        return game.category === filter;       });        const fragment = document.createDocumentFragment();       grid.classList.remove('is-loading');
      grid.replaceChildren();       empty.hidden = games.length > 0;        games.forEach((game) => {         const card = document.createElement("article");
        card.className = `game-card fade-up ${getGameAccentClass(game)}`;         const gameTop = document.createElement('div');         gameTop.className = 'game-top';         const gameIcon = document.createElement('div');         gameIcon.className = 'game-icon';
        gameIcon.appendChild(createFaIcon(game.icon || 'fa-gamepad'));         const tagStack = document.createElement('div');         tagStack.className = 'tag-stack';         const categoryTag = createTextElement('span', 'mini-tag', game.category === 'online' ? 'Online' : 'Klasik');         if (game.category === 'online') categoryTag.prepend(createTextElement('span', 'live-dot', ''));
        const accessTag = createTextElement('span', 'mini-tag', game.access === 'auth' ? 'Giriş Gerekir' : 'Ücretsiz');         tagStack.append(categoryTag, accessTag);         gameTop.append(gameIcon, tagStack);          const gameBody = document.createElement('div');
        gameBody.className = 'game-body';         gameBody.append(createTextElement('h3', 'game-title', game.name), createTextElement('div', 'game-desc', game.desc));         const featureList = document.createElement('div');         featureList.className = 'feature-list';         (game.tags || []).forEach((tag) => featureList.appendChild(createTextElement('span', 'feature-pill', tag)));
        gameBody.appendChild(featureList);          const gameFooter = document.createElement('div');         gameFooter.className = 'game-footer';         const status = createTextElement('span', 'game-status', game.access === 'auth' ? (user ? 'Hazır' : 'Önce oturum aç') : 'Anında başla');
        const cta = document.createElement('button');         cta.className = 'game-cta' + (game.access === 'free' || user ? ' is-primary' : '');         cta.type = 'button';         cta.textContent = game.access === 'auth' ? (user ? 'Oyunu Aç' : 'Giriş Yap') : 'Ücretsiz Oyna';         gameFooter.append(status, cta);
        card.append(gameTop, gameBody, gameFooter);         const openGame = () => {           if (game.access === "auth" && !auth.currentUser) {             setAuthMode("login");             openSheet("auth", "Hesabına giriş yap", `${game.name} için önce hesabına giriş yapmalısın.`);
            return;           }           location.href = game.url;         }; 
        const primaryBtn = card.querySelector(".game-cta.is-primary, .game-cta");         if (primaryBtn) {           primaryBtn.addEventListener("click", (event) => {             event.stopPropagation();             openGame();
          });         }          if (auth.currentUser && game.category === "online" && game.name === "Satranç") {           const footer = card.querySelector(".game-footer");
          const statusEl = card.querySelector(".game-status");           const actionGroup = document.createElement("div");           actionGroup.className = 'game-action-group';            const quickBtn = document.createElement("button");
          quickBtn.className = "game-cta";           quickBtn.type = "button";           quickBtn.textContent = "Hızlı Eşleş";           quickBtn.addEventListener("click", async (event) => {             event.stopPropagation();
            await startMatchmaking(game.name === "Satranç" ? "chess" : "pisti", game.name === "Pişti" ? { mode: "2-52", bet: 1000 } : {});           });            if (primaryBtn) {             primaryBtn.replaceWith(actionGroup);
            actionGroup.append(quickBtn, primaryBtn);           } else {             actionGroup.appendChild(quickBtn);             footer?.appendChild(actionGroup);           }
           if (statusEl) statusEl.textContent = game.name === "Pişti" ? "Bahisli masaya hızlı gir" : "Anında rakip bul";         }          card.addEventListener("click", () => {
          openGame();         });         fragment.appendChild(card);       }); 
      grid.replaceChildren(fragment);       metricGames.textContent = String(GAMES.length);       revealFadeUps();     } 
    function renderGameShowcaseSkeleton(count = 3){       const grid = $('gamesGrid');       if (!grid) return;       const existingStatic = grid.dataset.staticPainted === '1' && grid.children.length > 0;       if (existingStatic) return;       const fragment = document.createDocumentFragment();       const total = Math.max(1, Math.min(6, Number(count) || 3));       Array.from({ length: total }).forEach(() => {         const card = document.createElement('article');         card.className = 'game-card skeleton is-skeleton';         const top = document.createElement('div');         top.className = 'game-top';         const icon = document.createElement('div');         icon.className = 'game-icon skeleton';         const tags = document.createElement('div');         tags.className = 'tag-stack';         tags.appendChild(createTextElement('span', 'mini-tag skeleton', ''));         tags.appendChild(createTextElement('span', 'mini-tag skeleton', ''));         top.append(icon, tags);         const body = document.createElement('div');         body.className = 'game-body';         body.appendChild(createTextElement('h3', 'game-title skeleton', ''));         body.appendChild(createTextElement('div', 'game-desc skeleton', ''));         const footer = document.createElement('div');         footer.className = 'game-footer';         footer.appendChild(createTextElement('span', 'game-status skeleton', ''));         footer.appendChild(createTextElement('span', 'game-cta skeleton', ''));         card.append(top, body, footer);         fragment.appendChild(card);       });       grid.classList.add('is-loading');       grid.replaceChildren(fragment);       const empty = $('gamesEmpty');       if (empty) empty.hidden = true;     }      function leaderboardSkeleton(){
      const container = $("leaderboardListArea");
      if (!container) return;
      container.dataset.widgetState = "loading";
      container.setAttribute("aria-busy", "true");
      const fragment = document.createDocumentFragment();
      Array.from({ length: 4 }).forEach(() => {
        const item = document.createElement("div");
        item.className = "lb-item skeleton is-skeleton";
        item.setAttribute("aria-hidden", "true");
        fragment.appendChild(item);
      });
      container.replaceChildren(fragment);
    }

    function buildLeaderboardState(type = "empty", title = "", message = "", actionLabel = "Tekrar Dene") {
      const wrap = document.createElement("div");
      wrap.className = `lb-state lb-state--${type}`;
      const heading = createTextElement("strong", "lb-state-title", title || "Liderlik verisi hazırlanıyor");
      const copy = createTextElement("span", "lb-state-copy", message || "Sunucu verisi geldiğinde otomatik güncellenir.");
      wrap.append(heading, copy);
      if (type === "error") {
        const button = document.createElement("button");
        button.type = "button";
        button.className = "ghost-btn lb-state-action";
        button.textContent = actionLabel;
        button.addEventListener("click", () => loadLeaderboard().catch((error) => console.error("[PlayMatrix] leaderboard retry", error)));
        wrap.appendChild(button);
      }
      return wrap;
    }
function getLeaderboardListForTab(tabType){       if (!currentLeaderboardData) return [];       if (tabType === 'level') return Array.isArray(currentLeaderboardData?.tabs?.level?.items) ? currentLeaderboardData.tabs.level.items : [];       if (tabType === 'activity') return Array.isArray(currentLeaderboardData?.tabs?.activity?.items) ? currentLeaderboardData.tabs.activity.items : [];
      return [];     }      async function showPlayerStats(uid){       const targetUid = String(uid || '').trim();
      if (!targetUid) {         showToast('İstatistikler', 'Oyuncu profili bulunamadı.', 'error');         return;       } 
      const content = $('playerStatsContent');       if (!content) return;        const formatDate = (value, includeTime = false) => {         const date = new Date(Number(value) || 0);
        if (!Number.isFinite(date.getTime()) || date.getTime() <= 0) return '—';         return includeTime ? date.toLocaleString('tr-TR') : date.toLocaleDateString('tr-TR');       };        const createHeader = () => {
        const header = document.createElement('div');         header.className = 'ps-modal-header';         header.appendChild(createTextElement('div', 'ps-modal-title', 'Oyuncu İstatistikleri'));         header.firstElementChild.id = 'playerStatsTitle';         const close = document.createElement('button');
        close.className = 'ps-modal-close';         close.type = 'button';         close.setAttribute('aria-label', 'Kapat');         close.appendChild(createFaIcon('fa-xmark'));         close.addEventListener('click', () => closeMatrixModal('playerStatsModal'));
        header.appendChild(close);         return header;       };        const createStatsRow = (label, value, className = '') => {
        const row = document.createElement('div');         row.className = 'player-stats-row';         row.appendChild(createTextElement('span', '', label));         const strong = createTextElement('strong', className, value);         row.appendChild(strong);
        return row;       };        const renderStatusBody = (iconClass, message, tone = 'info') => {         const body = document.createElement('div');
        body.className = 'ps-modal-body player-stats-status-body';         const wrap = document.createElement('div');         const icon = createFaIcon(iconClass);         if (iconClass.includes('spinner')) icon.classList.add('fa-spin', 'fa-2x');         icon.classList.add('player-stats-status-icon');
        icon.dataset.tone = tone;         const text = document.createElement('p');         text.textContent = message;         wrap.append(icon, text);         body.appendChild(wrap);
        return body;       };        content.replaceChildren(createHeader(), renderStatusBody('fa-spinner', 'İstatistikler çekiliyor...', 'info'));       openMatrixModal('playerStatsModal');
       try {         const payload = await fetchPrivate(`/api/user-stats/${encodeURIComponent(targetUid)}`);         const p = normalizeApiUser(payload?.data || {});         const level = getUserLevel(p);
        const isSelf = !!auth.currentUser && auth.currentUser.uid === String(p.uid || targetUid);          const body = document.createElement('div');         body.className = 'ps-modal-body player-stats-modal-body'; 
        const summary = document.createElement('section');         summary.className = 'player-stats-summary-card';         const avatarShell = document.createElement('div');         avatarShell.className = 'player-stats-avatar-shell';         avatarShell.appendChild(createAvatarShellNode(p.avatar || AVATARS[0], resolveAvatarFrameLevel(p, 0), 80, 'pm-avatar--profile'));
        summary.appendChild(avatarShell);         summary.appendChild(createTextElement('h3', 'player-stats-name', p.username || 'Oyuncu'));         const chips = document.createElement('div');         chips.className = 'player-stats-meta-chips';         chips.append(createTextElement('span', 'rank-badge rank-gold', 'Kayıt: ' + formatDate(p.createdAt)), createTextElement('span', 'rank-badge rank-silver', 'Son Görülme: ' + formatDate(p.lastLogin || p.lastSeen, true)));
        summary.appendChild(chips);         body.appendChild(summary);          const general = document.createElement('section');         general.className = 'player-stats-panel';
        general.appendChild(createTextElement('h4', 'player-stats-panel-title', 'Genel Durum'));         const generalRows = document.createElement('div');         generalRows.className = 'player-stats-rows';         generalRows.append(           createStatsRow('Seviye', String(level)),
          createStatsRow('Hesap XP', formatNumber(Number(p.accountXp) || 0)),           createStatsRow('MC Bakiyesi', formatNumber(Number(p.balance ?? p.statistics?.balance ?? 0) || 0) + ' MC'),           createStatsRow('Aylık Aktiflik', formatNumber(Number(p.monthlyActiveScore) || 0)),           createStatsRow('Toplam Oynanan Raund', formatNumber(Number(p.totalRounds) || 0)),           createStatsRow('Harcanan Toplam MC', formatNumber(Number(p.totalSpentMc) || 0))         );
        general.appendChild(generalRows);         body.appendChild(general);          const performance = document.createElement('section');         performance.className = 'player-stats-panel';
        performance.appendChild(createTextElement('h4', 'player-stats-panel-title', 'Oyun Performansı'));
        const performanceRows = document.createElement('div');         performanceRows.className = 'player-stats-rows';
        const gameStats = p.gameStats && typeof p.gameStats === 'object' ? p.gameStats : {};
        const totalStats = gameStats.total && typeof gameStats.total === 'object' ? gameStats.total : {};
        const chessStats = gameStats.chess && typeof gameStats.chess === 'object' ? gameStats.chess : {};
        const pistiStats = gameStats.pisti && typeof gameStats.pisti === 'object' ? gameStats.pisti : {};
        const crashStats = gameStats.crash && typeof gameStats.crash === 'object' ? gameStats.crash : {};
        const classicStats = gameStats.classic && typeof gameStats.classic === 'object' ? gameStats.classic : {};
        performanceRows.append(
          createStatsRow('Toplam Oyun', formatNumber(Number(totalStats.rounds ?? p.totalRounds) || 0)),
          createStatsRow('Kazanma Oranı', '%' + (Number(totalStats.winRatePct ?? p.winRatePct ?? 0) || 0).toFixed(1), 'player-stats-positive'),
          createStatsRow('Satranç', formatNumber(Number(chessStats.wins ?? p.chessWins) || 0) + ' G / ' + formatNumber(Number(chessStats.losses ?? p.chessLosses) || 0) + ' M'),
          createStatsRow('Pişti', formatNumber(Number(pistiStats.wins ?? p.pistiWins) || 0) + ' G / ' + formatNumber(Number(pistiStats.losses ?? p.pistiLosses) || 0) + ' M'),
          createStatsRow('Crash', formatNumber(Number(crashStats.rounds ?? p.crashRounds) || 0) + ' Raund'),
          createStatsRow('Klasik Oyun', formatNumber(Number(classicStats.totalRuns) || 0) + ' Koşu / En iyi ' + formatNumber(Number(classicStats.bestScore) || 0))
        );
        performance.appendChild(performanceRows);         body.appendChild(performance);

        const recentGames = Array.isArray(p.recentGames) ? p.recentGames.slice(0, 6) : [];
        const recentPanel = document.createElement('section');         recentPanel.className = 'player-stats-panel';
        recentPanel.appendChild(createTextElement('h4', 'player-stats-panel-title', 'Son Oyunlar'));
        const recentRows = document.createElement('div');         recentRows.className = 'player-stats-rows';
        if (recentGames.length) {
          recentGames.forEach((game) => {
            const label = String(game.title || game.gameType || 'Oyun');
            const outcome = game.outcome === 'win' ? 'Kazandı' : (game.outcome === 'loss' ? 'Kaybetti' : (game.outcome === 'draw' ? 'Berabere' : String(game.result || 'Tamamlandı')));
            recentRows.appendChild(createStatsRow(label, outcome + ' · ' + formatDate(game.createdAt, true)));
          });
        } else {
          recentRows.appendChild(createStatsRow('Kayıt', 'Henüz son oyun kaydı yok.'));
        }
        recentPanel.appendChild(recentRows);         body.appendChild(recentPanel);

        if (!isSelf) {           const addFriendButton = document.createElement('button');
          addFriendButton.className = 'btn btn-primary player-stats-action';           addFriendButton.id = 'playerStatsAddFriendBtn';           addFriendButton.type = 'button';           addFriendButton.append(createFaIcon('fa-user-plus'), document.createTextNode('Arkadaş Ekle'));           addFriendButton.addEventListener('click', () => sendFriendRequest(String(p.uid || targetUid)));
          body.appendChild(addFriendButton);         }          content.replaceChildren(createHeader(), body);       } catch (error) {
        reportClientError('home.player_stats', error, { source: 'legacy-home.runtime.js', targetUid });
        content.replaceChildren(createHeader(), renderStatusBody('fa-triangle-exclamation', error.message || 'Veri çekilemedi.', 'error'));       }     }     window.showPlayerStats = showPlayerStats;     window.openPlayerProfile = showPlayerStats;
    window.showPlayerProfile = showPlayerStats;      function renderLeaderboardTab(tabType = "level"){
      if (!currentLeaderboardData) return;
      currentLeaderboardTab = tabType;
      document.querySelectorAll("#leaderboardTabs .lb-tab-btn").forEach((btn) => {
        const isActive = btn.dataset.lbTab === tabType;
        btn.classList.toggle("active", isActive);
        btn.setAttribute("aria-selected", isActive ? "true" : "false");
      });
      const container = $("leaderboardListArea");
      if (!container) return;
      container.replaceChildren();
      container.dataset.widgetState = "ready";
      container.setAttribute("aria-busy", "false");
      const list = getLeaderboardListForTab(tabType).slice(0, 5);
      const categoryMeta = currentLeaderboardData?.tabs && typeof currentLeaderboardData.tabs === "object" ? currentLeaderboardData.tabs : {};
      let label = "";
      let scoreToneClass = "lb-score-val--default";
      if (tabType === "level") { label = "SEVİYE"; scoreToneClass = "lb-score-val--level"; }
      else if (tabType === "activity") { label = "AKTİFLİK"; scoreToneClass = "lb-score-val--activity"; }
      const categoryLabel = String(categoryMeta?.[tabType]?.label || "").trim();
      if (!list.length) {
        container.dataset.widgetState = "empty";
        container.appendChild(buildLeaderboardState("empty", categoryLabel || "Liderlik verisi yok", tabType === "activity" ? "Aylık aktiflik sıralamasında henüz kayıt yok." : "Hesap seviyesi sıralamasında henüz kayıt yok."));
        renderHomeStats(state.userData || {});
        return;
      }
      list.forEach((user, index) => {
        const rankNum = Math.max(1, Number(user?.leaderboard?.rank || (index + 1)) || (index + 1));
        const item = document.createElement("button");
        item.type = "button";
        item.className = "lb-item";
        item.dataset.uid = String(user.uid || user.id || "");
        item.setAttribute("aria-label", `${user.username || "Oyuncu"} profili`);
        const rank = document.createElement("div");
        rank.className = "lb-rank";
        if (rankNum === 1) rank.classList.add("top-1");
        else if (rankNum === 2) rank.classList.add("top-2");
        else if (rankNum === 3) rank.classList.add("top-3");
        rank.textContent = `#${rankNum}`;
        const wrapper = createAvatarShellNode(user.avatar || AVATARS[0], resolveAvatarFrameLevel(user, 0), 45, "pm-avatar--leaderboard");
        const name = document.createElement("div");
        name.className = "lb-name";
        name.textContent = String(user.username || "Anonim");
        const metaLine = document.createElement("div");
        metaLine.className = "lb-user-meta";
        metaLine.textContent = `MC ${formatNumber(Number(user.balance || user.statistics?.balance || 0) || 0)} · ${formatNumber(Number(user.stats?.totalWins || user.statistics?.totalWins || 0) || 0)} G`;
        name.appendChild(metaLine);
        let scoreVal = "";
        if (tabType === "level") scoreVal = `Lv. ${Math.max(1, Number(user.accountLevel) || 1)}`;
        else if (tabType === "activity") scoreVal = formatNumber(Number(user.monthlyActiveScore || user.score || 0) || 0);
        const score = document.createElement("div");
        score.className = "lb-score-box";
        const scoreValue = document.createElement("div");
        scoreValue.className = `lb-score-val ${scoreToneClass}`;
        scoreValue.textContent = scoreVal || "—";
        const scoreLabel = document.createElement("div");
        scoreLabel.className = "lb-score-label";
        scoreLabel.textContent = label || "SKOR";
        score.append(scoreValue, scoreLabel);
        item.append(rank, wrapper, name, score);
        container.appendChild(item);
      });
      renderHomeStats(state.userData || {});
    }
async function fetchLeaderboardPayload(){       const publicEndpoints = ['/api/leaderboard'];       let lastError = null;
       for (const endpoint of publicEndpoints) {         try {           const payload = await fetchPublic(endpoint);           if (payload && payload.ok !== false) return payload;
          lastError = new Error(payload?.error || 'Liderlik tablosu yüklenemedi.');         } catch (error) {           lastError = error;         }       }
       if (auth.currentUser) {         for (const endpoint of publicEndpoints) {           try {             const payload = await fetchPrivate(endpoint);
            if (payload && payload.ok !== false) return payload;             lastError = new Error(payload?.error || 'Liderlik tablosu yüklenemedi.');           } catch (error) {             lastError = error;           }
        }       }        throw lastError || new Error('Liderlik tablosu yüklenemedi.');     }
     async function loadLeaderboard(){
      leaderboardSkeleton();
      try {
        const payload = await fetchLeaderboardPayload();
        if (!payload || payload.ok === false) throw new Error(payload?.error || payload?.message || "Liderlik tablosu yüklenemedi.");
        currentLeaderboardData = normalizeLeaderboardPayload(payload || {});
        const levelTab = $("tab-level");
        const activityTab = $("tab-activity");
        if (levelTab && currentLeaderboardData?.tabs?.level?.label) levelTab.textContent = currentLeaderboardData.tabs.level.label;
        if (activityTab && currentLeaderboardData?.tabs?.activity?.label) activityTab.textContent = currentLeaderboardData.tabs.activity.label;
        renderLeaderboardTab(currentLeaderboardTab || "level");
      } catch (error) {
        const container = $("leaderboardListArea");
        if (container) {
          container.replaceChildren();
          container.dataset.widgetState = "error";
          container.setAttribute("aria-busy", "false");
          container.appendChild(buildLeaderboardState("error", "Liderlik verisi alınamadı", error.message || "Sunucuya ulaşılamadı."));
        }
        renderHomeStats(state.userData || {});
      }
    }
function drawWheel(){       const canvas = $("wheelCanvas");       const ctx = canvas.getContext("2d");
      const centerX = canvas.width / 2;       const centerY = canvas.height / 2;       const radius = canvas.width / 2;       const sliceAngle = (2 * Math.PI) / REWARDS.length;       ctx.clearRect(0, 0, canvas.width, canvas.height);
       REWARDS.forEach((reward, index) => {         const angle = index * sliceAngle - Math.PI / 2;         ctx.beginPath();         ctx.fillStyle = reward.color;
        ctx.moveTo(centerX, centerY);         ctx.arc(centerX, centerY, radius, angle, angle + sliceAngle);         ctx.closePath();         ctx.fill();         ctx.strokeStyle = "rgba(63,140,255,.22)";
        ctx.lineWidth = 6;         ctx.stroke();          ctx.save();         ctx.translate(centerX, centerY);
        ctx.rotate(angle + sliceAngle / 2);         ctx.textAlign = "right";         ctx.fillStyle = index % 2 === 0 ? "#BEEFFF" : "#F5F7FF";         ctx.font = "900 30px Inter";         ctx.fillText(reward.label, radius - 34, 12);
        ctx.restore();       });     }      async function refreshWheelUI(){
      if (!auth.currentUser) return;       await reload(auth.currentUser);       if (auth.currentUser.emailVerified) {         await tryClaimPendingReferral();       }
      const data = state.userData || {};       const lastSpin = Number(data.lastSpin || data.lastSpinAt || 0);       const cooldownEl = $("wheelCooldownText");       const trFormatter = new Intl.DateTimeFormat('en-CA', { timeZone:'Europe/Istanbul', year:'numeric', month:'2-digit', day:'2-digit' });       const todayTr = trFormatter.format(new Date());       const lastTr = lastSpin ? trFormatter.format(new Date(lastSpin)) : '';       const lockedToday = !!lastSpin && todayTr === lastTr;       if (lockedToday) {
        const nowTr = new Date(new Date().toLocaleString('en-US', { timeZone:'Europe/Istanbul' }));         const nextMidnightTr = new Date(nowTr);         nextMidnightTr.setHours(24,0,0,0);         const diff = Math.max(0, nextMidnightTr.getTime() - nowTr.getTime());         const h = Math.floor(diff / 3600000);         const m = Math.floor((diff % 3600000) / 60000);         $("wheelSpinBtn").disabled = true;         if (cooldownEl) cooldownEl.textContent = `Türkiye saati 00:00 sonrası açılır: ${h} saat ${m} dakika.`;         $("wheelResult").textContent = `Bugünkü hakkını kullandın. Haklar Europe/Istanbul 00:00'da yenilenir.`;
      } else {         $("wheelSpinBtn").disabled = !auth.currentUser.emailVerified;         if (cooldownEl) cooldownEl.textContent = auth.currentUser.emailVerified ? "Çark çevirmeye hazır." : "E-postanı onayla, çark açılır.";         if (!auth.currentUser.emailVerified) $("wheelResult").textContent = "Çarkı açmak için e-posta doğrulaması gerekli.";       }
    }      function resolveWheelSpinResponse(response){       const prize = Number(response?.prize ?? response?.amount ?? 0);       let index = Number(response?.index);       if (!Number.isInteger(index) || index < 0 || index >= REWARDS.length || Number(REWARDS[index]?.val || 0) !== prize) {         index = REWARDS.findIndex((reward) => Number(reward.val || 0) === prize);       }       if (!Number.isInteger(index) || index < 0 || index >= REWARDS.length) throw new Error("Sunucu çark sonucunu doğrulayamadı.");       if (!Number.isFinite(prize) || prize <= 0) throw new Error("Sunucu çark ödülünü doğrulayamadı.");       return { index, prize };     }      async function spinWheel(){       try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");
        if (!auth.currentUser.emailVerified) throw new Error("Çark için önce e-postanı doğrulamalısın.");         $("wheelSpinBtn").disabled = true;         $("wheelResult").textContent = "Sunucuda hesaplanıyor…";         const response = await fetchPrivate("/api/wheel/spin", "POST");         const resolvedSpin = resolveWheelSpinResponse(response);         const prize = resolvedSpin.prize;
        const randomIndex = resolvedSpin.index;         const segmentDeg = 360 / REWARDS.length;         const stopAt = 360 - (randomIndex * segmentDeg);         const extraTours = 3600;         state.wheelRotation += extraTours + stopAt;
        $("wheelCanvas").style.transform = `rotate(${state.wheelRotation}deg)`;         setTimeout(async () => {           $("wheelResult").textContent = `Başarılı! ${formatNumber(prize)} MC kazandın.`;           showToast("Çark sonucu", `${formatNumber(prize)} MC hesabına işlendi.`, "success");           await loadUserData();
          await refreshWheelUI();         }, 5000);       } catch (error) {         $("wheelResult").textContent = error.message;         $("wheelSpinBtn").disabled = false;
        showToast("Çark hatası", error.message, "error");       }     }      async function submitPromo(){
      try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         if (!auth.currentUser.emailVerified) throw new Error("Promo için önce e-postanı doğrulamalısın.");         const code = ($("promoCode").value || "").trim().toUpperCase();         if (!code) throw new Error("Promo kodu gir.");
        $("promoHelp").textContent = "Doğrulanıyor…";         $("promoHelp").className = "field-help";         const response = await fetchPrivate("/api/promo/claim", "POST", { code });         $("promoCode").value = "";         $("promoHelp").textContent = `Onaylandı! ${formatNumber(response.amount)} MC eklendi.`;
        $("promoHelp").className = "field-help is-success";         showToast("Promo başarılı", `${formatNumber(response.amount)} MC hesabına işlendi.`, "success");         await loadUserData();       } catch (error) {         $("promoHelp").textContent = error.message;
        $("promoHelp").className = "field-help is-error";       }     }      async function submitSupport(){
      try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         const subject = ($("supportSubject").value || "").trim();         const reference = ($("supportReference").value || "").trim();         const category = $("supportCategory").value || "Genel";
        const priority = $("supportPriority").value || "Normal";         const message = ($("supportMessage").value || "").trim();          if (subject.length < 4) throw new Error("Lütfen en az 4 karakterlik bir konu başlığı gir.");         if (message.length < 15) throw new Error("Sorun detayını daha açıklayıcı yaz. En az 15 karakter gerekli.");
         $("supportHelp").textContent = "Gönderiliyor…";         $("supportHelp").className = "field-help";         await fetchPrivate("/api/support/message", "POST", {           subject,
          message,
          text: message,           category,           priority,
          urgency: priority,           roundId: reference         });
        $("supportSubject").value = "";         $("supportReference").value = "";         $("supportCategory").value = "Genel";         $("supportPriority").value = "Normal";         $("supportMessage").value = "";
        $("supportHelp").textContent = "Destek kaydın oluşturuldu.";         $("supportHelp").className = "field-help is-success";         showToast("Destek talebi gönderildi", "Detaylı destek kaydın başarıyla iletildi.", "success");       } catch (error) {         $("supportHelp").textContent = error.message;
        $("supportHelp").className = "field-help is-error";       }     }      async function loadInviteLink(){
      try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         $("inviteHelp").textContent = "Link hazırlanıyor…";         $("inviteHelp").className = "field-help";         const response = await fetchPrivate("/api/referral/link");
        const code = (response.code || "").trim();         let link = (response.link || "").trim();         if (code && !link) link = `${window.location.origin}/?ref=${code}`;         if (link.startsWith("?ref=")) link = `${window.location.origin}/${link}`;         $("inviteCode").value = code;
        $("inviteLink").value = link;         $("inviteHelp").textContent = "Hazır. Kopyalayıp paylaşabilirsin.";         $("inviteHelp").className = "field-help is-success";       } catch (error) {         $("inviteHelp").textContent = error.message;
        $("inviteHelp").className = "field-help is-error";       }     }      async function copyFieldValue(id, message){
      const value = $(id).value || "";       if (!value) return;       try {         await navigator.clipboard.writeText(value);         showToast("Kopyalandı", message, "success");
      } catch {         showToast("Kopyalama hatası", "Panoya kopyalanamadı.", "error");       }     } 
    async function saveProfile(){       try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         const profilePayload = await syncProfileUpdateWithRetry({           fullName: ($("profileFullName").value || "").trim(),
          username: ($("profileUsername").value || "").trim(),           avatar: state.selectedAvatar || AVATARS[0],           selectedFrame: getSelectedFrameLevel()         }, { attempts: 3 });         if (profilePayload?.user) applyProfilePayload(profilePayload);         showToast("Profil güncellendi", "Profil bilgilerin kaydedildi.", "success");
      } catch (error) {         showToast("Profil hatası", error.message, "error");       }     }
     async function sendVerificationMail(){       try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         await sendEmailVerification(auth.currentUser);
        showToast("Doğrulama maili", "E-posta adresine doğrulama bağlantısı gönderildi.", "success");       } catch (error) {         showToast("Gönderim hatası", normalizeFirebaseAuthError(error), "error");       }     }
     async function requestEmailUpdate(){       try {         if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         const currentEmail = auth.currentUser.email || "";
        const rawEmail = await showInputDialog({ title:'E-posta Adresini Güncelle', message:'Yeni e-posta adresine güvenli doğrulama bağlantısı gönderilecek. Bağlantı onaylanmadan hesap e-postası değişmez.', value:currentEmail, placeholder:'ornek@mail.com', inputType:'email', confirmText:'Doğrulama Gönder' });         const nextEmail = String(rawEmail || '').trim().toLowerCase();         if (!nextEmail) return;         if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(nextEmail)) throw new Error('Geçerli bir e-posta adresi gir.');         if (nextEmail === currentEmail.toLowerCase()) {           showToast("Bilgi", "Yeni e-posta adresi mevcut adresle aynı.", "info");           return;
        }         await verifyBeforeUpdateEmail(auth.currentUser, nextEmail);         showToast("Güncelleme bağlantısı", "Yeni e-posta adresine doğrulama bağlantısı gönderildi.", "success");       } catch (error) {         showToast("E-posta güncelleme hatası", normalizeFirebaseAuthError(error), "error");
      }     }      async function handleVerifyEmailAction(){       try {
        if (!auth.currentUser) throw new Error("Önce giriş yapmalısın.");         await reload(auth.currentUser);         syncVerifyButtonState();         if (auth.currentUser.emailVerified) {           await requestEmailUpdate();
          return;         }         await sendVerificationMail();       } catch (error) {         showToast("İşlem hatası", error.message, "error");
      }     }      async function checkUsernameAvailability(){       const value = ($("profileUsername").value || "").trim();
      $("usernameHelp").textContent = "";       $("usernameHelp").className = "field-help";       if (!auth.currentUser || !value) return;       if (value.length < 3) {         $("usernameHelp").textContent = "Kullanıcı adı en az 3 karakter olmalı.";
        return;       }        const current = (state.userData?.username || "").toLowerCase();       if (current === value.toLowerCase()) {
        $("usernameHelp").textContent = "Mevcut kullanıcı adın.";         $("usernameHelp").className = "field-help is-success";         return;       } 
      try {         $("usernameHelp").textContent = "Kontrol ediliyor…";         const payload = await fetchPrivate("/api/check-username?username=" + encodeURIComponent(value));         $("usernameHelp").textContent = payload.available ? "Kullanılabilir." : "Bu kullanıcı adı dolu.";         $("usernameHelp").className = `field-help ${payload.available ? "is-success" : "is-error"}`;
      } catch(_) {         $("usernameHelp").textContent = "";         $("usernameHelp").className = "field-help";       }     }
     async function submitAuth(){       const email = ($("authEmail").value || "").trim();       const password = $("authPassword").value || "";       $("authHelp").textContent = "";
      $("authHelp").className = "field-help";        try {         if (state.authMode === "login") {           if (!email || !password) throw new Error("E-posta veya kullanıcı adı ve şifre zorunlu.");
          const resolvedEmail = await resolveLoginIdentifier(email);           await signInWithEmailAndPassword(auth, resolvedEmail, password);           const bootstrapPayload = await bootstrapServerSession(auth.currentUser, { force: true, forceRefresh: true });           if (!bootstrapPayload || bootstrapPayload.ok === false) {             throw new Error("Sunucu oturumu hazırlanamadı.");
          }           showToast("Giriş başarılı", "Oturum açıldı.", "success");           closeSheet();         } else {           const fullName = ($("authFullName").value || "").trim();
          const username = ($("authUsername").value || "").trim();           if (!fullName || !username || !email || !password) throw new Error("Tüm alanları doldur.");           const credential = await createUserWithEmailAndPassword(auth, email, password);           await sendEmailVerification(credential.user);           await refreshAuthVerificationState(true);           const profilePayload = await syncProfileUpdateWithRetry({ fullName, username, avatar: AVATARS[0] }, { attempts: 3, forceRefresh: true });           if (!profilePayload?.user?.username) throw new Error("Profil kayıt senkronizasyonu tamamlanamadı.");           showToast("Hesap oluşturuldu", "Profilin kaydedildi ve doğrulama bağlantısı e-posta adresine gönderildi.", "success");           closeSheet();         }
      } catch (error) {         const message = normalizeFirebaseAuthError(error);         $("authHelp").textContent = message;
        $("authHelp").className = "field-help is-error";       }     }      async function forgotPassword(){
      const email = ($("forgotEmail").value || "").trim();       try {         if (!email) throw new Error("E-posta adresi zorunlu.");         await sendPasswordResetEmail(auth, email);         $("forgotHelp").textContent = "Sıfırlama bağlantısı gönderildi.";
        $("forgotHelp").className = "field-help is-success";         showToast("Şifre sıfırlama", "Sıfırlama linki e-posta adresine gönderildi.", "success");       } catch (error) {         $("forgotHelp").textContent = normalizeFirebaseAuthError(error) || "Gönderilemedi.";         $("forgotHelp").className = "field-help is-error";
      }     }      function updateMobileTabs(){       const sections = ["#hero", "#games", "#leaderboard"];
      const positions = sections.map((selector) => {         const element = document.querySelector(selector);         return { selector, top: element ? element.getBoundingClientRect().top : Number.POSITIVE_INFINITY };       });       let active = "#hero";
      for (const entry of positions) if (entry.top <= window.innerHeight * .35) active = entry.selector;       document.querySelectorAll(".mobile-tab").forEach((button) => {         if (button.dataset.mobileLink) button.classList.toggle("is-active", button.dataset.mobileLink === active);       });     }
     function bindSheetDrag(){       const start = (clientY) => {         if (window.innerWidth > 860) return;         state.drag.active = true;
        state.drag.startY = clientY;         state.drag.deltaY = 0;       };       const move = (clientY) => {         if (!state.drag.active) return;
        state.drag.deltaY = Math.max(0, clientY - state.drag.startY);         $("sheetPanel").style.transform = `translateY(${state.drag.deltaY}px)`;       };       const end = () => {         if (!state.drag.active) return;
        const close = state.drag.deltaY > 120;         state.drag.active = false;         if (close) closeSheet();         else $("sheetPanel").style.transform = "";       };
      $("sheetHandle").addEventListener("touchstart", (e) => start(e.touches[0].clientY), { passive:true });       $("sheetHandle").addEventListener("touchmove", (e) => move(e.touches[0].clientY), { passive:true });       $("sheetHandle").addEventListener("touchend", end);       $("sheetHandle").addEventListener("pointerdown", (e) => start(e.clientY));       window.addEventListener("pointermove", (e) => move(e.clientY));
      window.addEventListener("pointerup", end);     }      function revealFadeUps(){       document.querySelectorAll(".fade-up").forEach((element) => {
        const rect = element.getBoundingClientRect();         if (rect.top < window.innerHeight - 40) element.classList.add("is-visible");       });     } 
    function installTouchHardening(){       document.documentElement.style.webkitTextSizeAdjust = "100%";       document.documentElement.style.touchAction = "manipulation";       document.body.style.touchAction = "manipulation"; 
      document.addEventListener("gesturestart", (event) => {         if (document.body.classList.contains("sheet-open") && event.touches && event.touches.length > 1) {           event.preventDefault();         }       }, { passive:false });
    }       function setAppHeight(){       const viewportHeight = window.visualViewport ? window.visualViewport.height : window.innerHeight;
      const stableHeight = Math.max(window.innerHeight || 0, viewportHeight || 0, document.documentElement.clientHeight || 0);       document.documentElement.style.setProperty("--app-height", `${Math.round(stableHeight)}px`);     }      function forceFirstPaintRefresh(){
      setAppHeight();       revealFadeUps();       updateMobileTabs();       const main = document.querySelector("main.container");       if (!main) return;
      main.style.transform = "translateZ(0)";       requestAnimationFrame(() => {         main.style.transform = "";       });     }
     function installFirstPaintStabilizer(){       try {         if ("scrollRestoration" in history) history.scrollRestoration = "manual";       } catch (_) {}
       const queueRefresh = (delay = 0) => window.setTimeout(() => requestAnimationFrame(forceFirstPaintRefresh), delay);       [0, 16, 40, 90, 180, 320, 520].forEach(queueRefresh);        window.addEventListener("load", () => queueRefresh(0), { once:true, passive:true });
      window.addEventListener("pageshow", () => queueRefresh(0), { passive:true });       window.addEventListener("resize", () => queueRefresh(0), { passive:true });       window.addEventListener("orientationchange", () => queueRefresh(60), { passive:true });       document.addEventListener("visibilitychange", () => {         if (document.visibilityState === "visible") queueRefresh(0);
      }, { passive:true });       document.addEventListener("touchstart", () => queueRefresh(0), { passive:true, once:true });        if (window.visualViewport) {         window.visualViewport.addEventListener("resize", () => queueRefresh(0), { passive:true });
        window.visualViewport.addEventListener("scroll", () => queueRefresh(0), { passive:true });       }     }      function installHomepageCoreDelegation(){
      document.addEventListener('click', (event) => {         const actionTrigger = event.target.closest('[data-pm-action="closeSocialSheet"], [data-pm-click-id="sheetClose"]');         if (actionTrigger) { event.preventDefault(); closeSheet(); return; }         const trigger = event.target.closest('#brandHome, #loginBtn, #registerBtn, #heroStartBtn, #refreshLeaderboardBtn, .mobile-tab, .nav-link[href^="#"]');         if (!trigger) return;          if (trigger.matches('#brandHome')) {
          event.preventDefault();           window.scrollTo({ top:0, behavior:'smooth' });           return;         } 
        if (trigger.matches('#loginBtn')) {           event.preventDefault();           setAuthMode('login');           openSheet('auth', 'Hesabına giriş yap', 'Oyunlara, çarka ve profil araçlarına anında eriş.');           return;
        }          if (trigger.matches('#registerBtn')) {           event.preventDefault();           setAuthMode('register');
          openSheet('auth', 'Yeni hesap oluştur', 'Kullanıcı adı ve e-posta ile hızlı kayıt akışı.');           return;         }          if (trigger.matches('#heroStartBtn')) {
          event.preventDefault();           if (auth.currentUser) openProfileSheet();           else {             setAuthMode('register');             openSheet('auth', 'Yeni hesap oluştur', 'Kullanıcı adı ve e-posta ile hızlı kayıt akışı.');
          }           return;         }          if (trigger.matches('#refreshLeaderboardBtn')) {
          event.preventDefault();           loadLeaderboard().catch((error) => console.error('[PlayMatrix] leaderboard refresh', error));           return;         } 
        if (trigger.matches('.mobile-tab')) {           const link = trigger.dataset.mobileLink;           const action = trigger.dataset.mobileAction;           if (link) {             event.preventDefault();
            document.querySelector(link)?.scrollIntoView({ behavior:'smooth', block:'start' });             updateMobileTabs();             return;           }           if (action === 'wheel') { event.preventDefault(); openWheelSheet(); return; }
          if (action === 'promo') { event.preventDefault(); openPromoSheet(); return; }           if (action === 'profile') { event.preventDefault(); openProfileSheet(); return; }           return;         } 
        if (trigger.matches('.nav-link[href^="#"]')) {           const target = trigger.getAttribute('href');           if (!target) return;           const section = document.querySelector(target);           if (!section) return;
          event.preventDefault();           section.scrollIntoView({ behavior:'smooth', block:'start' });         }       }, { passive:false });     }
     function hydrateHomepageCore(){       if (!document.body.dataset.leaderboardStatsDelegationBound) {         document.body.dataset.leaderboardStatsDelegationBound = '1';         document.addEventListener('click', (event) => {           const item = event.target?.closest?.('#leaderboardListArea .lb-item');           if (!item || item.classList.contains('skeleton')) return;           const targetUid = String(item.dataset.uid || '').trim() || String(auth.currentUser?.uid || '').trim();           if (targetUid && typeof showPlayerStats === 'function') showPlayerStats(targetUid);         });       }       safeExecute('games skeleton', () => safeRenderGameShowcaseSkeleton(3));       safeExecute('leaderboard skeleton', leaderboardSkeleton);       requestAnimationFrame(() => {
        safeExecute('render games', renderGames);         safeExecute('update overview', updateSystemOverview);         Promise.resolve(loadLeaderboard()).catch((error) => console.error('[PlayMatrix] leaderboard boot', error));         safeExecute('update user shell', updateUserShell);         safeExecute('verify button', syncVerifyButtonState);
        safeExecute('mobile tabs', updateMobileTabs);         safeExecute('fade up reveal', revealFadeUps);       });     } 
    function bindEvents(){       bindIfPresent("profileTrigger", "click", (e) => {         e.stopPropagation();         playSound("tap");         $("userDropdown").classList.toggle("active");
      });        document.addEventListener("click", (e) => {         if ($("userDropdown") && !e.target.closest("#topUser")) $("userDropdown").classList.remove("active");       });
       bindIfPresent("navProfileItem", "click", () => { $("userDropdown").classList.remove("active"); openProfileSheet(); });       bindIfPresent("navWheelItem", "click", () => { $("userDropdown").classList.remove("active"); openWheelSheet(); });       bindIfPresent("navBonusItem", "click", () => { $("userDropdown").classList.remove("active"); openPromoSheet(); });       bindIfPresent("navMarketItem", "click", () => { $("userDropdown").classList.remove("active"); openMarketSheet(); });       bindIfPresent("navSupportItem", "click", () => { $("userDropdown").classList.remove("active"); openSupportSheet(); });
      bindIfPresent("navInviteItem", "click", async () => { $("userDropdown").classList.remove("active"); await openInviteSheet(); });       bindIfPresent("navSocialItem", "click", async () => { $("userDropdown").classList.remove("active"); await primeRealtimeUX().catch(() => null); await openSocialSheet(); });       bindIfPresent("logoutDropdownBtn", "click", async () => { $("userDropdown").classList.remove("active"); await endServerSession(); await signOut(auth); closeSheet(); showToast("Çıkış yapıldı", "Oturum güvenli şekilde kapatıldı.", "info"); });        document.querySelectorAll("#leaderboardTabs .lb-tab-btn").forEach((button) => {
        button.addEventListener("click", () => renderLeaderboardTab(button.dataset.lbTab || "level"));       });       const openAvatarSelectionBtn = $("openAvatarSelectionBtn");       if (openAvatarSelectionBtn) openAvatarSelectionBtn.addEventListener("click", openAvatarSelectionModal);       const openFrameSelectionBtn = $("openFrameSelectionBtn");
      if (openFrameSelectionBtn) openFrameSelectionBtn.addEventListener("click", openFrameSelectionModal);       document.querySelectorAll('.ps-modal').forEach((modal) => {         modal.addEventListener('click', (event) => {           if (event.target === modal) closeMatrixModal(modal.id);         });
      });       document.addEventListener('keydown', (event) => {         if (event.key === 'Escape') {           const activeModal = document.querySelector('.ps-modal.active');           if (activeModal) closeMatrixModal(activeModal.id);
        }       });       bindIfPresent("sheetBackdrop", "click", closeSheet);       bindIfPresent("sheetClose", "click", closeSheet);       bindIfPresent("socialHomeBackBtn", "click", closeSheet);       $("authSegment").querySelectorAll("button").forEach((button) => button.addEventListener("click", () => setAuthMode(button.dataset.authMode)));
      bindIfPresent("authSubmitBtn", "click", submitAuth);       bindIfPresent("forgotPasswordBtn", "click", () => {         $("forgotEmail").value = $("authEmail").value || "";         $("forgotHelp").textContent = "";         $("forgotHelp").className = "field-help";
        openSheet("forgot", "Şifremi Unuttum", "Sıfırlama bağlantısı e-posta adresine gönderilir.");       });       bindIfPresent("forgotSubmitBtn", "click", forgotPassword);        bindIfPresent("profileSaveBtn", "click", saveProfile);
      bindIfPresent("verifyEmailBtn", "click", handleVerifyEmailAction);       bindIfPresent("logoutBtn", "click", async () => { await endServerSession(); await signOut(auth); closeSheet(); showToast("Çıkış yapıldı", "Oturum güvenli şekilde kapatıldı.", "info"); });       bindIfPresent("wheelSpinBtn", "click", spinWheel);       bindIfPresent("wheelRefreshBtn", "click", refreshWheelUI);       bindIfPresent("promoSubmitBtn", "click", submitPromo);       bindIfPresent("homeStatsRetryBtn", "click", async () => { renderHomeStats(state.userData || {}, { state: "loading" }); await Promise.allSettled([auth.currentUser ? loadUserData() : Promise.resolve(), loadLeaderboard()]); renderHomeStats(state.userData || {}); });
      bindIfPresent("supportSubmitBtn", "click", submitSupport);       bindIfPresent("generateInviteBtn", "click", loadInviteLink);       bindIfPresent("copyInviteCodeBtn", "click", () => copyFieldValue("inviteCode", "Davet kodu panoya kopyalandı."));       bindIfPresent("copyInviteLinkBtn", "click", () => copyFieldValue("inviteLink", "Davet bağlantısı panoya kopyalandı."));       document.querySelectorAll(".ps-tab").forEach((button) => {
        button.addEventListener("click", () => {           playSound("tap");           const targetTab = button.dataset.socialTab || "global";           setSocialTab(targetTab, { resetSelection: true, openPanel: targetTab === "global" || targetTab === "add" });         });
      });       bindIfPresent("psSendBtn", "click", () => { primeRealtimeUX().catch(() => null); sendSocialMessage(); });       bindIfPresent("psChatInput", "keydown", (event) => {         if (event.key === "Enter") {           event.preventDefault();
          sendSocialMessage();         }       });       bindIfPresent("psChatInput", "input", () => {         setActiveComposerDraft($("psChatInput")?.value || "");
        handleSocialComposerInput();         handleDirectTypingActivity();       });       bindIfPresent("psMobileBackBtn", "click", () => {         state.social.mobilePanelOpen = false;
        renderSocialHub();       });       window.addEventListener("resize", () => {         const widthChanged = Math.abs(window.innerWidth - lastSocialViewportWidth) > 2;         lastSocialViewportWidth = window.innerWidth;
        lastSocialViewportHeight = window.innerHeight;         if (!isSocialMobile()) {           state.social.mobilePanelOpen = true;         }         if (state.currentSheet !== "social") return;
        if (!widthChanged && isSocialKeyboardSensitiveFocus()) return;         renderSocialHub(widthChanged ? {} : { main: false });       }, { passive:true });       bindIfPresent("profileUsername", "input", () => {         clearTimeout(state.usernameDebounce);
        state.usernameDebounce = setTimeout(checkUsernameAvailability, 320);       });       bindIfPresent("soundToggle", "click", () => {         state.soundEnabled = !state.soundEnabled;         localStorage.setItem("pm_ui_sound", state.soundEnabled ? "on" : "off");
        updateSoundUI();         showToast("Arayüz sesi", state.soundEnabled ? "Arayüz tık sesleri açık." : "Arayüz sesleri kapatıldı.", "info");       });        document.querySelectorAll(".filter-chip").forEach((button) => {
        button.addEventListener("click", () => {           document.querySelectorAll(".filter-chip").forEach((chip) => chip.classList.remove("is-active"));           button.classList.add("is-active");           state.activeFilter = button.dataset.filter;           renderGames();
        });       });        bindIfPresent("gameSearch", "input", renderGames); 
      window.addEventListener("scroll", updateMobileTabs, { passive:true });       window.addEventListener("scroll", revealFadeUps, { passive:true });       window.addEventListener("resize", revealFadeUps, { passive:true });     } 
    function isTransientApiBootstrapError(error) {       const message = String(error?.message || '').toLowerCase();       return message.includes('404')         || message.includes('405')         || message.includes('502')
        || message.includes('503')         || message.includes('504')         || message.includes('firebase admin')         || message.includes('credential')         || message.includes('hazır değil')         || message.includes('hazir degil')         || message.includes('sunucuyla bağlantı kurulamadı')         || message.includes('fetch')         || message.includes('network');
    }      onAuthStateChanged(auth, async (user) => {       state.authBootAttemptedAt = Date.now();       if (state.authBootInFlight) return;
      state.authBootInFlight = true;       try {         if (user) {           state.authStatus = "syncing";           state.authReady = false;           const bootstrapPayload = await bootstrapServerSession(user, { force: true, forceRefresh: true });
          if (!bootstrapPayload || bootstrapPayload.ok === false) {             throw new Error('Sunucu oturumu hazırlanamadı.');           }           state.authReady = true;           state.authStatus = "authenticated";           await loadUserData();
          startActivityHeartbeat();           await tryClaimPendingReferral();           await ensureRealtimeConnection(false, { reason: 'auth_boot' }).catch(() => null);           await loadFriends().catch(() => null);         } else {
          state.authReady = false;           state.authStatus = "guest";           stopActivityHeartbeat();           disconnectRealtime({ preserveIntent: false });           state.userData = null;                     state.friends = { accepted: [], incoming: [], outgoing: [], counts: { accepted:0, incoming:0, outgoing:0, online:0 } };
          state.lobbyMessages = [];           resetSocialState();           safeExecute('render social hub (guest)', renderSocialHub);           safeExecute('update user shell (guest)', updateUserShell);           safeExecute('verify button (guest)', syncVerifyButtonState);
        }       } catch (error) {         console.error('[PlayMatrix] auth state flow', error);         if (user) {           if (isTransientApiBootstrapError(error)) {
            state.authReady = false;             state.authStatus = "degraded";             stopActivityHeartbeat();             disconnectRealtime({ preserveIntent: false });             notifySessionIssueOnce('Sunucu bağlantısı', 'Hesap verileri geçici olarak yüklenemedi. Sayfayı yenileyip tekrar dene.', 'warning');             safeExecute('update user shell (degraded)', updateUserShell);
          } else {             await forceSecureLogout(error?.message || 'Oturum hazırlanamadı.', { silent: false });           }         }       } finally {
        state.authBootInFlight = false;         safeExecute('render games after auth', renderGames);         await Promise.resolve(loadLeaderboard()).catch((error) => console.error('[PlayMatrix] leaderboard after auth', error));         safeExecute('mobile tabs after auth', updateMobileTabs);       }
    });      document.addEventListener("mousemove", (event) => {       document.documentElement.style.setProperty("--mx", `${(event.clientX / window.innerWidth) * 100}%`);       document.documentElement.style.setProperty("--my", `${(event.clientY / window.innerHeight) * 100}%`);
    }, { passive:true });       safeExecute('capture referral', captureReferralFromUrl);     safeExecute('social badge', updateSocialConnectionBadge);
    safeExecute('render social hub', renderSocialHub);     safeExecute('sound UI', updateSoundUI);     safeExecute('default auth mode', () => setAuthMode("login"));     safeExecute('homepage delegation', installHomepageCoreDelegation);     safeExecute('bind events', bindEvents);
    safeExecute('bind sheet drag', bindSheetDrag);     safeExecute('touch hardening', installTouchHardening);     safeExecute('first paint bootstrap', installFirstPaintStabilizer);     safeExecute('first paint refresh', forceFirstPaintRefresh);     safeExecute('draw wheel', drawWheel);
    hydrateHomepageCore();     window.addEventListener('pageshow', () => hydrateHomepageCore(), { passive:true }); 
