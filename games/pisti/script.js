const PMGame=(()=>{const API=(window.__PLAYMATRIX_API_URL__||location.origin).replace(/\/$/,'');const uid=localStorage.getItem('pm_uid')||localStorage.getItem('pm_user_uid')||'guest';function h(){const v={'Content-Type':'application/json','x-playmatrix-user':uid};const t=localStorage.getItem('pm_session_token');if(t)v['x-session-token']=t;return v;}async function req(path,opts={}){const r=await fetch(API+path,{cache:'no-store',credentials:'include',...opts,headers:{...h(),...(opts.headers||{})},body:opts.body&&typeof opts.body!=='string'?JSON.stringify(opts.body):opts.body});const p=await r.json().catch(()=>({ok:false,error:'BAD_JSON'}));if(!r.ok||p.ok===false)throw Object.assign(new Error(p.error||`HTTP_${r.status}`),{payload:p,status:r.status});return p;}function fmt(n){return Number(n||0).toLocaleString('tr-TR');}function toast(m){const el=document.getElementById('toast');if(!el)return;el.textContent=m;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2400)}async function sync(){try{const p=await req('/api/me');const u=p.user||p.profile||p;document.querySelectorAll('[data-balance]').forEach(e=>e.textContent=fmt(u.balance||0));document.querySelectorAll('[data-level]').forEach(e=>e.textContent=u.level||u.accountLevel||1);document.querySelectorAll('[data-pct]').forEach(e=>e.textContent=(u.progressPercent||u.accountLevelProgressPct||0).toFixed?`${(u.progressPercent||u.accountLevelProgressPct||0).toFixed(1)}%`:`${u.progressPercent||0}%`);document.querySelectorAll('[data-progress]').forEach(e=>e.style.width=`${Math.max(0,Math.min(100,Number(u.progressPercent||u.accountLevelProgressPct||0)))}%`);document.querySelectorAll('[data-avatar]').forEach(e=>{if(u.avatar)e.src=u.avatar});return u}catch(e){return null}}return{req,fmt,toast,sync,uid};})();
document.addEventListener('DOMContentLoaded',()=>PMGame.sync());

let roomId=new URLSearchParams(location.search).get('room')||'';const $=id=>document.getElementById(id);async function lobby(){try{const p=await PMGame.req('/api/games/pisti/lobby');$('rooms').innerHTML=(p.rooms||[]).map(r=>`<div class="list-item"><b>${r.id}</b><span>${r.players}/${r.playerCount} · ${r.bet?PMGame.fmt(r.bet)+' MC':'Bahissiz'}</span><button class="btn secondary" data-join="${r.id}">Katıl</button></div>`).join('')||'<p class="muted">Aktif masa yok.</p>'}catch(e){PMGame.toast(e.message)}}async function state(){if(!roomId)return;try{const p=await PMGame.req(`/api/games/pisti/state?roomId=${encodeURIComponent(roomId)}`);render(p.room,p.hand||[])}catch(e){PMGame.toast(e.message)}}function card(c){return `<button class="playing-card" data-card="${c.id}">${c.rank}<br>${c.suit}</button>`}function render(r,hand){$('roomId').textContent=r.id;$('turn').textContent=r.turnUid||'-';$('pile').innerHTML=(r.pile||[]).slice(-4).map(card).join('');$('hand').innerHTML=hand.map(card).join('');$('score').textContent=(r.players||[]).map(p=>`${p.username}: ${p.score||0}`).join(' · ')}async function create(bot=false){try{const p=await PMGame.req('/api/games/pisti/create',{method:'POST',body:{bot,playerCount:2,bet:0}});roomId=p.room.id;await state();await lobby()}catch(e){PMGame.toast(e.message)}}async function quick(){try{const p=await PMGame.req('/api/games/pisti/quick',{method:'POST',body:{playerCount:2,bet:0}});roomId=p.room.id;await state();await lobby()}catch(e){PMGame.toast(e.message)}}document.addEventListener('click',async e=>{if(e.target.id==='create')create(false);if(e.target.id==='bot')create(true);if(e.target.id==='quick')quick();if(e.target.id==='refresh')lobby();if(e.target.dataset.join){roomId=e.target.dataset.join;await PMGame.req('/api/games/pisti/join',{method:'POST',body:{roomId}}).catch(x=>PMGame.toast(x.message));await state();}if(e.target.dataset.card){await PMGame.req('/api/games/pisti/play',{method:'POST',body:{roomId,cardId:e.target.dataset.card}}).catch(x=>PMGame.toast(x.message));await state();}});lobby();state();setInterval(state,2500);
