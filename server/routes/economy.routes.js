const express=require('express');const {requireAuth}=require('../core/security');const {adjustBalance}=require('../core/economyService');const router=express.Router();router.post('/economy/reward',requireAuth,async(req,res)=>{const amount=Math.max(0,Math.floor(Number(req.body.amount)||0));const reason=String(req.body.reason||'reward').slice(0,80);const key=`${req.user.uid}:${reason}:${String(req.body.rewardId||Date.now())}`;res.json(await adjustBalance(req.user.uid,amount,{reason,idempotencyKey:key}));});module.exports=router;
